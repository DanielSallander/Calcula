// FILENAME: app/src/addins/index.ts
// PURPOSE: Add-in loader - registers all enabled add-ins.
// CONTEXT: Called at application startup to load add-ins.

import { ExtensionRegistry } from "../core/extensions";

/**
 * Load all enabled add-ins.
 * Add-ins register themselves with the ExtensionRegistry.
 */
export function loadAddIns(): void {
  console.log("[AddIns] Loading add-ins...");

  // TODO: Import and register add-ins here as they are created
  // Example:
  // import { FormattingAddIn } from "./formatting";
  // ExtensionRegistry.registerAddIn(FormattingAddIn.manifest);

  console.log(`[AddIns] Loaded ${ExtensionRegistry.getRegisteredAddIns().length} add-ins`);
}
