// FILENAME: app/src/shell/App.tsx
// PURPOSE: Main application shell - minimal UI that hosts core and add-ins.
// CONTEXT: This is the entry point. It loads core components and registered add-ins.

import React from "react";
import { Layout } from "./Layout";
import { GridProvider } from "../core/state/GridContext";

export function App(): React.ReactElement {
  return (
    <GridProvider>
      <Layout />
    </GridProvider>
  );
}
