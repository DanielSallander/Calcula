# Bug Ledger

Bugs found by the automated soak/oracle system.
GENERATED from bug-ledger.json by tests/soak/bug-ledger.mjs — do not edit by hand.

Total: 125 | Open: 2 | Triaged: 0 | Fixed: 123 | Other: 0

## BUG-0086 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** media-ingress

A REFUSED IMAGE WAS STILL DELIVERED TO THE DECODER, so MAX_MEDIA_PIXELS -- the decompression-bomb defence, and the entire reason the header is parsed at all -- protected nothing on the inline path. Mechanism: legacy/distributed control payloads carry pictures as inline `data:image/*;base64,...`. Both migrations (migrate_legacy_data_urls for .cala, rewrite_inline_images for .calp) decoded each payload, ran inspect_media, and on ANY error counted `refused` and LEFT THE STRING EXACTLY WHERE IT WAS. That tolerance was designed for FORMAT refusals (an SVG the old picker accepted must not be destroyed by a later, narrower allowlist) but it was applied to the CAP refusals too. So a 30,000 x 30,000 single-colour PNG -- a few KB on the wire, 3.6 GB of RGBA at decode -- was refused entry to the media store, kept its place in the control property, materialized into the subscriber's own controls.json, and was handed to the WebView as <img src="data:...">, the one component with no caps at all. THE EXISTING TEST NAMED THIS EXACT CASE AND PASSED: `a_correctly_signed_decompression_bomb_inline_is_refused_not_admitted` asserted only `bytes.is_empty()` -- the store was clean and the renderer still got the bomb. THREE COMPOUNDING FACTS: (1) INVERTED CONTROL -- decode_image_data_url returns None for a payload over the byte cap, and None was read as 'leave it alone', so the BIGGER the payload the more certainly it survived, unbounded, into the saved document; (2) the 64 KiB property bound does not apply -- a pull calls materialize_saved_controls DIRECTLY, though MAX_CONTROL_PROPERTY_CHARS' own docstring claimed '.calp materialization' arrives at that door; (3) the write door was open too -- a bomb is SMALL, so set_control_property/set_control_metadata admitted one well inside 64 KiB, and nothing there ever consulted inspect_media. Travels inside a SIGNED .calp: the signature proves the publisher sent those bytes, never that they are a picture.

**Repro:** Unit: migrate_distributed_inline_images / migrate_legacy_data_urls with a control property holding data:image/png;base64,<30000x30000 IHDR> -- pre-fix the payload is returned unchanged and report.refused == 1; the same with a data: URL whose base64 exceeds MAX_MEDIA_BYTES survives verbatim at full length. Command door: check_property_value("src", <same bomb>) returns Ok pre-fix, the payload being ~60 bytes and the cap 64 KiB.
**Triage:** product-bug (confidence 1) — CONFIRMED from source and by sabotage. One conflation: 'this build will not re-admit it' was treated as a single outcome when it is two. A refusal on FORMAT (SvgRefused, BmpRefused, UnknownFormat, MalformedHeader, Empty) describes a picture that is safe to keep showing; a refusal on a CAP (TooLarge, DimensionOutOfRange, TooManyPixels) describes bytes whose decoding is itself the harm. Read tolerance was correct for the first class and was applied to both.
**Fix:** fixed — Refusal became COMPLETE instead of bookkeeping. A cap-refused inline payload is cleared to "" (the control keeps its identity, geometry and every other property and paints the honest 'No Image' placeholder -- deleting the control would silently change the sheet layout); a format-refused payload is still left inline and still renders, so no legacy picture is lost. The same judgement now runs on the .cala load, the .calp pull, and the two property commands.
  Files: core/calcula-format/src/media.rs (+MediaError::is_decode_hazard -- exhaustive match, so a new variant is a compile error rather than a silent default), app/src-tauri/src/media.rs (+InlineVerdict/judge_inline_image: ONE place decides admit/leave/drop for both corpora; +exceeds_byte_cap_encoded closes the inverted control; +is_hazardous_inline_image for the write door; MediaMigration gains `dropped`), app/src-tauri/src/controls.rs (check_property_size -> check_property_value, now also refusing the hazard class; corrected the docstring that wrongly claimed .calp materialization passes this door), app/src-tauri/src/persistence.rs (load-path log reports dropped)

## BUG-0059 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** collection-guard

THE COLLECTION GUARD FALSE-POSITIVELY FAILED EVERY SCENARIO RUN: 24 tests listed, 24 collected, all passed -- and the run exits 1 with '24 missing + 24 phantom'. Mechanism: listedIdentities used spec.file (where test() was CALLED -- scenarios/lib/scenario.ts for every lib-registered test) as the identity's file component, while the run side (collectedIdentities) uses titlePath()[2] -- the file-suite of the spec the runner imported (scenarios/budget-model.scenario.ts). For direct-registration projects the two spellings agree, which is why the guard's measured zero-false-positive claim (functional/visual/journey/invariant/soak) held; the scenario project was the one project never live-verified, and its lib-indirection is exactly where the two attributions diverge. A guard that cannot pass on a healthy suite trains people to expect red, which is how suppression starts.

**Repro:** Pre-fix: npx playwright test --project=scenario on a healthy tree -- all tests pass, run exits 1, guard names all 24 tests as both missing (under scenarios/lib/scenario.ts) and phantom (under the three .scenario.ts files). Unit-level: the lib-registered listing fixture in collectionGuard.test.ts.
**Triage:** test-bug (confidence 1) — CONFIRMED from source: identity attribution mismatch between the two sides of the comparison for lib-registered tests. The file-suite title is the run side's exact spelling (the guard's own docstring recorded the correspondence: 'the file-level suite's title ... corresponds to titlePath()[2]') -- the listing side simply did not use it.
**Fix:** fixed — One function: the listing-side identity now uses the same file attribution the run side has always used. The guard's strength is unchanged: a dropped or truncated spec file still produces missing identities under its own file-suite title, and the zero-byte/zero-test coverage checks are untouched.
  Files: app/e2e/collectionGuard.ts (listedIdentities takes the FILE-SUITE title as the identity file component, spec.file as fallback; spec.file remains the axis for the zero-test-file coverage check, where lib-indirection is deliberately detected and declined), app/e2e/__tests__/collectionGuard.test.ts (+2: the lib-registered fixture pins the file-suite attribution; the empty-title fallback pins spec.file)

## BUG-0023 `[fixed]`

**Found:** 2026-08-11 (review)
**Oracle:** visual-golden

The committed screenshot corpus is split across two capture paths. Decoding all 71 goldens: 44 hold the dpr-2 grid hairline (241,241,241) and 27 -- the whole e2e/visual tree, all re-recorded 2026-08-11 14:11-14:13 -- hold the dpr-1 hairline (226,226,226). The display is 200% (GDI DESKTOPHORZRES 2944 / HORZRES 1472 = 2), so dpr 2 is correct and the visual corpus encodes an environment this machine does not produce. e2e/captureEnvironment.ts declares devicePixelRatio 2 as the configuration EVERY committed golden was captured under; for 27 of 71 files that is false.

**Repro:** Run npm run e2e:visual on this machine. Every grid golden under e2e/visual differs from its capture by ~39,400 pixels against a 200-pixel budget, entirely in the gridline hairline. No product code is involved.
**Triage:** test-bug (confidence 0.95) — The visual corpus was re-recorded through a launch whose device pixel ratio was 1. The re-record absorbed no structural change: fitting the dpr-2 grid-empty-grid-default against the dpr-1 grid-core-empty-canvas leaves 0.83% residual, and it is a symmetric one-pixel gridline phase shift (1980 px of 241->255 against 1980 px of 255->226) plus text rasterization, spread over the whole frame with no localized bounding box.
**Fix:** fixed — The 27 e2e/visual goldens were re-recorded at dpr 2 on 2026-08-11 against a cold app of the fixed build (--project=visual --update-snapshots, 18/18). Decoding all 72 committed goldens afterwards reads dpr 2 on every one of them, so MIS_RECORDED_CORPORA is now empty and the BUG-0023 quarantine is deleted. The quarantine was designed to self-expire and it did: its two cases assert that every quarantined file still MEASURES recordedAt (1), so they would have failed the moment the files were corrected.
  Files: app/e2e/visual/__screenshots__/core-visual.spec.ts/*.png, app/e2e/visual/__screenshots__/workflow-visual.spec.ts/*.png, app/e2e/goldenCorpus.ts

## BUG-0022 `[fixed]`

**Found:** 2026-08-11 (review)
**Oracle:** undo-round-trip

`change_pivot_data_source` records no undo entry at all: repointing a pivot at a different source range is not undoable. Same discovery as BUG-0021.

**Repro:** Create a pivot, use Change Data Source to point it at a different range, press Ctrl+Z — the pivot keeps the new source.
**Triage:** app-bug (confidence 1) — CONFIRMED on the current tree: change_pivot_data_source recorded nothing at all, and it both rebuilds the cache from the new range and can overwrite cells it did not previously cover. Same root as BUG-0021 and fixed in the same pass.
**Fix:** fixed — The old definition AND the old cache are captured before the range is repointed, the cells the new (possibly larger) pivot overwrites are SAVED rather than merely counted, and one 'Change pivot data source' step is recorded after the work succeeds.
  Files: app/src-tauri/src/pivot/commands.rs (change_pivot_data_source snapshots definition+cache, saves the overwritten cells and records one step), app/src-tauri/src/undo_commands.rs (the cache-carrying snapshot it needs)

## BUG-0021 `[fixed]`

**Found:** 2026-08-11 (review)
**Oracle:** undo-round-trip

`update_bi_pivot_fields` records no undo entry at all: changing a BI pivot's fields is not undoable, and Ctrl+Z afterwards silently undoes whatever action came before it instead. Found by narrowing the blanket `pivots.` suppression that had hidden the whole pivot subtree from the undo round-trip oracle since 2026-06-11.

**Repro:** Create a pivot from a BI model, change its fields via update_bi_pivot_fields, press Ctrl+Z — the pivot keeps the new fields and an unrelated earlier edit is reverted instead.
**Triage:** app-bug (confidence 1) — CONFIRMED on the current tree, and the code said so itself: the auto-fit branch carried a comment reading 'DELIBERATELY DISCARDED ... this command records NO undo entry at all'. The command has FIVE mutating exits (cosmetic fast path, no-fields-clear, no-measures-save, failed-query-save, main path) and none recorded anything. A definition-only snapshot is not sufficient because four of the five replace the cache.
**Fix:** fixed — The pivot-definition undo snapshot grew an optional `cache`, and the restore puts it back when it is present. All five mutating exits of update_bi_pivot_fields now record ONE undo step, carrying the old cache (the four that replace it) and the saved overwritten cells and auto-fit column widths, so the resize undoes with the change that caused it instead of being discarded. The payload had THREE independent writers and a fourth private decoder in undo_pivot_overwrite; all four now go through one encoder/decoder in undo_commands, because a field added to one author is silently defaulted away in the others - which is exactly the failure `cache` would have had.
  Files: app/src-tauri/src/undo_commands.rs (PivotDefinitionSnapshot.cache + encode/decode + PIVOT_DEFINITION_RESTORE_KIND), app/src-tauri/src/pivot/commands.rs (record_pivot_definition_undo takes a cache; 5 exits of update_bi_pivot_fields record), app/src-tauri/src/mcp/objects.rs (json! literal -> the shared encoder), app/src-tauri/src/calp_commands.rs (local struct copy -> the shared encoder), app/src-tauri/src/pivot_undo_cache_tests.rs (new)

## BUG-0020 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 777)
**Oracle:** undo-round-trip

Conditional formatting rules are not registered in the undo system — a CF rule added during the window survives undo-all. Surfaced once the walker's cf.add-rule action used the correct internally-tagged serde shape ({type: "cellValue", ...}); before that the action failed silently and CF was never exercised.

**Repro:** add_conditional_format, then Ctrl+Z — the rule remains. Caught by the undo round-trip oracle (seed 777, 80 actions).
**Triage:** app-bug (confidence 0.9) — conditional_formatting.rs add/update/delete/reorder commands push no undo transactions. Fix with the obj_* swap pattern (snapshot the sheet's Vec<ConditionalFormatDefinition>, like obj_validation).
**Fix:** fixed — fixed 2026-08-11 — the CF commands (add/update/delete/reorder/clear) recorded no undo entry at all. They now snapshot the sheet's whole rule list as `obj_conditional_formats` and announce the `conditionalFormats` refresh domain. The ledger status lagged the fix; corrected while re-examining the oracle suppressions, which had already removed the BUG-0020 entry.
  Files: app/src-tauri/src/conditional_formatting.rs, app/src-tauri/src/undo_commands.rs

## BUG-0019 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** recalc-consistency

Second-order cross-sheet recalculation does not cascade: with Sheet2!B3 = Sheet1!C9 and Sheet1!C9 = SUM(C4:C8), editing Sheet1!C5 updates C9 (first-order, works after the BUG-0016 fix) but Sheet2!B3 keeps the stale value — update_cell's cascade only consults cross_sheet_dependents for the directly edited cell, not for cells recalculated as dependents.

**Repro:** Scenario budget-model phase 04: the commented-out B3/B4 assertions reproduce it. Sheet1: C9==SUM(C4:C8); Sheet2: B3==Sheet1!C9; edit Sheet1!C5 — B3 stays stale.
**Triage:** app-bug (confidence 0.9) — In commands/data.rs update_cell, the cross-sheet dependent propagation (the dep_sheet_idx block around line ~1240) runs only for the edited cell's direct cross-sheet dependents; cells recalculated in the local cascade (C9) never get their own cross_sheet_dependents looked up. Fix: after the local recalc loop, iterate the recalculated cells and propagate their cross-sheet dependents transitively.
**Fix:** fixed — Two independent causes, one per hop, both in cascade_cross_sheet_dependents. (1) The walk was rooted ONLY on the cells the caller edited; the cells the caller RECALCULATED were marked processed but never queued, so C9's cross-sheet dependents were never looked up (hop 1, exactly the triage hypothesis). Both sets are now walk roots. (2) The walk expanded a NON-ACTIVE sheet's same-sheet dependents through the ACTIVE sheet's dependents map — a map with no sheet dimension — so Sheet2!B4 = B2-B3 was invisible (hop 2). A per-sheet SheetDependencyIndex is now derived on demand from that sheet's own formula ASTs (cell + whole-column + whole-row edges) and expanded in topological order. Both hand-copied duplicates of the walk in update_cells_batch_core (paste) and fill_range were deleted in favour of the shared function, and a wiring test fails if a third copy appears.
  Files: app/src-tauri/src/commands/data.rs, app/src-tauri/src/calculation.rs, app/src-tauri/src/control_values.rs, app/src-tauri/src/commands/cross_sheet_recalc_tests.rs, app/e2e/scenarios/budget-model.scenario.ts

## BUG-0018 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** save-reload-round-trip

Freeze panes are lost across save/reload: freezeRow=1 before save, null after reopening the same .cala. The save side writes freeze (enrich_workbook_metadata populates sheet.freeze_row), so the load path likely fails to restore state.freeze_configs.

**Repro:** Freeze the top row, save to .cala, reopen — the freeze is gone. Caught by the save/reload round-trip oracle in scenario monthly-report phase 08.
**Triage:** app-bug (confidence 0.85) — open_file does not copy sheet.freeze_row/freeze_col back into state.freeze_configs (or the .cala reader drops them).
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: core/calcula-format/src/sheet_metadata.rs, core/calcula-format/src/zip_io.rs

## BUG-0017 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** undo-round-trip

set_freeze_panes does not register an undo transaction at all — freeze changes are invisible to Ctrl+Z. NOTE: Excel does not make freeze panes undoable either, so this may be expected behavior; user decision required (undo.freeze-panes). Originally misattributed as a redo bug; the freeze diff that surfaced in scenarios was the save/reload gap (BUG-0018).

**Repro:** Freeze the top row, Ctrl+Z (freeze removed, correct), Ctrl+Y (freeze NOT restored). Caught by the undo round-trip oracle in scenario monthly-report phase 08.
**Triage:** app-bug (confidence 0.3) — sheets.rs set_freeze_panes pushes no undo transaction. Could be declared Excel-parity expected behavior instead of fixed.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/sheets.rs, app/src-tauri/src/undo_commands.rs

## BUG-0016 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** recalc-consistency

Recalculation does not propagate to dependent formulas after a sheet switch: with =SUM(C4:C8) in C9 on Sheet1, add a second sheet, switch to it, switch back to Sheet1, then edit C5 — C5 shows the new value but C9 keeps its stale total (silently wrong results). Even an explicit full recalculation (calculate_now) leaves C9 stale, which suggests the edited cell landed in a grid copy that formulas do not read (state.grid mirror vs state.grids[i] desync after sheet switching), not just lost dependency edges.

**Repro:** Sheet1: data in C4:C8, C9 ==SUM(C4:C8). Add a sheet (auto-switch), click back to Sheet1's tab, edit C5 via update_cell — C9 does not change. Scenario budget-model phase 04 reproduces this (see the BUG-0016 workaround comment there).
**Triage:** app-bug (confidence 0.9) — AppState keeps an active-sheet mirror (state.grid) AND per-sheet storage (state.grids) which are swapped on sheet switch. After add-sheet + switch-back, update_cell writes and get_cell reads agree (C5 shows 6950) but calculate_now computes from a copy where C5 is still the old value — the mirror and grids[0] have diverged. Known stale-mirror hazard: see the comment in commands/data.rs get_watch_cells ('grids[active_sheet] is stale').
**Fix:** fixed — Same-sheet propagation after sheet switch fixed (calculate_now mirror sync + dependency rebuild on switch). Residual second-order cross-sheet cascade tracked as BUG-0019.
  Files: app/src-tauri/src/calculation.rs, app/src-tauri/src/sheets.rs, app/src-tauri/src/undo_commands.rs

## BUG-0015 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** undo-round-trip

Undo of pivot-table creation leaves the pivot definition behind in PivotState.pivot_tables (a 'ghost pivot'): after undo-all, the digest still contains the full pivot definition. This CONTRADICTS the [verified] behavior undo.pivot-filter ('pivot create/delete are all undoable') — possibly the UI creation path registers undo while the create_pivot_table command path does not, or undo restores the grid region but not the PivotState entry.

**Repro:** Invoke create_pivot_table + update_pivot_fields (as scenario monthly-report phase 05 does), then Ctrl+Z twice — get_all_pivot_tables still returns the pivot definition.
**Triage:** app-bug (confidence 0.7) — The pivot undo transaction restores cells/protected regions but does not remove the (definition, cache) entry from PivotState.pivot_tables, or the direct command path skips undo registration the UI path performs.
**Fix:** fixed — fixed (cause) — the ledgered path no longer leaves a ghost: `apply_pivot_create_restore` removes the entry from `PivotState.pivot_tables`. Re-examining the suppression found the blanket `pivots.` prefix had gone on to hide its SUCCESSORS: four pivot commands mutate `pivot_tables` and record no undo entry at all. `create_pivot_from_bi_model` (the same symptom on the BI path — a pivot Ctrl+Z could not remove) and `relocate_pivot` are fixed here. `update_bi_pivot_fields` and `change_pivot_data_source` remain OPEN as BUG-0021 and BUG-0022: both rebuild the cache, so they need a definition+cache snapshot rather than the definition-only one. `refresh_pivot_cache` records nothing and is correct — Excel does not undo a PivotTable refresh.
  Files: app/src-tauri/src/pivot/operations.rs, app/src-tauri/src/pivot/commands.rs, app/src-tauri/src/undo_commands.rs, app/e2e/oracles/knownIssues.ts

## BUG-0014 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** undo-round-trip

Undo of pivot-table creation does not restore column widths: creating a pivot auto-sizes its destination columns (e.g. col H -> 80.2px), but undoing the pivot creation leaves the new widths behind.

**Repro:** Create a pivot at H1 (columns auto-size), Ctrl+Z until the pivot is gone — column H keeps its pivot-fitted width. Caught by the undo round-trip oracle in scenario monthly-report phase 05.
**Triage:** app-bug (confidence 0.75) — Pivot render auto-fit sets column widths outside the pivot-create undo transaction.
**Fix:** fixed — fixed — `auto_fit_pivot_columns` wrote into `column_widths` / `all_column_widths` and recorded nothing on the undo stack. It now returns the widths it overwrote and `record_pivot_definition_undo` records them in the SAME transaction as the pivot change, as a new `pivot_col_widths` restore kind that is sheet-INDEXED (a pivot can render on a sheet the user is not looking at, where the fit writes `all_column_widths[dest]`). `None` means the column had no explicit width, so the restore removes the entry rather than writing a default.
  Files: app/src-tauri/src/pivot/operations.rs, app/src-tauri/src/pivot/commands.rs, app/src-tauri/src/undo_commands.rs, app/e2e/oracles/knownIssues.ts

## BUG-0013 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** save-reload-round-trip

A table's autoFilterId linkage is lost across save/reload (0 before save, absent after reload), breaking the table's filter-button/autofilter association. Root cause visible in code: saved_to_table in app/src-tauri/src/persistence.rs hardcodes auto_filter_id: None when restoring tables.

**Repro:** Create a table with showFilterButton, save to .cala, reopen — table.autoFilterId is gone. Caught by the save/reload round-trip oracle in scenario monthly-report phase 04.
**Triage:** app-bug (confidence 0.9) — SavedTable has no auto_filter_id field (or it is not persisted); saved_to_table sets auto_filter_id: None on load.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/persistence.rs

## BUG-0012 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** save-reload-round-trip

Sparkline groups are lost across save/reload: a sparkline group present in AppState.sparklines before save_file is gone after open_file of the same .cala file (either not written to the archive or not restored on load).

**Repro:** Create a sparkline group, save to .cala, reopen — the group is gone. Caught by the save/reload round-trip oracle.
**Triage:** app-bug (confidence 0.85) — collect_sparklines_for_save exists in build_workbook_for_save, but either save_file's path does not include it, the .cala writer skips workbook.sparklines, or the load path never restores them into AppState.sparklines. || RE-TRIAGED 2026-08-12: already fixed on the tree and never closed -- the same pattern as BUG-0024, a filing that outlived its defect. collect_sparklines_for_save is wired into build_workbook_for_save, zip_io.rs writes sparklines.json and reads it back unconditionally, and restore_sparklines puts them into AppState. Reading the source cannot settle that, which is the only reason this entry survived three handovers; it was RUN.
**Fix:** fixed — No product change was needed. Proved live on the real app: a real sparkline group, a real save_file to a real .cala, a real open_file, and the BACKEND'S OWN DIGEST on both sides -- the same instrument (save-reload-round-trip) that filed the bug. The digest hashes sheetIndex -> sorted groups_json, i.e. the group's LOCATION and SOURCE range, so it is strictly stronger than a count: a reload that restores an empty group at the wrong address would still fail. The spec also reads the .cala archive bytes directly and requires a sparklines.json entry, which separates 'the writer dropped it' from 'the reader dropped it' -- the two halves this bug's own triage could not tell apart. A non-vacuity assertion requires the digest to actually hold a group BEFORE the save, so the test cannot rot into a no-op.
  Files: app/e2e/journeys/sparkline-persistence.spec.ts (new, the proof)

## BUG-0010 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** recalc-consistency

Sorting rows that contain relative-reference formulas leaves the formulas inconsistent with their new positions: immediately after sort_range the displayed values are correct for the new row order, but a full recalculation (calculate_now) changes 7 cells — e.g. E2 went from 720 (= new row's C2*D2) to 75. Either sort fails to adjust formula references when moving rows, or it adjusts displayed values without updating the underlying formulas/dependency graph.

**Repro:** Run scenario data-cleanup up to phase 'sort by line total descending' with the recalc oracle enabled (remove the `oracles` override in app/e2e/scenarios/data-cleanup.scenario.ts). Minimal: put =C2*D2-style formulas in E2:E9, sort A2:E9 descending by column E, then calculate_now — values change.
**Triage:** app-bug (confidence 0.9) — commands::sort_range moves cell values/formulas between rows without rewriting relative references (or without rebuilding the dependency graph), so incremental state and a from-scratch recalculation disagree.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/commands/data.rs

## BUG-0011 `[fixed]`

**Found:** 2026-06-11 (scenario)
**Oracle:** save-reload-round-trip

save_file persists only the active sheet: saving a 2-sheet workbook and reopening it produced 45 digest differences (activeSheet, sheet list, second sheet's cells). Predicted by code reading: save_file uses single-sheet Workbook::from_grid (persistence.rs:1136) and enrich_workbook_metadata only populates sheets[0] (persistence.rs:355).

**Repro:** Create a second sheet with data, save to .cala, reopen — the second sheet's content is lost/misplaced. Run scenario budget-model phase 03 with the saveReload oracle enabled (remove the `oracles` override).
**Triage:** app-bug (confidence 0.95) — save_file builds the workbook from the single active grid instead of using the multi-sheet build_workbook_snapshot-style iteration over state.grids.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/persistence.rs

## BUG-0009 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 777)
**Oracle:** undo-round-trip

Redo of merge_cells does not restore the merged region. Undo correctly removes the merge, but redo leaves the cells unmerged. Minimized by ddmin to a SINGLE action (merge.merge), replay-confirmed.

**Repro:** `tests/regression/repros/BUG-0009.trace.json` (1 actions)
**Triage:** app-bug (confidence 0.95) — The redo path in undo_commands.rs apply_changes handles cell changes but does not re-apply the merged-region part of a merge transaction (UndoResult.mergeChanged flag exists, so undo-side handling is present; the redo direction misses it).
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/undo_commands.rs

## BUG-0007 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 424242)
**Oracle:** undo-round-trip

Named range create/delete is not restored by undo — a name defined during the window survives undo-all. NOTE: Excel does NOT make name definition undoable, so this may be expected behavior rather than a bug; needs a user decision (see undo.named-ranges in docs/expected-behavior.md).

**Repro:** create_named_range, then Ctrl+Z — the name remains. Caught by the undo round-trip oracle (seed 424242).
**Triage:** app-bug (confidence 0.5) — named_ranges.rs commands do not push undo transactions. Could equally be declared Excel-parity expected behavior — user decision required.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/named_ranges.rs, app/src-tauri/src/undo_commands.rs

## BUG-0008 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 424242)
**Oracle:** undo-round-trip

Data validation rules are not restored by undo — set_data_validation changes survive undo-all (Excel DOES undo validation changes).

**Repro:** set_data_validation on a range, then Ctrl+Z — the rule remains. Caught by the undo round-trip oracle (seed 424242).
**Triage:** app-bug (confidence 0.8) — data_validation.rs commands do not push undo transactions.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/data_validation.rs, app/src-tauri/src/undo_commands.rs

## BUG-0006 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 424242)
**Oracle:** undo-round-trip

Table create/delete is not registered in the undo system — after undo-all, a table created during the window still exists. Same unregistered-lifecycle class as charts (BUG-0001), sparklines (BUG-0002) and autofilters (BUG-0003). Note: slicers, pivots, ribbon filters and merges ARE undo-integrated (UndoResult has flags for them), so the fix pattern exists in the codebase.

**Repro:** Create a table (create_table), press Ctrl+Z — the table remains. Caught by the undo round-trip oracle (seed 424242, 40 actions).
**Triage:** app-bug (confidence 0.9) — tables.rs create_table/delete_table mutate TableStorage without pushing undo transactions, unlike slicer/pivot commands.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/tables.rs, app/src-tauri/src/undo_commands.rs

## BUG-0005 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 424242)
**Oracle:** undo-round-trip

Undo is sheet-unaware: after adding a sheet mid-window (auto-switching to it) and performing further actions, undo-all neither removes the added sheet nor restores the active sheet, and cell-level undo transactions appear to apply to whichever sheet is currently active rather than the sheet they were recorded on. 14 digest differences after undo-all: activeSheet 0->1, sheetNames[1] persists, multiple sheets[0] cells diverge.

**Repro:** `tests/regression/repros/BUG-0005.trace.json` (40 actions)
**Triage:** app-bug (confidence 0.85) — Undo transactions do not record a sheet index; apply_changes in undo_commands.rs operates on state.grid (the active-sheet mirror). Sheet add/delete/rename push no undo entries and do not clear the stack, so undo walks 'through' a sheet boundary applying changes to the wrong sheet. Either make sheet ops undoable with sheet-aware transactions, or (Excel parity) have sheet structural ops clear the undo stack.
**Fix:** fixed — ROOT: the third appearance of one missing dimension. `CellChange::SetCell` had already grown a `sheet` field, but the field holds an INDEX -- a POSITION, not an identity. Deleting, moving or copying a sheet renumbers every index after it, so a queued undo entry silently came to describe a different sheet and undo wrote onto a sheet the user never edited. `remap_sheet_keyed_stores` re-aimed the `CustomRestore` payloads and NOT the `SetCell` indices beside them in the same transactions, so it repaired half the problem. Two hazards were not about indices at all: a RENAME shifts nothing but rewrites every formula in the workbook, leaving queued `previous` cells holding ASTs that spell the old sheet name; and the width/height/merge/snapshot changes carry no sheet dimension AT ALL. VERIFIED EXCEL BEHAVIOUR: Excel does not let a sheet structural operation be undone -- deleting a worksheet is the documented case ('you can't undo deleting sheets') and no undo entry exists for inserting, renaming, moving or copying one either -- so a workbook-structure change ends the undo history. Under the standing 'Excel parity wins' rule that is now Calcula's behaviour, applied at ONE choke point that all five commands call, with a source census that fails by name when a command stops calling it. It also answers the target-sheet-deleted case with Excel's own answer instead of an invented one: the question cannot arise, because the delete ended the history that could have asked it. The MCP tool layer had been TELLING callers this all along ('Sheet structure changes are NOT undoable') while leaving the stack intact behind them. Removing the re-aim also removed a real lock-order inversion: it took `undo_stack` while `move_sheet`/`copy_sheet` held the grid pair, the reverse of `apply_changes`. Finally, the soak oracle could not tell 'a sheet operation ended the history' from 'the walk undid past its checkpoint' -- which is exactly how this bug was FILED -- so `UndoStack` now counts wholesale clears separately from cap evictions and `stepsBackToBaseline` names the right cause.
  Files: core/engine/src/undo.rs (UndoStack::cleared_total + counted clear(); visit_custom_restores DELETED), app/src-tauri/src/sheets.rs (invalidate_undo_history_for_sheet_structure + the five structural commands; remap_sheet_keyed_stores no longer re-aims queued undo entries; move_sheet/copy_sheet release their guards first), app/src-tauri/src/undo_commands.rs (UndoState.cleared_total), app/src-tauri/src/undo_sheet_structure_tests.rs (NEW: 16 tests incl. the census + its teeth), app/src-tauri/src/undo_sheet_domain_tests.rs (Fixture made pub(super) so the new file reuses it), app/src-tauri/src/formula_serialisation_tests.rs (free_function_bodies made pub(crate) for census reuse), app/src/core/lib/tauri-api.ts, app/e2e/oracles/types.ts, app/e2e/oracles/undoRoundTrip.ts, app/e2e/oracles/index.ts, app/e2e/__tests__/undoHistoryHorizon.test.ts (NEW: 9 vitest cases for stepsBackToBaseline)

## BUG-0003 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 424242)
**Oracle:** undo-round-trip

AutoFilter state is not restored by undo. After undoing all steps in a checkpoint window, the autoFilters digest section diverged from the baseline (filter applied via apply_auto_filter/set_column_filter_values survives undo-all).

**Repro:** Apply an autofilter (apply_auto_filter), then Ctrl+Z — the filter state remains. Caught by the undo round-trip oracle.
**Triage:** app-bug (confidence 0.85) — AutoFilter mutations (AutoFilterStorage in AppState) do not push undo transactions.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/autofilter.rs, app/src-tauri/src/undo_commands.rs

## BUG-0004 `[fixed]`

**Found:** 2026-06-11 (soak-walk, seed 424242)
**Oracle:** contextual-ribbon-tabs

File > New (new_file) does not fully reset workbook state: sparkline groups survive into the new workbook (frontend store and/or backend sparklines), and a stale Slicer contextual ribbon tab remained visible with zero slicers. Observed as cross-run state leakage in back-to-back walks.

**Repro:** Create a sparkline group and a slicer, File > New — the sparkline group still exists and the Slicer tab can remain visible.
**Triage:** app-bug (confidence 0.8) — new_file clears grid/tables/charts/etc. but not AppState.sparklines (confirmed by code reading: persistence.rs new_file clears charts at ~line 1791, sparklines absent). Frontend object stores are additionally not notified to reset on new_file.
**Fix:** fixed — Backend part: new_file now clears state.sparklines (one line next to the charts clear). Frontend-store notification on new_file remains open as a follow-up.
  Files: app/src-tauri/src/persistence.rs

## BUG-0001 `[fixed]`

**Found:** 2026-06-10 (invariant-walk, seed 1781119899201)
**Oracle:** undo-round-trip

Undoing all steps did not remove charts created during the window. Chart entries (save_chart / chart delete) are not registered in the undo system, so Ctrl+Z never affects charts.

**Repro:** Create a chart (save_chart), press Ctrl+Z — the chart remains. Caught by the undo round-trip oracle at the first 25-action checkpoint.
**Triage:** app-bug (confidence 0.95) — AppState.charts (Vec<ChartEntry>, opaque JSON) is mutated by chart commands without pushing an undo Transaction. The undo Transaction model has flags for pivot/slicer changes but no chart support.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/chart_commands.rs, app/src-tauri/src/undo_commands.rs

## BUG-0002 `[fixed]`

**Found:** 2026-06-10 (invariant-walk, seed 1781119899201)
**Oracle:** undo-round-trip

Sparkline group create/delete is suspected to bypass the undo system (same storage pattern as charts: AppState.sparklines opaque JSON entries). Surfaced in the same oracle failure as BUG-0001 (4-6 diffs).

**Repro:** Create a sparkline group via __CALCULA_SPARKLINES__, press Ctrl+Z — the group likely remains. Needs confirmation once BUG-0001 suppression isolates remaining diffs.
**Triage:** app-bug (confidence 0.7) — Same pattern as BUG-0001 for AppState.sparklines.
**Fix:** fixed — Fixed in the 2026-06-11 fix campaign; validated by oracle walk (seed 424242), BUG-0009 repro replay, and the scenario suite.
  Files: app/src-tauri/src/sparkline_commands.rs, app/src-tauri/src/undo_commands.rs

## BUG-0024 `[fixed]`

**Found:** 2026-08-11 (review)
**Oracle:** visual-golden

The committed screenshot corpus is ALSO split on a second axis that the device-pixel-ratio census structurally cannot see: the COLOUR PROFILE. Measured on a cold --project=functional run of the fixed build: 512 passed / 31 failed / 11 skipped, and all 31 failures are goldens with NONE of them dpr (every failing pair holds the same hairline on both sides). The app declares #217346 (33,115,70) and #10b981 (16,185,129); a capture under --force-color-profile=sRGB reproduces those bit-exactly and the committed goldens hold 63,112,75 / 95,180,134, the wide-gamut transform of them. The pin reached the manual launch path for the first time when e2e/webview2Args.mjs became the single definition of the WebView2 arguments (register 3by section 5), and the manual path is how every golden in this tree was recorded -- so the whole corpus predates the pin. The profile transform is the IDENTITY on neutrals and both hairline constants are neutral, which is exactly why the dpr census reported a clean corpus over this.

**Repro:** Run --project=functional on this machine before the re-record. statusbar-text-range alone moves 30,683 px (99.88% of the capture) against a 15-pixel budget, and 29,569 of them are the single mapping 63,112,75 -> 33,115,70. No product code is involved.
**Triage:** test-bug (confidence 0.99) — The goldens were recorded before --force-color-profile=sRGB was actually delivered on the manual launch path. The direction is proved from source rather than assumed: 33,115,70 and 16,185,129 are the literal colours the application declares (uiTypes.ts, ribbonIcons.tsx, darkTheme.ts), so the PINNED capture is the faithful one and the committed golden is the stale one. Structurally safe to re-record: reducing each failing pair to a colour relation gives a functional share of 0.9666..1.0000 with fan-out 1 on every dominant mapping and no compact bounding box, which is a recolouring and not a displacement.
**Fix:** fixed — Re-recorded with --update-snapshots=changed (never 'all', which would rewrite goldens that currently pass and discard the evidence they represent), and added the missing SECOND census axis so the same class cannot recur silently. readColourProfile abstains on neutrals rather than counting them as agreement; describeProfileSplit fires in BOTH directions, so a LOST pin is reported as a launcher fault instead of being re-recorded into every golden.
  Files: app/e2e/tests/__screenshots__/**/*.png (38 goldens), app/e2e/scenarios/__screenshots__/**/*.png (3 goldens), app/e2e/goldenCorpus.ts (readColourProfile + describeProfileSplit), app/e2e/captureEnvironment.ts (CAPTURE_ENVIRONMENT.colourProfile), app/e2e/__tests__/goldenCorpus.test.ts

## BUG-0025 `[fixed]`

**Found:** 2026-08-11 (e2e)
**Oracle:** vba-idioms-wave3

vba-idioms-wave3 #5 retypes the token SEEDREF to A1 in the macro editor and then reads the stored module source. Expected substring 'const jump = "A1"'; the store held 'const jump = "A"' -- the final keystroke is missing. Passed in both earlier full ordered runs the same evening; 1 failure in 3.

**Repro:** Run --project=functional in its natural order. Running the spec ALONE is NOT a reproduction: it depends on sheets earlier specs create and fails at line 1080 with Expected 1 / Received 0, a different assertion entirely.
**Triage:** app-bug (confidence 0.97) — SEPARATED: hypothesis (b). The measurement the filing named -- read the Monaco buffer at the moment the assertion fails -- was taken deterministically instead of by re-running a 1-in-3 flake: a vitest reproduction of the exact sequence (type a character, let the 400ms idle window elapse, hold the store write open, type the next character while it is in flight, release) reproduces the symptom exactly. The store held 'A', the BUFFER held 'A1', and the live chip read 'live'. So no keystroke was dropped and this is NOT the grid's type-to-open defect; the editor's live indicator claimed the store held the buffer while a write was still owed. Corroborated by the harness's own history: two of the six copies of retypeToken's caller already carried a comment describing this race and worked around it by polling the store, and those two are green while the copy without the workaround is the flake.
**Fix:** fixed — PRODUCT: a persist outcome describes the bytes it wrote, not the bytes on screen, so the live chip is now derived from the persister's own buffer-vs-store comparison at the moment the outcome lands and reads 'Saving...' while a later keystroke is still unstored. The same staleness had a destructive second face: a 'compiled' outcome replaced the buffer with the stored JavaScript even when the author had typed since, deleting those keystrokes and desynchronising the persister (setSource never reaches it); outcomes now carry the `input` they compiled FROM, the swap is skipped when the screen has moved on, and when it does happen the persister is told via a new `adopt`. HARNESS: retypeToken's six byte-identical copies (plus a seventh editorText and inline indicator locators) became one e2e/helpers/macroEditor.ts, whose failure message prints the chip state, the Monaco buffer and the stored source -- so the separating measurement is permanent instead of costing a 38-minute run.
  Files: app/extensions/ScriptableObjects/components/ObjectScriptEditorApp.tsx, app/extensions/ScriptableObjects/lib/liveModuleBuffer.ts, app/extensions/ScriptableObjects/__tests__/liveMacroEditing.test.tsx, app/extensions/ScriptableObjects/lib/__tests__/liveModuleBuffer.test.ts, app/e2e/helpers/macroEditor.ts (new), app/e2e/tests/macro-live-edit.spec.ts, app/e2e/tests/vba-idioms-wave1.spec.ts, app/e2e/tests/vba-idioms-wave2.spec.ts, app/e2e/tests/vba-idioms-wave3.spec.ts, app/e2e/tests/vba-idioms-wave4.spec.ts, app/e2e/tests/vba-wiring-batch.spec.ts, app/e2e/tests/macro-editor-inventory.spec.ts, app/e2e/tests/macro-link-model.spec.ts

## BUG-0026 `[fixed]`

**Found:** 2026-08-11 (soak, seed 20260812731)
**Oracle:** contextual-ribbon-tabs

Contextual tab "Slicer" is visible but at least one slicer must exist. Found: slicers=0, charts=0, tables=0, pivots=0, timelines=0, sparklines=0

**Repro:** `app/e2e/results/soak/failures/2026-08-11T21-37-06-340Z-soak-contextual-ribbon-tabs/minimized.trace.json` (3 actions)
**Triage:** product-bug (confidence 0.95) — THE FRONTEND NEVER HEARS ABOUT A CASCADED DELETE. SlicerEvents.SLICER_DELETED is dispatched from exactly one place in the tree -- deleteSlicerAsync in extensions/Slicer/lib/slicerStore.ts -- which is the FRONTEND delete path (context menu, Options pane). Slicer/index.ts listens for that event and calls deselectSlicer(), with a comment saying 'without this, deleting a selected slicer leaves the Options tab visible with no slicer to configure' -- i.e. the defect was already known and fixed for the DIRECT path. Deleting the owning TABLE removes the slicer through the register-3bt object-dependency cascade, which runs in the backend and never passes through deleteSlicerAsync, so no event is raised, the selection is never cleared, and the contextual tab survives its subject. This is the frontend half of the 3bt cascade and the object-deps census cannot see it: that census checks that a Rust delete command DECLARES what happens to its dependents, not that the UI is told when a dependent dies.
**Fix:** fixed — SLICER_DELETED was dispatched from deleteSlicerAsync alone -- the one route a user takes by hand -- so every backend cascade removed the slicer and told the UI nothing, and the contextual tab is a function of the selection. Fixed in the STORE, not at the call site: refreshCache diffs the id set it just read against the one it held and announces whatever vanished, which covers every route including ones that do not exist yet, prunes the leaked items cache by the same diff, and keeps a multi-selection's survivors. The identical defect in TimelineSlicer (never reported) and a partial one in Pivot are fixed with it. The direction nothing had covered: an MCP tool is not invoked BY the frontend, so four bespoke per-kind Tauri events -- one of which, sheets:refresh, had no listener anywhere in the app -- were replaced by one announce_cascade -> mutation:refresh bridged into the same MUTATION_DOMAIN_EVENTS fan-out. Deriving the domain list transitively from DEPENDENCY_MATRIX found three live backend orphans: delete_sheet never ran cascade_deleted_charts, cascade_deleted_sources never ran cascade_deleted_slicers, and neither pruned object scripts.
  Files: app/src-tauri/src/object_deps.rs (UiDomain, dependent_kind, cascade_domains, announce_cascade, delete_sheet/cascade_deleted_sources transitive fixes), app/src-tauri/src/object_deps_census_tests.rs, app/src-tauri/src/undo_commands.rs (MutationDomain now aliases object_deps::UiDomain), app/src-tauri/src/mcp/objects.rs, app/src-tauri/src/mcp/tools.rs, app/src/shell/bootstrap.ts (mutation:refresh bridged into MUTATION_DOMAIN_EVENTS), app/src/api/__tests__/cascadeAnnouncementCensus.test.ts, app/extensions/Slicer/lib/slicerStore.ts, app/extensions/Slicer/handlers/selectionHandler.ts, app/extensions/TimelineSlicer/lib/timelineSlicerStore.ts

## BUG-0027 `[fixed]`

**Found:** 2026-08-11 (e2e, seed 1786456498740)
**Oracle:** harness

An invariant walk stopped at [step 47/75] ribbon.switch-tab and printed nothing for TWELVE MINUTES while the app stayed alive, Responding=True, CPU flat, CDP answering. Not the lock-order wedge (that stops the WebView2 message pump; this did not).

**Repro:** E2E_MANUAL=1 INVARIANT_SEED=1786456498740 npx playwright test --project=invariant --grep "random action sequence" with actionTimeout unset.
**Triage:** test-bug (confidence 0.99) — TWO DEFAULTS MEETING. (1) Playwright's default actionTimeout is 0 -- NO timeout -- so locator.click() waits for actionability forever; playwright.config.ts set expect.timeout and never set actionTimeout, and walker/actionCatalog.ts has 5 bare .click() calls, ribbon.switch-tab among them (it probes isVisible({timeout:500}) and then clicks unbounded, so a visible-but-not-actionable button parks it). (2) state-consistency.spec.ts raises its own ceiling with test.setTimeout(1_500_000), so the project's 300s does not apply and the hang had 25 minutes to run in. A hang is invisible to an exit-status check.
**Fix:** fixed — use.actionTimeout = 30_000 and use.navigationTimeout = 60_000, with the measurement recorded beside them. 30s is far above the slowest legitimate action in these suites, so it cannot turn a slow action into a false failure; it turns an infinite one into a reported failure that names the locator.
  Files: app/playwright.config.ts

## BUG-0028 `[fixed]`

**Found:** 2026-08-11 (e2e)
**Oracle:** visual-golden

4 of the 27 e2e/visual goldens are a function of app WARMTH rather than of the page. --project=visual is 18/18 twice on a warm app (one that has already run functional/scenario/journey) and 14/18 then 15/18 on a cold-launched one. menu-file-open, menu-edit-open and menu-data-open fail on cold in BOTH cold runs and pass on warm in ALL THREE warm runs -- deterministic per condition, opposite between conditions. core-empty-grid failed the FIRST capture after a cold start and passed the second, so it is not even stable within one condition.

**Repro:** Run --project=visual against an app that has already run other suites: 18/18. Kill the app, cold-launch it, run --project=visual as the first suite on the instance: 4 fail, then 3 fail on an immediate re-run.
**Triage:** test-bug (confidence 0.8) — NOT a recolouring and therefore not a capture-path artifact: fan-out reaches 22 and saturated colours map to greys (37,98,156 -> 110,110,110; 96,37,38 -> 65,65,66; 193,153,97 -> 153,153,154), i.e. coloured menu iconography present in the golden and grey in the cold capture -- consistent with an icon font or accent asset that only resolves once the app has been exercised, though that specific mechanism is a hypothesis and was not confirmed. core-empty-grid is separate: its dominant transitions are 221,245,237 -> 255,255,255 (510 px) and 128,217,188 -> 255,255,255 (80 px), a pale green highlight present in the golden and absent from the first cold capture -- a highlight that has not settled, the same class as the FormulaInput selection race (register 3bw).
**Fix:** fixed — TWO defects, and 'warmth' was neither time nor fonts. (a) The three menu-* goldens differ by TEXT ANTIALIASING, decided per composited layer: an accelerated 2D canvas is its own layer, an overlay that overlaps it is composited too, and Chromium refuses LCD text on a layer it cannot prove opaque. The app de-accelerates its own canvas through GridCanvas.captureRange, so 'warm' meant 'some earlier suite exercised the capture seam'. Measured on one cold app: 7 layers and 0 chromatic pixels before 120 getImageData calls, 5 layers and 1575 after. Fixed with --disable-accelerated-2d-canvas, which pins every golden to the side it was already recorded on (zero re-records for that axis) rather than --disable-lcd-text, which would have turned all 89 goldens grey. (b) core-empty-grid was a PRODUCT bug: useHomeTabState keyed its style read on the selection alone and returned without clearing when getCell resolved to null, so an empty cell inherited the previous cell's ribbon state and File > New / undo / Clear-Formats never re-read at all.
  Files: app/e2e/webview2Args.mjs (--disable-accelerated-2d-canvas in DETERMINISTIC_CAPTURE_FLAGS), app/e2e/captureEnvironment.ts (compositedCanvasLayers axis), app/e2e/helpers/screenshots.ts (CDP LayerTree reader), app/extensions/BuiltIn/HomeTab/components/useHomeTabState.ts, app/e2e/visual/workflow-visual.spec.ts, 5 goldens re-recorded: core-empty-grid, ribbon-core-default-ribbon, menu-file-open, menu-edit-open, workflow-multisheet-sheet1

## BUG-0029 `[fixed]`

**Found:** 2026-08-12 (review)
**Oracle:** visual-golden

tests/__screenshots__/grid-rendering.spec.ts/empty-grid-full-window.png photographs a product state the build no longer produces. BUG-0028's product fix (useHomeTabState clears the ribbon when getCell resolves to null) was verified by re-recording five goldens in the VISUAL project; the FUNCTIONAL project was not re-run and still holds a capture of the same brand-new workbook, through the same resetToNewWorkbook helper, with the Center-Vertically toggle latched. Measured off the committed bytes: 546 pressed-accent pixels where the re-recorded sibling core-empty-grid.png measures 36. A pixel-for-pixel fit of the two 1280x800 captures leaves 2,173 differing pixels of which 590 are exactly this box (510 of 221,245,237 -> 255,255,255 plus 80 of its 128,217,188 border) inside x 128..441 / y 69..94. The comparator budget is min(200, 0.0005 * 1,024,000) = 200, so it cannot pass.

**Repro:** Run --project=functional after the BUG-0028 product fix; 'empty grid renders correctly with headers and gridlines' fails on its second capture. No run is needed to see it: app/e2e/__tests__/goldenCorpus.test.ts now reads the state off the committed bytes, and removing the BUG-0029 entry from STALE_PRODUCT_STATE_GOLDENS fails 'shows an UNLIT Home tab on every golden taken on a brand-new workbook' naming the file and the count.
**Triage:** test-bug (confidence 0.97) — A product fix invalidates goldens in EVERY project that photographs the affected surface, and the tree had no way to say which. Both existing corpus censuses ask which MACHINE took the picture (device pixel ratio, colour profile); neither can see which BUILD did. Added as a third axis rather than a note: readPressedAccentFill recovers 'was a Home-tab toggle latched' from a golden's bytes, EMPTY_DOCUMENT_GOLDENS declares the population and is checked against the SPEC source so it cannot claim a populated capture is empty, and STALE_PRODUCT_STATE_GOLDENS is a two-sided quarantine that expires the moment the file is re-recorded.
**Fix:** fixed — Re-recorded on a COLD app inside a full ordered --project=functional run with --update-snapshots=changed, which rewrote exactly three files and nothing else. THE PREDICTION WAS EXACT: the live comparator reported 2,173 differing pixels, the same 2,173 computed off the committed bytes with no run, and decoding the pair gives 510 of the predicted 221,245,237 fill against a 200-pixel budget. The file now measures 36 pressed-accent pixels, the same 36 its re-recorded sibling core-empty-grid.png holds. The STALE_PRODUCT_STATE_GOLDENS quarantine then did what it was built to do: it requires its entry to STILL measure 546, so re-recording the file correctly turned the census RED on its own entry rather than letting it rot. The entry is deleted and the array is empty. One detector self-test that used this file as its LIT example failed with it, correctly, and now uses sheets-default-tabs.png, which is lit legitimately (its capture follows a setCellValue) and will still be a valid example next year.
  Files: app/e2e/tests/__screenshots__/grid-rendering.spec.ts/empty-grid-full-window.png, app/e2e/goldenCorpus.ts, app/e2e/__tests__/goldenCorpus.test.ts

## BUG-0030 `[fixed]`

**Found:** 2026-08-12 (e2e)
**Oracle:** visual-golden

tests/__screenshots__/ribbon-tabs.spec.ts/ribbon-ribbon-tab-insert.png and tests/__screenshots__/tables.spec.ts/grid-tables-after-create.png were recorded BEFORE BUG-0028 --disable-accelerated-2d-canvas pin and hold the OTHER side of it. ribbon-ribbon-tab-insert: 1,145 differing pixels against a 200-pixel budget, every top transition a neutral grey mapping to a chromatic one (153,153,154 -> 193,153,97; 110,110,110 -> 37,98,156) -- the golden holds GRAYSCALE-antialiased text, the pinned build paints LCD. grid-tables-after-create: a 199x88 crop whose budget is min(200, 0.0005*17512) = 8.75 px; 36 pixels cross the YIQ gate and the actual is systematically about 1 unit lighter (mean signed delta R +1.44 G +1.07 B +0.57, max 28), which is the GPU-vs-CPU 2D canvas rasterizer. Both diffs are BYTE-IDENTICAL across two independent cold runs.

**Repro:** Run --project=functional from a cold app with the flag on. Deterministic: identical diffs on two independent cold runs (532/11/11, then 540/3/11 after the BUG-0033 fix). No run is needed to see the ribbon half: e2e/__tests__/goldenCorpus.test.ts now measures it off the committed bytes -- the ribbon-strip cohort other seven members sit at 3.8% mid-tone-neutral and this one at 44.8%, a ratio of 11.9x.
**Triage:** test-bug (confidence 0.97) — BUG-0028 pinned a real axis with --disable-accelerated-2d-canvas and stated in webview2Args.mjs that the flag leaves every committed golden on the side it was recorded on, so it costs no re-record at all. That sentence was reached by measuring exactly ONE candidate golden (autocomplete-dropdown-visible) in the two projects that were not re-run. It is false: two functional goldens sit on the other side. This is the twin of BUG-0029 one axis over -- BUG-0029 is a PRODUCT fix invalidating goldens in projects nobody re-ran, this is an ENVIRONMENT pin doing the same. Both generalised from a single measured file.
**Fix:** fixed — Both re-recorded under the pin, on a COLD app, inside a full ordered run, and verified by a further cold run (543 passed / 0 failed / 11 skipped). A FOURTH corpus axis was built to catch the class ahead of a run and then DELETED, which is the more useful half of this entry. The reader restricted the denominator to mid-tone pixels (luminance 40..200, where glyph edges live) and compared inside a cohort of same-element captures; it separated 11.9x against the 4x margin, it fired on the real corpus naming the file, and then the re-record disproved it: the CORRECT recording measures 29.3% against its cohort's 3.8% and the stale one's 44.8%, i.e. 7.7x from its own cohort and only 1.5x from the defect it was meant to distinguish. Part of the Insert ribbon is composited for its own CONTENT reasons in the correctly-pinned build, so a cohort-agreement rule states something true that is not a corpus defect and would have been permanently red. The measurement, including the one number the earlier rejection could not have had (the same file recorded both ways), is written into goldenCorpus.ts so this is not attempted a third time. The guard for this class is a PROCESS rule, not a reader: when a capture pin changes, every project has to run.
  Files: app/e2e/tests/__screenshots__/ribbon-tabs.spec.ts/ribbon-ribbon-tab-insert.png, app/e2e/tests/__screenshots__/tables.spec.ts/grid-tables-after-create.png, app/e2e/goldenCorpus.ts

## BUG-0031 `[fixed]`

**Found:** 2026-08-12 (review)
**Oracle:** walker-action-catalog

e2e/walker/actionCatalog.ts chart.create wrote the chart with a raw save_chart Tauri invoke and told the chart STORE nothing. Measured on a running app: get_charts answered 2 while __CALCULA_CHARTS__.getAllCharts() answered 0. The next two actions read the STORE, so chart.select -- whose entire job is to raise the Chart Design contextual tab -- selected nothing and returned successfully, chart.delete deleted nothing and returned successfully, and getCurrentChartId() stayed null throughout. Their preconditions are satisfied from the BACKEND count, so the generator kept issuing them and the walk kept reporting them executed.

**Repro:** On a running app, invoke save_chart directly and then read __CALCULA_CHARTS__.getAllCharts(): backend 2, store 0. chart.select is then a no-op and no contextual tab ever appears.
**Triage:** test-bug (confidence 0.98) — The lesson table.delete had already learned in this same file (THE PRODUCT OWN DELETE, not a raw delete_table invoke) was never applied to charts. The consequence is the shape this program keeps deleting: the contextual-ribbon-tabs invariant -- the very invariant that found BUG-0026 -- could NEVER observe a chart tab, because nothing in the catalog could raise one. Section 3cd matrix lists Chart as already correct, which is a reading of the source; the walk that would have tested it was structurally unable to.
**Fix:** fixed — chart.create goes through the store own createChart (what Insert > Chart calls), which pushes into the store AND persists through the same save_chart, so the backend sees exactly what it saw before and the store is no longer blind. Proved live by the positive control in cascade-announcement-live.spec.ts: before the fix, the assertion that a selected chart must raise a chart contextual tab FAILED with Received: []; after it, the tab appears and the sheet-delete cascade removes it.
  Files: app/e2e/walker/actionCatalog.ts, app/e2e/journeys/cascade-announcement-live.spec.ts

## BUG-0032 `[fixed]`

**Found:** 2026-08-12 (review)
**Oracle:** state-snapshot

captureSnapshot().logical.activeSheet has been a CONSTANT 0 for the whole life of e2e/invariants/stateSnapshot.ts. It read gridState.activeSheet, which does not exist -- the grid state exposes sheetContext.activeSheetIndex -- so the ?? 0 defaulted every time. Probed on a running app sitting on Sheet2: sheetContext.activeSheetIndex was 1 and the field said 0.

**Repro:** Switch to any sheet other than the first and read captureSnapshot().logical.activeSheet. It answers 0.
**Triage:** test-bug (confidence 1) — A defaulted read of a misspelled key is indistinguishable from a correct read of a true value, which is why it survived. The field appears in every soak and invariant failure bundle and in every minimized trace, so every triage that reasoned about which sheet was active was reasoning from a constant.
**Fix:** fixed — Falls back through sheetContext.activeSheetIndex, keeping the old key first so a rename in the other direction is picked up rather than silently zeroed. Asserted live: adding a sheet switches to it, and the journey spec requires snapshot.logical.activeSheet to equal the new index.
  Files: app/e2e/invariants/stateSnapshot.ts, app/e2e/journeys/cascade-announcement-live.spec.ts

## BUG-0033 `[fixed]`

**Found:** 2026-08-12 (e2e)
**Oracle:** functional-suite

Three MCP specs asserted Tauri events that BUG-0026 DELETED -- tables:refresh, pivots:refresh, named-ranges:refresh -- so all three failed at step 3. Worse, mcp-create-table and mcp-create-pivot removed the object they created only in step 4 undo, INSIDE the try, after the assertion that threw; their finally blocks cleared the seeded cells and left the OBJECT. The AI-created table then sat at A1:B3 for the rest of the ordered run with its header fill, its banded rows and its Table Design contextual ribbon tab, and took later tests down with it -- paste-special x2 and scrolling photographed the blue table where their goldens hold white, and two ribbon-tabs goldens differed by exactly the 68x12 px of the extra tab label. Three root failures presented as eleven.

**Repro:** Run --project=functional in its natural order after BUG-0026. Root-versus-cascade was proved by measurement rather than argument: fixing ONLY the three MCP specs took the same ordered cold run from 11 failures to 3.
**Triage:** test-bug (confidence 0.99) — BUG-0026 replaced five bespoke per-kind Tauri announcements with one mutation:refresh carrying domains and source, fanned out by the Shell through MUTATION_DOMAIN_EVENTS. The PRODUCT is correct -- verified live: the backend announces the right domain and the Shell dispatches the feature event the extension consumes. The tests named a transport that no longer exists, and nothing unit-tier could see it, because the contract they assert is a live Tauri event. The cleanup half is independent and worse: a cleanup that runs only when the assertions passed is not a cleanup.
**Fix:** fixed — Each spec now asserts BOTH halves, and they fail for opposite reasons: that the BACKEND announced (a mutation:refresh arrived naming the domain ObjectKind maps to in object_deps::ui_domain -- objects / pivot / namedRanges), and that the UI HEARD IT (the Shell fanned out TABLE_DEFINITIONS_UPDATED / pivot:refresh / NAMED_RANGES_CHANGED, which is what actually decides whether the user sees the object). Naming the transport alone could never have distinguished a broken bridge from a silent backend. The table and the pivot are now removed BY NAME in finally, unconditionally.
  Files: app/e2e/tests/mcp-create-table.spec.ts, app/e2e/tests/mcp-create-pivot.spec.ts, app/e2e/tests/mcp-create-named-range.spec.ts

## BUG-0034 `[fixed]`

**Found:** 2026-08-12 (code-review)
**Oracle:** undo-round-trip

Undo of a non-cell change lands on the ACTIVE sheet rather than the sheet it was recorded on. CellChange::SetColumnWidth / SetRowHeight / AddMergeRegion / RemoveMergeRegion / RestoreSnapshot carried no sheet dimension at all, and apply_changes wrote them into state.column_widths / state.row_heights / state.merged_regions / state.grid unconditionally. The severe case is RestoreSnapshot, which replaces a WHOLE grid: insert a row on Sheet2, click the Sheet1 tab, press Ctrl+Z, and Sheet1's entire cell map is replaced by Sheet2's saved one -- silently, with no error and no way back.

**Repro:** Three ordinary actions. Rust reproduction: undo_commands::undo_sheet_domain_tests::a_structural_snapshot_undo_does_not_replace_the_active_sheets_whole_grid (and three siblings for widths, heights and merges). Each fails when the off-sheet routing in apply_changes is disabled, and the control test the_active_sheet_path_is_unchanged_by_the_off_sheet_one still passes -- i.e. only the previously-broken branch changed.
**Triage:** app-bug (confidence 1) — Same missing dimension as BUG-0005, on the changes that never got one. 'The active sheet' is true when these are RECORDED and need not be true when they are RESTORED. Found while sweeping BUG-0005's family; not reachable by that bug's fix, because no sheet structural operation is involved.
**Fix:** fixed — The four variants now carry the sheet they were recorded on, exactly as SetCell does. When that sheet IS the active one the arms behave byte-identically to before -- which is what makes the change safe without a live run -- and when it is not, the restore is queued into the pass's existing DEFERRED phase and applied to all_column_widths / all_row_heights / all_merged_regions / grids[sheet] there. Deferred rather than inline because apply_changes holds the active mirrors for its whole pass and set_active_sheet takes mirror-then-all in every case, so reaching for the all- stores inline would both invert the lock order and deadlock against the pass's own non-reentrant guards (the same trap the pivot column-width restore fell into). The off-sheet snapshot path also runs record_subscription_override_edits for its own sheet, so it does not repeat the gap the JSON-payload sheet_structural_snapshot path has.
  Files: core/engine/src/undo.rs (sheet on GridSnapshot, SetColumnWidth, SetRowHeight, AddMergeRegion, RemoveMergeRegion; recorders take it), app/src-tauri/src/undo_commands.rs (OffSheetGeometry + apply_off_sheet_geometry, run in the DEFERRED phase), app/src-tauri/src/commands/dimensions.rs, app/src-tauri/src/merge_commands.rs, app/src-tauri/src/commands/structure.rs (recorders stamp the active sheet), app/src-tauri/src/undo_sheet_domain_tests.rs (GAP 4: 5 tests incl. the active-path control)

## BUG-0035 `[fixed]`

**Found:** 2026-08-12 (walk, seed 90010001)
**Oracle:** walker-action-effect

The walker's chart SELECT and DELETE actions are silent no-ops, and so is the chart half of deepResetForWalk. All three pass a chart's identity as `charts[0].id`, but a ChartDefinition in the frontend store keys its identity as `chartId` (the BACKEND's ChartEntry is the one that calls it `id`). Every call therefore receives `undefined` and returns successfully having done nothing. MEASURED LIVE on a 75-action chart-weighted invariant walk: 46 chart actions ran, `get_charts` afterwards answered 9, and `__CALCULA_CHARTS__.getCurrentChartId()` was still null -- nothing had ever been selected and nothing had ever been deleted. This is the residual of BUG-0031, which fixed the CREATE half (backend-only -> store+backend) and left select/delete reading a key that does not exist.

**Repro:** E2E_MANUAL=1 INVARIANT_SEED=90010001 INVARIANT_CATEGORY_WEIGHTS="chart:10" npx playwright test --project=invariant --grep "random action sequence". The walk passes; then probe the app: `get_charts` returns 9 while `getCurrentChartId()` is null. Minimal form: run the catalog's chart.create then chart.delete against a live app and observe get_charts stays at 1.
**Triage:** test-bug (confidence 0.99) — One object type in this tree names its store id differently from its backend id (`chartId` vs `id`); slicers and sparkline groups both use `id`, so the harness's uniform `.id` read is correct everywhere except charts. Nothing asserted that a walker action had an effect, so the mismatch was invisible: the action ran, the trace recorded it, and the oracles compared an unchanged workbook to an unchanged workbook.
**Fix:** fixed — chart.select and chart.delete read `charts[0].chartId ?? charts[0].id` and THROW naming the available keys when neither exists, so a future rename fails loudly instead of no-opping; deepResetForWalk reads the same. The lasting fix is the new live guard spec `e2e/soak/walker-action-effects.spec.ts`: it drives the catalog's own ActionDef objects against a running app and asserts each one's observable effect -- create raises both the backend and the store count and yields a usable id, select raises the Chart Design contextual tab AND sets getCurrentChartId, deselect clears it, delete lowers both counts, deepResetForWalk really empties the store, and a generic net checks create-raises/delete-lowers for chart, table, slicer and sparkline. A unit test cannot see any of this: the defect is in the seam between the harness and a live extension, which is where all three instances of it (table.delete, BUG-0031, BUG-0035) have been.
  Files: app/e2e/walker/actionCatalog.ts, app/e2e/walker/reset.ts, app/e2e/soak/walker-action-effects.spec.ts

## BUG-0036 `[fixed]`

**Found:** 2026-08-12 (walk, seed 90010001)
**Oracle:** contextual-ribbon-tabs

Contextual tab "Chart Design" is visible but at least one chart must exist. Found: slicers=0, charts=0, tables=1, pivots=0, timelines=0, sparklines=0. ROOT CAUSE: there were TWO chart-delete recipes and they had drifted. The UI path (Delete key / context menu) deselected the chart and cleared its render cache with removeChartFromCache; the component-store registry's deleteChart -- the route used by object scripts, the MCP tools and the broker's api.deleteChart -- did NEITHER. Deleting the selected chart from a script therefore left the contextual Chart Design tab on screen over a workbook with zero charts, with getCurrentChartId() still returning the dead id, so every Chart Design command was aimed at a chart that no longer existed. Two further drifts rode along: no CHART_SELECTION_CHANGED emission (FormulaBar/NameBox kept the deleted chart's name), and invalidateChartCache (which only bumps a version counter) standing in for removeChartFromCache (which drops the OffscreenCanvas, the parsed data cache, the point selection and the widget values) -- so a script-deleted chart leaked all four for the life of the session under an id nothing could reach again.

**Repro:** `tests/regression/repros/BUG-0036.trace.json` (3 actions)
**Triage:** product-bug (confidence 0.97) — A copied recipe, the failure mode the Seam Rule already names: `performChartDelete` was a local closure inside activate() and the component-store registry re-implemented it from memory. The copies could not be compared because neither was reachable from the other. Excel parity is unambiguous -- deleting a chart clears its selection and the contextual tab goes with it -- and Microsoft's own contextual-tab behaviour is that the tab exists only while its object is selected.
**Fix:** fixed — ONE chart delete. `performChartDelete(chartId): boolean` is hoisted to module scope in Charts/index.ts and is now the only implementation: it refuses an unknown id, deselects AND announces the selection change when the deleted chart is the selected one, deletes from the store, removes (not merely invalidates) the render cache, re-syncs the grid regions, and emits CHART_DELETED + GRID_REFRESH. The component-store registry method delegates to it (so api.deleteChart / MCP / object scripts get the whole recipe), the Delete key and context-menu handlers delegate to it, and the E2E bridge `__CALCULA_CHARTS__.deleteChart` now hands out the PRODUCT'S delete instead of the raw chartStore primitive -- the same correction `chart.create` needed in BUG-0031. `chartStore.deleteChart` is left as what it always was, an internal store+backend persistence primitive with no external callers. The deselect is CONDITIONAL, which the old UI copy was not: deleting chart A while chart B is selected must leave B selected, as Excel does, and the context-menu route could already pass a non-selected id.
  Files: app/extensions/Charts/index.ts, app/e2e/soak/walker-action-effects.spec.ts

## BUG-0037 `[fixed]`

**Found:** 2026-08-12 (walk, seed 1786456498740)
**Oracle:** ui-not-blocked

`ribbon.switch-tab` opens the Customize Home Tab modal by accident, and the walk then runs to completion with the UI unreachable and reports PASS. The action matched its target with `page.locator("button").filter({ hasText: name }).first()` -- a SUBSTRING match resolved by DOM order. Clicking the "View" tab opens the View MENU, whose "Customize Home Tab..." item is a <button> containing the word "Home" and sits at DOM index 22, ten positions before the real ribbon Home tab at 32. So the action's "return to Home" click opened a modal: position fixed, z-index 1050, covering 100% of the viewport, with NO role="dialog" (so the long-captured `visibleDialogCount` stayed 0). From that step on every UI action in the walk clicked into the modal's backdrop; each burned the 30s `actionTimeout` and was TOLERATED by the runner. MEASURED on seed 1786456498740: two such throws, 60 seconds of a 75-action walk spent clicking nothing, reported as a pass. Before `actionTimeout` was set at all this same state hung forever -- it is the unexplained twelve-minute stall at `[step 47/75] ribbon.switch-tab` recorded in playwright.config.ts, now diagnosed.

**Repro:** E2E_MANUAL=1 INVARIANT_SEED=1786456498740 npx playwright test --project=invariant --grep "random action sequence". Pre-fix: passes, with two `[action failed] ribbon.switch-tab: locator.click: Timeout 30000ms exceeded` at steps 41 and 42. With the `ui-not-blocked` invariant in place but the old locator restored, the same seed FAILS at step 40 naming the modal. Direct proof of the locator confusion: open the View menu, then enumerate buttons whose text contains "Home" -- "Customize Home Tab..." is at DOM index 22, the ribbon tab at 32.
**Triage:** test-bug (confidence 0.99) — Two independent gaps met. (1) A substring `.first()` locator can click a completely different control than the one named. (2) The walker had no notion of 'the UI is unreachable', so a poisoned state was indistinguishable from a healthy one: an action that cannot click is tolerated, and the only signal -- `visibleDialogCount` -- was captured after every action and read by nothing, and would not have fired anyway because the modal carries no ARIA role.
**Fix:** fixed — Three changes, and only the first is the bug. (1) `ribbon.switch-tab` presses Escape (closing whatever a previous action left open) and targets `getByRole("button", { name, exact: true })`, which cannot match "Customize Home Tab..." while looking for "Home". (2) NEW INVARIANT `ui-not-blocked`: `captureVisualState` hit-tests the centre of a real ribbon tab button with `document.elementFromPoint` and records `visual.ribbonBlockedBy` when the hit is neither the button nor inside the tab strip -- a question immune to both styled-components hashes and missing ARIA roles. A walk that continues with a covered ribbon now FAILS naming the covering element instead of quietly proving nothing. (3) The walker now takes an overlay census the moment an action throws (`ActionTiming.overlays`: every position:fixed/absolute element covering >=40% of the viewport, with class, z-index, pointer-events and text). That census is what identified this bug in a single run, after the register had carried the undiagnosed hang for a pass.
  Files: app/e2e/walker/actionCatalog.ts, app/e2e/walker/walkRunner.ts, app/e2e/invariants/stateSnapshot.ts, app/e2e/oracles/cheapInvariants.ts, app/e2e/__tests__/cheapInvariants.test.ts

## BUG-0038 `[fixed]`

**Found:** 2026-08-12 (walk, seed 90040001)
**Oracle:** selection-in-bounds

Malformed selection (endRow < startRow): {"startRow":14,"startCol":1,"endRow":3,"endCol":6}. ROOT CAUSE, measured on the running app: a ZERO-DURATION click leaves the grid permanently dragging. `handleGlobalMouseUp` in useMouseSelection is always attached but reads isDragging/isSelectionDragging/... from the CLOSURE of the render it was attached in. A mousedown starts a drag with setIsDragging(true); when the matching mouseup is dispatched before React commits that state, the still-attached handler sees every flag false and ends nothing. The flag then stays true forever, and the conditional `mousemove` listener -- which IS attached on the next render -- extends the selection on every subsequent pointer MOVE with no button held. Live matrix: hold 0ms -> the selection follows the pointer forever; hold 5ms/20ms/150ms -> correct. A physical mouse never produces a 0ms click, which is why no human reported it; `element.click()` always does, so every synthetic click -- macro replay, object scripts, MCP-driven interaction, OS-synthesized touch/pen taps -- puts the grid into an unending drag. It reached the surface as an INVERTED range because the walk clicked a cell and then a chart: the chart overlay consumes the second mousedown, so nothing re-anchored the phantom drag and the malformed selection survived to be observed. Anything iterating startRow..endRow over it silently operates on nothing.

**Repro:** `tests/regression/repros/BUG-0038.trace.json` (3 actions)
**Triage:** product-bug (confidence 0.95) — A React closure-vs-event-timing race in Core interaction code. The teardown handler is always mounted but only ever sees the state of the render that mounted it, so an event that arrives inside the same commit window is dropped. Widened by the header paths being `await`ed, which can move setIsDragging(true) a whole task past the mousedown.
**Fix:** fixed — LATCH THE MISSED EVENT rather than duplicate the teardown. `handleGlobalMouseUp` gains a final `else` branch: when no drag is committed it records `pendingMouseUpRef` instead of returning silently. The same effect, which re-runs whenever the drag flags change, replays `handleGlobalMouseUp` the moment a drag becomes visible while the latch is set -- so the ONE teardown recipe runs, one render later, with a consistent closure. A native window `mousedown` listener (capture phase) clears the latch so a stale one can never end a later gesture. THE POSITION OF THAT CLEAR WAS MEASURED, NOT GUESSED: the first attempt cleared it at the top of `handleMouseDown` and the fix did not work, because instrumentation showed the React handler running AFTER the window mouseup ('up' then 'down' in the log) -- it is async and delegated. A plain window listener runs during mousedown dispatch, which always completes first.
  Files: app/src/core/hooks/useMouseSelection/useMouseSelection.ts, app/e2e/soak/synthetic-click-drag.spec.ts

## BUG-0039 `[fixed]`

**Found:** 2026-08-12 (walk, seed 20260810)
**Oracle:** native-dialog-blocking

A NATIVE dialog hangs the walker silently and indefinitely. MEASURED: a soak walk stopped printing at `[shrink] replay 22` and sat there. Fifteen minutes later the app was still answering `page.evaluate`, a screenshot showed an ordinary spreadsheet, and `list-app-windows.ps1` reported TWELVE visible `#32770` windows stacked on it -- `TEXT:Failed to rename sheet: Sheet index 2 out of range`. The walk burned its whole 30-minute spec timeout without printing a line. TWO independent causes met. (1) The shrinker recorded `sheet.rename {tabIndex: 2}` against a three-sheet workbook, then dropped the `sheet.add` that made the third sheet and replayed the rename against two; `ActionDef.precondition` received only the snapshot, never the recorded params, so it could ask 'are there two sheets?' but never 'does sheet 2 exist?' -- and the catalog's own header had been promising that preconditions are re-checked on replay. (2) Nothing in the harness could see or answer a native dialog: it is a separate Win32 window, absent from page screenshots, with no DOM, and invisible to the new `ui-not-blocked` invariant's elementFromPoint hit test. `deepResetForWalk` issues a long chain of invokes inside ONE `page.evaluate` with no timeout, so a blocked IPC channel there is an unbounded await.

**Repro:** Add a sheet, then dispatch `sheet:requestRename` with an index one past the end, repeatedly. Each raises an alertAsync message box that nothing answers. Then call deepResetForWalk: before the fix it never returns. e2e/soak/native-dialog-hang.spec.ts rebuilds the pile (12 alerts) and asserts the reset clears it.
**Triage:** test-bug (confidence 0.95) — The harness had exactly one blind spot left that a timeout could not cover, and a shrink step that could manufacture invalid input walked straight into it. Playwright's actionTimeout bounds locator actions, not `page.evaluate`, so the one place the walker awaits an unbounded chain of invokes -- the reset -- was the one place with no bound.
**Fix:** fixed — Both causes, and a bound for what neither covers. (1) `ActionDef.precondition` now takes the recorded `params` as an optional second argument, supplied ONLY on the replay path (the generator has none yet, so both sheet actions stay generatable); `sheet.rename` and `sheet.switch` refuse a tabIndex that no longer addresses a sheet, and `createTraceSource` hands the params over -- without that wiring the preconditions are correct and the walk still hangs. (2) NEW `e2e/helpers/nativeDialogs.ts` wraps `answer-native-dialog.ps1` for the Node side: `readNativeDialogText()` and `sweepNativeDialogs()`, which dismisses the whole STACK (they pile one per replay) and returns each message, because the text is the only evidence of what raised it. `deepResetForWalk` sweeps before anything else and prints what it cleared -- a reset that has to clear dialogs is itself a finding -- and is now bounded (90s) with one retry, then throws a named error instead of hanging. The walk runner additionally bounds `captureSnapshot` (45s, its first invoke after every action) and, on timeout, sweeps, names the dialogs and fails with `native-dialog-blocking`. NOT CLAIMED: that a single alert blocks IPC. Measured on the live app, one open alert left `get_charts` answering normally; the pile-up is what stopped `new_file`. The sweep keeps the pile from forming; the timeouts are the backstop.
  Files: app/e2e/helpers/nativeDialogs.ts, app/e2e/walker/reset.ts, app/e2e/walker/walkRunner.ts, app/e2e/walker/actionCatalog.ts, app/e2e/walker/sources.ts, app/e2e/soak/native-dialog-hang.spec.ts, app/e2e/__tests__/walkerReplayPreconditions.test.ts

## BUG-0040 `[fixed]`

**Found:** 2026-08-13 (walk, seed 20260810)
**Oracle:** save-reload-round-trip

Removing a table's AutoFilter does not survive save/reload: the LOAD path invents the filter again. Digest diff, two entries: `tables.<id>.autoFilterId: "<absent>" -> "<new id>"` and `autoFilters.0: "<absent>" -> {enabled:true, startRow:0, startCol:30, endRow:2, endCol:32, ...}` -- exactly the walker's table range AE1:AG3. MECHANISM, read off the source: `create_table` creates an AutoFilter when `show_filter_button` is set (tables.rs ~line 865), `remove_auto_filter` removes it, and `auto_filter_id` is derived state that is never persisted. On load, `persistence.rs` finds no `autofilters.json` for the sheet and executes a block commented "Seed a filter from the lowest filter-button table when the workbook has none saved (pre-existing behavior, kept)" -- manufacturing an AutoFilter from the lowest filter-button table and then relinking ownership to it. So a filter the user deliberately removed comes back on reopen, and the reopened document now differs from the one that was saved.

**Repro:** `tests/regression/repros/BUG-0040.trace.json` (2 actions)
**Triage:** product-bug (confidence 0.9) — A legacy compensation in the load path outliving its reason, in the same family as the suppressions §5 and §10a are about. The seed exists so that a workbook with filter-button tables and no saved filters still shows filter buttons; since `create_table` now establishes the filter itself, the only workbooks that reach the seed are ones whose filter was REMOVED on purpose.
**Fix:** fixed — ROOT CAUSE, and it is not the load-path seed the entry pointed at. Calcula kept TWO records of one thing: `Table.style_options.show_filter_button` (persisted, on the table) and the entry in `auto_filters` (persisted separately in autofilters.json). `create_table` wrote both; `remove_auto_filter` cleared only the second. `Table.auto_filter_id` is derived state that is never saved, so the load path reconstructs it -- and finding no saved filter for a sheet whose table STILL advertised buttons, it seeded one from that table. The removal was undone by the reopen. EXCEL PARITY SETTLES THE DESIGN: Excel has a single state, `ListObject.ShowAutoFilter`. Data > Filter (Ctrl+Shift+L) on a table and Table Design > Filter Button are the same switch, and turning it off persists. There is no Excel state where a table shows filter dropdowns but has no filter. So `remove_auto_filter_inner` now clears `show_filter_button` on the table that OWNED the removed filter (matched by exact id, so bystander tables on the same sheet keep their own buttons). THE SEED STAYS, AND THAT IS THE QUESTION THE ENTRY SAID THE FIX COULD NOT ANSWER BY ITSELF -- so it was traced rather than guessed. `load_xlsx` (core/persistence/src/xlsx_reader.rs) hard-codes `user_files: HashMap::new()`, so an imported workbook NEVER carries an autofilters.json; the `show_filter_button` in the table metadata is the only record of its filters that exists. Deleting the seed would have silently stripped the filter buttons from every imported workbook. With the flag now cleared on removal, the seed only fires for a table that still asks for buttons -- exactly its job. UNDO HAD TO MOVE WITH IT, or the fix would have traded one bug for another: ownership is RE-DERIVED from `show_filter_button` by `relink_autofilter_owner`, so an undo restoring the filter alone would restore an ORPHAN -- a sheet filter no table claims, on a table showing no buttons to clear it with. `AutoFilterObjSnapshot` grew a `filter_buttons: Vec<(EntityId, bool)>`; the restore arm applies it and records the CURRENT values into the inverse so redo is exact; `record_autofilter_undo_with_buttons` is the one recorder that populates it, and the other nine autofilter recorders pass an empty vec (a criteria change moves no buttons).
  Files: app/src-tauri/src/autofilter.rs, app/src-tauri/src/undo_commands.rs, app/src-tauri/src/autofilter_table_button_tests.rs, app/e2e/journeys/orphaned-sheet-state.spec.ts

## BUG-0041 `[fixed]`

**Found:** 2026-08-13 (walk, seed 90050003)
**Oracle:** save-reload-round-trip

A sparkline group created on a sheet that is then DELETED survives in the backend under the dead sheet's index, and reappears on ANOTHER SHEET after save/reload. Digest diff: `sparklines.1` present before save and absent after; `sparklines.0[1]` absent before and present after -- the same group, re-keyed from the deleted sheet 1 onto sheet 0, where it becomes that sheet's second group. A hyperlink goes the other way: `hyperlinks.1:16:0` present before, absent after. So the in-memory workbook holds per-sheet state for a sheet that no longer exists, and the loader resolves the out-of-range index by landing it on sheet 0. This is data appearing on the WRONG SHEET after a reopen.

**Repro:** `tests/regression/repros/BUG-0041.trace.json` (3 actions)
**Triage:** product-bug (confidence 0.85) — NOT the obvious one, and that is why it is filed rather than patched. `cascade_sheet_removed` (object_deps.rs ~1594) DOES walk `state.sparklines` and drops every entry whose sheet index remaps to None, and `object_deps.rs`'s own census carries the rule `sparkline.sheetIndex -> cascade_sheet_removed`. The census is satisfied and the entry survived anyway, so the leak is elsewhere: the likely candidates are the FRONTEND sparkline store re-persisting after the cascade ran (the walker creates through `__CALCULA_SPARKLINES__`, which write-throughs to the backend), or the walker's `sheet.delete` UI path completing after the cascade. Hyperlinks are a separate question again: they are cell-keyed with the sheet in the key, like comments, and comments ARE remapped in `remap_sheet_keyed_stores` while hyperlinks and notes are not listed there at all.
**Fix:** fixed — The entry named three candidate mechanisms and refused to guess. It is the FIRST -- the frontend write-through -- and the damage is done by two SILENT FALLBACKS in the serialiser that nobody had connected to it. MECHANISM, end to end. (1) `cascade_sheet_removed` correctly drops the sparkline entry whose sheet index no longer resolves -- this half was never broken, which is exactly why the object-deps census was satisfied while the bug was live. (2) The Sparklines extension saves on every SHEET_CHANGED (`saveNow()` in extensions/Sparklines/index.ts), and SHEET_CHANGED also fires for a sheet COLLECTION change; the grid-state snapshot still holds the PRE-change active index, so the save arrives naming the DELETED sheet and re-inserts what step 1 removed. (3) On save, `sheet_index_to_id` could not resolve that index and MINTED A FRESH RANDOM SheetId -- turning a detectable inconsistency into an undetectable one. (4) On load, `sheet_id_to_index` could not find that id and answered `0`. The orphan landed on the first sheet. FIXED AT ALL THREE LEVELS. (a) `save_sparklines_impl` refuses an entry naming a sheet index that does not exist, reading the live `sheet_names` length -- the backend is the authority on which sheets exist and no longer takes the caller's word for it. The refusal is silent and CLEAN: the write is spurious, so it must not dirty the document. (b) `sheet_index_to_id` now returns `Option<SheetId>` and (c) `sheet_id_to_index` returns `Option<usize>`; all 29 call sites drop the orphan instead. STEPS 3 AND 4 WERE NEVER SPARKLINE-SPECIFIC and that is the real weight of this entry: every per-sheet store on the save/load path routes through that one pair -- tables, slicers, charts, conditional formats, data validation, comments, scenarios, outlines, named ranges and sheet PROTECTION. Any of them, orphaned by any means, was being relocated onto sheet 0 rather than dropped. Two call sites needed more than a mechanical change: a SHEET-SCOPED named range whose sheet is gone is dropped rather than flattened to workbook scope (flattening `Some(idx)` to `None` would silently promote a local name to global), and a pending-recalc marker naming a missing sheet is dropped rather than landing on sheet 0 and warning that a calculated sheet is stale. The frontend was deliberately NOT changed: its stale write is harmless now that the authority refuses it, and for an ordinary sheet SWITCH the pre-change index it uses is the correct one.
  Files: app/src-tauri/src/sparkline_commands.rs, app/src-tauri/src/persistence.rs, app/src-tauri/src/orphaned_sheet_state_tests.rs, app/src-tauri/src/document_effect_objects_tests.rs, app/e2e/journeys/orphaned-sheet-state.spec.ts

## BUG-0042 `[fixed]`

**Found:** 2026-08-13 (e2e)
**Oracle:** functional-assertion

Clicking a cell that carries a NOTE does not open the note editor, and the note's red indicator triangle DISAPPEARS at the same moment. `vba-idioms-wave4.spec.ts:735` step 'clicking the annotated cell opens the note editor with the text' fails on `locator('textarea:visible').first()` — element(s) not found, 10s. MEASURED, and this is the part that makes it a product bug rather than a flaky test: the two screenshots the spec itself writes show the triangle PRESENT on AA61 before the click (wave4-note-triangle.png) and ABSENT after it (test-failed-1.png), with AA61 selected and no overlay. NO DATA IS LOST. A direct probe (add_note at row 60/col 26, grid:refresh, click AA61) reports 0 visible textareas while `get_note` STILL returns the note, byte-identical, after the click. So the note is intact in the backend and unreachable from the UI: clicking is the product's documented inspection gesture for a note (the spec's own comment records that the hover preview in hoverHandler.ts is unwired dead code — initHoverHandler has no caller), so there is currently NO way to read a note's text by pointing at it. [CORRECTED 2026-08-13, see triage: the claim that the triangle DISAPPEARS is WRONG. Re-measured two ways (a live probe sampling the corner before and after the click, and a pixel decode of both screenshots) the indicator is present and IDENTICAL in both frames. The editor half is real, but its cause is not the cache: the editor opens and is closed ~35ms later by the same click's selection change. The claim that the hover preview is unwired is also stale — initHoverHandler is called from Review/index.ts.]

**Repro:** FOUR BACKEND CALLS AND ONE CLICK, on a cold app, no macro and no editor involved: (1) new_file; (2) add_note {params:{row:60,col:26,authorName:'Probe',content:'PROBE-NOTE-TEXT',richContent:null,width:null,height:null}}; (3) dispatch window Event('grid:refresh') and wait ~1.2s — the triangle paints; (4) click cell AA61. Then: page.locator('textarea:visible').count() === 0, and get_note {row:60,col:26} still returns the note. Full-suite form: E2E_MANUAL=1 npx playwright test --project=functional --grep "setNote/getNote/listNotes". IT REPRODUCES COLD AND ALONE — which corrects the previous pass's triage (§10 recorded it as passing on a cold app and therefore order/residue dependent). It was re-run twice on a freshly launched app with nothing else in the run and failed both times.
**Triage:** product-bug (confidence 1) — MEASURED, and it CORRECTS the hypothesis this entry was filed with. It is NOT a synchronous read of an asynchronously refilled cache, and the triangle does NOT disappear. Both halves were measured before anything was changed. (1) THE TRIANGLE: an instrumented probe sampled the indicator corner before and after the click on the live app and got the SAME red fraction, 0.1428 both times; independently, the spec's own two screenshots were decoded pixel by pixel and both contain the same 66-pixel triangle in the same 11x11 device-pixel box, with the cell plainly tinted (i.e. selected) in the frame taken AFTER a successful click. The over-selection anchor works. (2) THE EDITOR: the same probe recorded document.querySelectorAll('textarea').length on every animation frame for 5s after the click. The editor DOES open -- 1 textarea at t=32ms and t=50ms -- and is GONE from t=67ms. So the cache HIT (that is why the editor opened and why the triangle painted); something closed the editor ~35ms later. That something is Review/handlers/selectionHandler.ts: handleAnnotationClick deliberately returns false so the cell still selects (Excel parity), the same click therefore moves the selection onto the annotated cell, Layout.tsx reports it, and the handler hid 'note-editor' whenever the active cell differed from previousCell -- which on this gesture is the cell the user was on BEFORE the click. The rule 'close when the selection moves' closed the editor the click had just opened. The comments sidebar's click-a-note-to-open-it was broken by the same rule from the other direction: it navigates first and shows the overlay second.
**Fix:** fixed — THE BUG: the annotation editors now close per ANCHOR, not per change. handleSelectionChange reads each editor overlay's own row/col out of OverlayExtensions.getVisibleOverlays() and hides it only when the active cell is a different cell -- order-independent, so it does not matter whether the selection lands before or after the overlay is shown. Proved on the live app with the same probe that measured the defect: the textarea now stays up for the full 5s, and the triangle's red fraction is unchanged at 0.1428 before and after. THE CLASS the brief asked to enumerate: five extension caches are read synchronously while refilled asynchronously (Review annotations, DataValidation rules, Hyperlinks indicators, ErrorChecking indicators, Grouping outline). NONE of them clears before refilling -- the filed shape does not exist in the family. Three real things did: (a) invalidateAnnotationCache() was the only function that emptied a live cache with nothing scheduled to refill it, and had NO production caller -- deleted, and a source-census guard in annotationStore.deep.test.ts (self-tested) now keeps resetAnnotationStore the only clearer; (b) invalidate() was a NO-OP for Review/DataValidation/Hyperlinks because all three dropped the stillCurrent predicate createCoalescedRefresh hands them, so Review's documented sheet-switch abandonment abandoned nothing -- all three now consult it; (c) Hyperlinks wiped both caches on any read error, turning one dropped IPC call into 'this cell has no hyperlink' -- it now keeps the previous answer. THREE MORE DEFECTS in the same indicator path, each found by reading and then measured: (1) the frozen/split pane painter (drawCellTextZone) ran NO over-selection decorations at all and skipped every empty cell before the under-selection pass, so freezing the top row made every note/error/bookmark indicator on the sheet vanish (measured 0 indicator pixels) -- it now runs both anchors and returns its deferred list, each entry carrying the pane clip it must be replayed under; (2) the main painter's empty-cell skip was gated on hasCellDecorations(), which answers for the UNDER-selection registry only, so a note triangle on an existing-but-empty unstyled cell painted only while some unrelated extension had an under-selection decoration registered -- the gate now asks both anchors; (3) the cell-click-interceptor dispatch and the grid context menu called getCellFromPixel WITHOUT the pane options every other caller passes, so with a frozen header row an interceptor was handed a different cell from the one the click selected (measured: row 0 with the options, row 20 without) -- both now pass them, with a self-tested source census.
  Files: app/extensions/Review/handlers/selectionHandler.ts, app/extensions/Review/lib/annotationStore.ts, app/extensions/DataValidation/lib/validationStore.ts, app/extensions/Hyperlinks/index.ts, app/src/core/lib/gridRenderer/rendering/cells.ts, app/src/core/lib/gridRenderer/core.ts, app/src/core/components/Spreadsheet/useSpreadsheetSelection.ts, app/src/core/components/Spreadsheet/Spreadsheet.tsx, app/extensions/Review/handlers/__tests__/selectionHandler.test.ts, app/src/core/lib/gridRenderer/interaction/paneAwareHitTesting.test.ts, app/src/core/lib/gridRenderer/cellDecorationZOrder.test.ts, app/extensions/Review/lib/__tests__/annotationStore.test.ts, app/extensions/Review/lib/__tests__/annotationStore.deep.test.ts

## BUG-0043 `[fixed]`

**Found:** 2026-08-13 (review)
**Oracle:** excel-parity

UNDO OF AN OFF-SHEET EDIT IS INVISIBLE. Excel keeps ONE undo history and switches to the sheet the undone action happened on, so the user sees what changed. Calcula restored the right cells on the right sheet (BUG-0034 fixed that) and then said nothing at all: the tab strip, the grid, the formula bar and every piece of per-sheet chrome stayed on the sheet the user happened to be looking at. An undo of an edit made on another sheet was therefore indistinguishable from an undo that did nothing -- the value moved, correctly, on a sheet nobody could see. Named as still-open in the register at 9f and deferred there because a backend-only switch would show Sheet1's tab over Sheet2's data, which is worse than the silence.

**Repro:** THREE ORDINARY ACTIONS. (1) Click the Sheet2 tab and type a value into Sheet2!A1. (2) Click the Sheet1 tab. (3) Press Ctrl+Z. Before: nothing on screen changes -- the value on Sheet2 is correctly restored, but the app stays on Sheet1 and reports no switch, so there is no way to tell the undo happened. After: the app switches to Sheet2, selects A1, and the tab strip, grid, formula bar, column widths, row heights, hidden rows, merges, zoom, split and freeze panes all land on Sheet2 together. Redo behaves the same way. Unit-level reproduction: undo_commands::undo_sheet_activation_tests::an_undo_of_an_off_sheet_edit_activates_that_sheet.
**Triage:** product-bug (confidence 1) — `UndoResult` had no sheet dimension at all, so the frontend could not have followed a switch even if the backend had made one, and `sheets::set_active_sheet` was a Tauri command taking `State<AppState>` -- unreachable from `apply_changes`, which holds an `&AppState`. Both halves were needed: the backend performing a REAL activation through the one implementation that swaps every per-sheet store, and the frontend following it through the same channel a tab click uses.
**Fix:** fixed — `Transaction::target_sheet()` answers WHICH sheet a transaction belongs to (first recorded change that carries one; None for a CustomRestore-only transaction, whose sheet is inside opaque bytes). `sheets::set_active_sheet` is now a one-line delegation to `sheets::activate_sheet(&AppState, index)`, the single implementation of the swap -- grid mirror, column widths, row heights, merged regions, user-hidden rows/cols, then the dependency-map rebuild. `apply_changes` calls it AFTER the restore and with no lock held, so the whole off-sheet path (deferred geometry, grids[sheet] writes, recalc_after_off_sheet_write) still runs and is still exercised by the BUG-0034 guards; the mirror swap then picks up exactly the stores those arms wrote. A HIDDEN target is restored but NOT activated -- Excel cannot make a hidden sheet active, and following it would put the grid on a sheet with no tab. `UndoResult` gains activeSheetIndex / activeSheetName / restoredAnchor, reported unconditionally so the frontend compares against its own state. The frontend follows through the tab click's OWN channel (sheet:beforeSwitch, the grid sheet context, sheet:normalSwitch, SHEET_CHANGED) with no await between the dispatches, and aims the selection at the restored anchor. handleUndo and handleRedo -- previously byte-identical copies -- are now one `applyRestoreToTheView`.
  Files: core/engine/src/undo.rs, app/src-tauri/src/sheets.rs, app/src-tauri/src/undo_commands.rs, app/src-tauri/src/undo_sheet_domain_tests.rs, app/src-tauri/src/undo_sheet_activation_tests.rs, app/src/core/lib/tauri-api.ts, app/src/core/components/Spreadsheet/useSpreadsheetSelection.ts, app/src/core/components/Spreadsheet/__tests__/undoSheetActivation.test.tsx

## BUG-0044 `[fixed]`

**Found:** 2026-08-13 (review)
**Oracle:** functional-assertion

THE FORMULA BAR SHOWS THE PREVIOUS SHEET'S CELL AFTER A SHEET SWITCH. Found while making undo's sheet switch atomic, but it is NOT caused by it -- it fires on an ordinary tab click and predates this pass. The effect that fills the formula bar in useSpreadsheetSelection depended on [selection.endRow, selection.endCol, isEditing] only. `getCell` reads the ACTIVE sheet, so a switch that lands on the SAME coordinates re-ran nothing and the bar kept the old sheet's content. That is the COMMON case, not an edge: a sheet with no saved selection is given A1, and A1 is usually where you already were. The grid painted the new sheet while the formula bar described the old one, and the only clue was that the value was right for a sheet you were no longer on.

**Repro:** TWO CLICKS. (1) On Sheet1, select A1 and give it a value (say 100). (2) Click the Sheet2 tab, where A1 is empty and no selection has been saved, so the switch selects A1. The grid correctly shows Sheet2, and the formula bar still reads 100. Unit-level reproduction: 're-reads the formula bar when the sheet moves under an unchanged selection' in app/src/core/components/Spreadsheet/__tests__/undoSheetActivation.test.tsx.
**Triage:** product-bug (confidence 1) — A missing dependency on a React effect: the fetch reads backend state keyed by the ACTIVE SHEET but listed only the selection coordinates as its inputs, so the one input that actually changed was invisible to it.
**Fix:** fixed — `sheetContext.activeSheetIndex` added to the effect's dependency list, so the active cell is re-read whenever the sheet moves under an unchanged selection. Fixes the ordinary tab click as well as undo's switch.
  Files: app/src/core/components/Spreadsheet/useSpreadsheetSelection.ts, app/src/core/components/Spreadsheet/__tests__/undoSheetActivation.test.tsx

## BUG-0045 `[fixed]`

**Found:** 2026-08-13 (walk, seed 1786446166374)
**Oracle:** app-hang

THE APP DEADLOCKS WHEN A FILE IS OPENED WHILE THE BACKGROUND GATHER-REFRESH RECALCULATION IS RUNNING. Measured: the walk stopped printing at `[oracle checkpoint] after step 200`; the PAGE stayed alive (DOM answered, window title read `oracle-save-13232-1.cala`, no native dialogs open) but every Tauri invoke timed out. Two out-of-process stack dumps a minute apart were byte-identical. Main thread: `open_file` -> `restamp_workbook_name_casing` HOLDING `named_ranges` + `tables` + `table_names`, WAITING for `sheet_names`. Gather worker (`calp_commands::queue_gather_refresh` -> `calculation::recalculate_sheet_values`): HOLDING `grid` + `grids` + `sheet_names` + `style_registry`, WAITING for `tables`. Each holds what the other waits for. No panic, no crash, nothing in the app log after the checkpoint's own DIGEST line -- the message pump stops with the main thread and the window goes Not Responding. Third instance of this class in the programme and the SECOND on this seed. THE GUARD THAT EXISTS FOR THIS SHAPE WAS GREEN: `no_lock_is_held_while_a_grid_lock_is_acquired` treated `.ok()` as consuming the guard, but `LockResult::ok()` MOVES the guard into an `Option` -- so all three of restamp's held guards read as temporaries that had already died.

**Repro:** `tests/regression/repros/BUG-0045.trace.json` (200 actions)
**Triage:** product-bug (confidence 1) — A lock-order inversion between the load path and the background recalculation pass, on a PAIR no census asked about. The crate already had one canonical order (`grid` before `grids`, and no lock held while a grid lock is acquired) enforced crate-wide; nothing enforced the order of the four NAMING authorities relative to `sheet_names`, and the pass takes `sheet_names` FIRST while seven functions took one of `tables` / `table_names` / `named_ranges` / `style_registry` before it. `restamp_workbook_name_casing` was additionally inverted against the grid pair and escaped the existing census only because of the `.ok()` hole.
**Fix:** fixed — ONE ORDER, TAKEN FROM THE THREAD THAT CANNOT CHANGE IT. `recalculate_sheet_values` runs on a background thread and takes `grid`, `grids`, `sheet_names`, `style_registry`, ... `tables`, `table_names`, `named_ranges`; everything else now agrees with it. `restamp_workbook_name_casing` takes the two grid locks FIRST and then the four authorities in the pass's order (it used to take the authorities first and the grid locks last). Ten other functions that held one of the four and then asked for `sheet_names` hoist a clone of `sheet_names` above it: `tables::{delete_table, set_totals_row_function, toggle_totals_row, convert_to_range, set_calculated_column}`, `calp_commands::apply_override_value_to_grid`, `named_ranges::{get_named_range_for_selection, resolve_named_range_coords}`, `tracing::{trace_precedents, trace_dependents}`, `commands::print::get_print_data`, `commands::data::remove_duplicates`, and the four `commands::structure::{insert,delete}_{rows,columns}_impl`. `.ok()` is out of `CONSUMING` (it moves the guard into an Option, it does not release it) with a planted-body tooth that fails if it returns. New crate-wide census `no_function_holds_a_name_authority_while_acquiring_sheet_names` over all four pairs, its direction pinned to the pass by `the_recalculation_pass_still_takes_sheet_names_first`, plus a live deadlock probe and its non-vacuity partner.
  Files: app/src-tauri/src/persistence.rs, app/src-tauri/src/tables.rs, app/src-tauri/src/calp_commands.rs, app/src-tauri/src/named_ranges.rs, app/src-tauri/src/tracing.rs, app/src-tauri/src/commands/print.rs, app/src-tauri/src/commands/structure.rs, app/src-tauri/src/commands/data.rs, app/src-tauri/src/state_digest_lock_order_tests.rs

## BUG-0046 `[fixed]`

**Found:** 2026-08-13 (review)
**Oracle:** excel-parity

HIDING THE ACTIVE SHEET LEAVES THE BACKEND ON THE HIDDEN SHEET, SO EDITS GO TO THE WRONG SHEET. `hide_sheet` set the visibility flag and RETURNED a recommended new active index without touching `state.active_sheet` or any per-sheet mirror -- its own doc comment said "frontend should call set_active_sheet if it changed". None of the three callers did: `SheetTabs.handleHide` passes `backendHandledSwitch: true` ("Backend already swapped - just sync frontend state"), `ScriptNotebook/lib/deferredActionHost.setSheetVisibility` comments "Hiding the active sheet makes the backend switch", and the broker's `api.setSheetVisibility` goes through `announceSheetsChanged`, which dispatches `setActiveSheet` into the frontend store. All three therefore moved the FRONTEND onto the recommended sheet while the backend still had the hidden sheet active -- and every cell read and every cell write goes to the active sheet. The tab strip highlights Sheet1, the canvas paints Sheet2's data, and typing writes into the sheet the user just hid. Excel's rule settles it: a hidden sheet cannot be the active sheet (no tab to select; `Activate` raises in VBA).

**Repro:** TWO ACTIONS. (1) Add a sheet (which makes it active). (2) Right-click its tab and choose Hide. Before: the tab strip and the grid claim to be on Sheet1 while `get_active_sheet` still answers with the hidden sheet, so `get_cell_value`/`update_cell` operate on it -- typing appears to edit Sheet1 and edits the hidden sheet. After: the backend performs the switch through `activate_sheet`, so the index, the grid mirror, the column widths, the row heights, the merged regions and the user-hidden sets all move together. Unit-level: `sheets::hidden_active_sheet_tests::{hiding_the_active_sheet_activates_a_visible_one, the_grid_mirror_moves_with_it, the_sheet_it_lands_on_is_visible}`. NOTE: `TestRunner`'s "Hide and unhide a sheet" hides the sheet it has just added -- i.e. the ACTIVE sheet -- every time it runs, and asserted only `visibility == "hidden"`, so the defect sat underneath a passing test.
**Triage:** product-bug (confidence 1) — A command that returns a RECOMMENDATION into a codebase where every caller reads it as a report of what happened. The four other sheet-structure commands (`add`, `delete`, `move`, `copy`) all write `active_sheet` and swap the mirrors themselves; `hide_sheet` was the only one that did not, and the asymmetry was invisible because the return type is identical.
**Fix:** fixed — `hide_sheet` splits into a `hide_sheet_inner(&AppState, &FileState, ..)` body (so the switch has a unit tier) that computes the target with every guard scoped to a block, releases them, and then calls `activate_sheet` -- the ONE implementation of the swap, the same one BUG-0043 extracted. A source census fails the build if it ever assigns `active_sheet` itself instead. SECOND DEFECT, found by the test fixture: `ensure_vec_len(&mut sheet_visibility, n)` pads with `String::default()`, i.e. the EMPTY STRING, while every reader compares against the literal "visible" -- `SheetTabs` renders `sheets.filter(s => s.visibility === "visible")`, `hide_sheet` counts the visible sheets that way, and `next_sheet`/`previous_sheet` skip anything else. A padded slot is therefore a sheet with no tab that the workbook believes is neither hidden nor visible, and `build_sheet_list`'s `.unwrap_or("visible")` cannot repair it because the slot is `Some("")` rather than `None`. All four padding sites go through a new `ensure_visibility_len`. INSTRUMENT: the walker snapshot now carries `backendActiveSheet` and `sheetVisibility`, and a new cheap invariant `active-sheet-agrees` fails the walk when the two halves of the app name different active sheets, or when the active sheet is not visible -- a class no digest oracle can see, because the digest is assembled entirely from AppState and a backend that is wrong in a self-consistent way passes all three. `sheet.hide` is now a walker action.
  Files: app/src-tauri/src/sheets.rs, app/src-tauri/src/hidden_active_sheet_tests.rs, app/e2e/invariants/stateSnapshot.ts, app/e2e/oracles/cheapInvariants.ts, app/e2e/__tests__/cheapInvariants.test.ts, app/e2e/walker/actionCatalog.ts

## BUG-0047 `[fixed]`

**Found:** 2026-08-13 (walk, seed 1786446166374)
**Oracle:** save-reload-round-trip

TABLES DO NOT FOLLOW THEIR SHEET THROUGH A MOVE OR A COPY. `state.tables` is keyed by sheet INDEX, and it is the one sheet-index-keyed store that is NOT in `remap_sheet_keyed_stores` -- `delete_sheet` re-keys it inline, because the same pass has to drop the deleted sheet's table NAMES out of `table_names` and a generic remap cannot do that half. `move_sheet` and `copy_sheet` therefore re-keyed every OTHER per-sheet store (comments, scenarios, outlines, conditional formats, data validations, cell types, controls, protection, AUTOFILTERS, spill tracking, reports, and the floating slicer/timeline/chart/sparkline stores) and left this one where it was. The measured symptom is the AutoFilter link, because `auto_filters` DID move: on reload `relink_autofilter_owner` looks for the owning table under the filter's sheet index, finds none, and the link is gone (`tables.<id>.autoFilterId: <id> -> <absent>`) -- or worse, finds a DIFFERENT sheet's table and hands it the filter (`<A> -> <B>`, seed 90070001; `<absent> -> <id>`, seed 90070002). The lost link is only the visible half. The table itself is now registered against another sheet, and that registry is what every structured reference (`=SUM(Table1[Amount])`) resolves through, what the save path writes per sheet, and what the AutoFilter, totals-row and calculated-column commands look the table up in.

**Repro:** `tests/regression/repros/BUG-0047.trace.json` (9 actions)
**Triage:** product-bug (confidence 1) — One store outside the shared helper. §3bn's sweep asked 'is this store keyed by sheet index, and does the helper reach it' and `tables` answered the first question yes and the second no -- it has its own inline re-key in `delete_sheet`, so it looked handled. Nothing asked the two commands that do not delete anything.
**Fix:** fixed — New `remap_tables_store(state, effect, remap)` re-keys the store and re-stamps the `sheet_index` each `Table` carries -- both halves, because a key that moved without the field is the same divergence one layer down. `move_sheet` and `copy_sheet` call it with the very remap closures they already pass to `remap_sheet_keyed_stores`; `delete_sheet` keeps its inline pass, which also has to drop the deleted sheet's names out of `table_names`. A source census (`every_structural_command_re_keys_the_table_store`) checks all three by name, because 'it is a sheet-keyed store' is exactly the reasoning that missed it. Lock order: the call sites already hold `sheet_names`, and taking `tables` under it is the canonical direction the BUG-0045 census enforces.
  Files: app/src-tauri/src/sheets.rs

## BUG-0048 `[fixed]`

**Found:** 2026-08-13 (review)
**Oracle:** excel-parity

DELETING A SHEET CAN LEAVE A WORKBOOK WITH NO VISIBLE SHEET, AND CAN LEAVE THE ACTIVE SHEET ON A HIDDEN ONE. `hide_sheet` has always refused to hide the last VISIBLE sheet; `delete_sheet` only ever checked that at least one SHEET remained (`sheet_names.len() <= 1`). Hide Sheet1, delete Sheet2, and that guard is satisfied while the workbook is left with a single HIDDEN sheet: no tab to click, and an active index naming a sheet the user cannot look at. Excel's wording is exactly the distinction it was missing -- "a workbook must contain at least one visible worksheet". Separately, the index arithmetic that picks the post-delete active sheet is pure bookkeeping, so deleting the sheet beside a hidden one lands the user ON the hidden one even when other visible sheets exist.

**Repro:** TWO ACTIONS, from the sheet tab context menu. (1) Right-click Sheet1 > Hide. (2) Right-click Sheet2 > Delete. Before: the delete succeeds and the workbook has one sheet, hidden -- the tab strip is empty and the grid is showing a sheet with no tab. After: the delete is refused with "Cannot delete the last visible sheet". The second half: with three sheets, hide Sheet2, then delete Sheet3 -- before, the active index lands on the hidden Sheet2; after, it walks on to a visible one. Unit-level: `sheets::hidden_active_sheet_tests::{a_delete_that_would_leave_no_visible_sheet_is_counted_as_zero, the_landing_sheet_skips_a_hidden_preference, delete_sheet_asks_both_questions}`.
**Triage:** product-bug (confidence 1) — "At least one sheet" and "at least one VISIBLE sheet" are different tests, and only one of the two commands that can violate the rule was making the right one. The asymmetry survived because `delete_sheet` takes eight `State<T>` arguments and cannot be driven from a unit test, so the rule was never asked of it.
**Fix:** fixed — Two pure functions with their own unit tier -- `visible_sheets_after_removing` (the refusal) and `nearest_visible_sheet` (the landing) -- plus `sheet_is_visible`, which reads a SHORT `sheet_visibility` as visible exactly as `build_sheet_list` does. `delete_sheet` calls both, and a source census (`delete_sheet_asks_both_questions`) fails the build if either call goes away, because the command itself cannot be tested directly. Non-vacuity partners pin that the landing walk is a no-op when the preferred sheet is already visible, and that a short visibility vector does not make a workbook undeletable.
  Files: app/src-tauri/src/sheets.rs, app/src-tauri/src/hidden_active_sheet_tests.rs

## BUG-0049 `[fixed]`

**Found:** 2026-08-13 (walk, seed 1786446166374)
**Oracle:** undo-round-trip

THE UNDO ORACLE REPORTS A FALSE PRODUCT DEFECT WHEN A SHEET IS ADDED WHILE THE UNDO HISTORY IS EMPTY. Measured: "Undoing 22 steps did not restore the checkpoint state. 2 differences; first: sheetNames[2]: \"<absent>\" -> \"Sheet3\"" -- and both digest differences were that one EMPTY sheet. The walk had ADDED Sheet3 inside the checkpoint window; adding a sheet is not undoable (Excel parity, BUG-0005), so no number of undo steps returns to the checkpoint and the window is not decidable. The oracle decided it anyway. `stepsBackToBaseline` asks whether a wholesale clear DISCARDED anything (`clearedTotal`, a count of TRANSACTIONS), and a structure change made while the stack is already empty discards nothing -- so the counter does not move, the oracle sees no clear, winds the history back and reports the sheet the walk added as an undo defect. This is the exact false alarm BUG-0005's fix removed for the non-empty case, on the one case it left standing; it is a harness defect, and its cost is a triage cycle plus a lost walk each time it fires.

**Repro:** Any walk that (a) reaches a checkpoint with an empty or freshly-cleared undo stack, (b) pushes some ordinary transactions, and (c) adds/deletes/renames/moves/copies a sheet in the same window. Seed 1786446166374 at 200 actions does it at the checkpoint after step 125. Deterministic unit-level reproduction: `undoHistoryHorizon.test.ts::says so when the clear discarded NOTHING because the stack was empty` -- baseline {clearedTotal: 4, clearsTotal: 1}, now {clearedTotal: 4, clearsTotal: 2}: `clearedTotal` has not moved and the window must still be undecidable. Its partner covers the non-empty-baseline branch. Sabotage-checked: reverting the decision to `cleared > 0` fails exactly those two.
**Triage:** test-bug (confidence 1) — The counter answered a question next to the one that was being asked. `cleared_total` was introduced to tell "a structure change ended the history" from "the cap dropped it" and "the walk undid past it", and it does -- as long as the history had something in it. The fact the caller actually needs is whether a clear HAPPENED, which is not the same number.
**Fix:** fixed — `UndoStack` gains `clears_total`, incremented on EVERY `clear()` regardless of what it discarded, alongside the transaction-counting `cleared_total` -- kept separate so `no_phantom_clear_on_a_pristine_workbook` keeps meaning what it says. `get_undo_state` reports it as `clearsTotal`; `stepsBackToBaseline` decides on `clears > 0` and uses `cleared` only to say how much was lost ("1 clear(s), 0 transaction(s) discarded"). Two new cases in `undoHistoryHorizon.test.ts` cover both branches (empty and non-empty baseline) with `clearedTotal` deliberately unchanged, and they fail when the decision is reverted.
  Files: core/engine/src/undo.rs, app/src-tauri/src/undo_commands.rs, app/e2e/oracles/undoRoundTrip.ts, app/e2e/oracles/types.ts, app/e2e/__tests__/undoHistoryHorizon.test.ts

## BUG-0050 `[fixed]`

**Found:** 2026-08-13 (walk, seed 90070001)
**Oracle:** undo-round-trip

SHEET VISIBILITY (AND TAB COLOUR) ARE PERSISTED MUTATIONS WITH NO UNDO ENTRY AND NO HISTORY INVALIDATION. Measured: "Undoing 26 steps did not restore the checkpoint state. 1 differences; first: sheets[1].visibility: \"visible\" -> \"hidden\"" -- the sheet was VISIBLE at the checkpoint, the walk hid it inside the window, and undoing the whole window left it hidden. `hide_sheet`, `unhide_sheet` and `set_tab_color` all construct `DocumentEffect::mutates` (the state IS saved into the .cala), take no `undo_stack`, record nothing, and -- unlike the five structural commands -- do not call `invalidate_undo_history_for_sheet_structure` either. THE DEFECT IS REAL UNDER EITHER PARITY READING, and only the fix differs: if Excel undoes a sheet hide (see the evidence note below, which could not settle it from any source), the commands owe an undo entry that restores `sheet_visibility` / `tab_colors`; if it does not, they owe the history invalidation the other sheet commands make, so the oracle declines the window instead of deciding it. What is NOT defensible is the current third state, where the history stays intact across a change undo cannot reverse.

**Repro:** THREE ACTIONS. (1) Type a value into Sheet1!A1. (2) Right-click Sheet2's tab and choose Hide. (3) Press Ctrl+Z. The cell edit is undone and Sheet2 stays hidden, with no indication that anything was skipped -- and pressing Ctrl+Z again walks further back through a history that no longer describes the workbook. Same shape for Unhide and for a tab colour. Walk-level: E2E_MANUAL=1 SOAK_SEED=90070001 SOAK_ACTIONS=250 SOAK_CATEGORY_WEIGHTS="sheet:10" SOAK_ORACLE_EVERY=25 npx playwright test --project=soak --grep "random walk" -- fails at the checkpoint after step 125. NOT SUPPRESSED: no `KNOWN_ISSUES` entry was added, so sheet-weighted walks will keep reporting it until it is decided.
**Triage:** product-bug (confidence 1) — CONFIRMED, and the OWNER CALL is decided in favour of an undo ENTRY. The two candidates were 'record a CustomRestore' and 'end the history the way the five structural sheet commands do', and what settles it is that the argument FOR ending the history does not reach these three. Read `invalidate_undo_history_for_sheet_structure`'s own doc: the five end the history because an undo entry names its sheet by INDEX and those five RENUMBER every index (or, for a rename, rewrite every formula that spells the name), so a queued entry silently comes to describe a different sheet. Hiding a sheet renumbers nothing and rewrites nothing -- it assigns one element of a parallel Vec in place -- so every queued entry still describes exactly the sheet it was recorded on, and the hazard the invalidation exists to prevent is ABSENT. With the structural argument gone what remains is an asymmetry of cost: ending the history destroys every undo step the user had accumulated, recording an entry costs a few bytes. `hide_sheet_inner` already takes that same cheap direction one line over for the dirty flag ('a false positive, which is the cheap direction').
**Fix:** fixed — A new `"sheet_tab_state"` CustomRestore kind carries `{ visibility, tab_colors, active_sheet }` -- whole vectors (their LENGTH is state: `ensure_visibility_len` pads them lazily and `build_sheet_list` reads them by index, so a partial restore could leave the list disagreeing with itself), each field `Option` so 'this change did not touch it' is expressible, exactly like `UserHiddenSnapshot`. `hide_sheet_inner`, `unhide_sheet_inner` and `set_tab_color_inner` record it; the latter two are NEW `_inner` splits, added for the same reason `hide_sheet_inner` exists (a `State<T>` command has no unit tier). Every recording happens with NO other state lock held -- `record_custom_restore` takes `undo_stack`, which the crate's canonical order puts ahead of the grid locks -- and only when something actually changed, so a no-op hide/unhide/recolour and a REFUSED hide burn no undo step. Registry row: domain `Sheets` (the translator fans it out to "sheets:refresh" + SHEET_CHANGED, which is what makes the tab strip re-read), `defer: true` -- not a preference: undoing the hide of the ACTIVE sheet puts the user back through `sheets::activate_sheet`, which takes grid/grids/widths/heights/merges, all of which `apply_changes` still holds during the inline phase. `active_sheet` is recorded ONLY by hide (the only one that moves the user) and the restore refuses to follow it onto a sheet that is out of range or hidden, under the same rules as `activation_target`. 15 unit tests in `sheet_tab_state_undo_tests.rs` (undo/redo of all three, the standing-point follow and its non-active control, the hidden-target refusal, three no-op cases, a refused hide) plus a source census that every command recording persisted tab state names the kind, with a synthetic-source self-test proving the detector fires in both directions and that the brace matcher stops at the right function. `restore_registry_tests` (which fired on the new kind and had to be taught it) pins the domain and the defer flag. PROVED LIVE from a cold app through the REAL tab context menu and REAL Ctrl+Z (`sheet-tab-state-undo.spec.ts`): the hidden tab's absence is asserted FIRST so the probe is shown able to report the broken answer, then the tab returns, the backend and the rendered strip agree on the active index, and the formula bar reads a mark that only exists on the restored sheet. Paired control: undoing the hide of a NON-active sheet restores it and moves nobody.
  Files: app/src-tauri/src/sheets.rs, app/src-tauri/src/undo_commands.rs, app/src-tauri/src/sheet_tab_state_undo_tests.rs, app/e2e/journeys/sheet-tab-state-undo.spec.ts

## BUG-0051 `[fixed]`

**Found:** 2026-08-13 (walk, seed 90080002)
**Oracle:** contextual-ribbon-tabs

THE "TABLE DESIGN" CONTEXTUAL TAB SURVIVES THE DELETE OF THE LAST TABLE WHEN A SHEET HAS BEEN RENAMED IN BETWEEN. Measured: 'Contextual tab "Table Design" is visible but at least one table must exist. Found: slicers=0, charts=0, tables=0, pivots=0, timelines=0, sparklines=0'. The backend has NO tables and the ribbon still offers a tab whose every button addresses an object that no longer exists — the same end state BUG-0026 and the `syncDesignTabToTables` fix (soak seed 20260810) were about, reached through a door those fixes do not cover. The shrinker kept the rename: removing it makes the trace pass, so it is load-bearing and not incidental.

**Repro:** `tests/regression/repros/BUG-0051.trace.json` (4 actions)
**Triage:** product-bug (confidence 1) — ESTABLISHED, and it is NEITHER of the two filed candidates -- no probe was needed, the source says it. `deleteTableAsync` DOES succeed and DOES refresh its own cache; what it never does is announce the `objects` domain. The only thing that re-derives the contextual tab is `syncDesignTabToTables`, which runs on TABLE_DEFINITIONS_UPDATED, which is dispatched by the `objects` domain and by nothing else in the delete path. `announceObjectCascade` in the Table store named only ["slicer", "ribbonFilter"]; TWO CALLERS papered over it by emitting TABLE_DEFINITIONS_UPDATED BY HAND after awaiting the store (TableDesignTab's Delete and Convert-to-Range buttons), so the defect was invisible from the screen a human uses and reproducible from every other route -- a script, an MCP tool, the E2E walker. WHY THE CENSUS THAT EXISTS FOR THIS DID NOT CATCH IT: the two halves of `cascadeAnnouncementCensus.test.ts` contradicted each other. Its cascade half runs off `requiredDomainsFor`, which DELETES the owner's own domain from the required set on the reasoning that a route re-reading its own cache needs no announcement for itself -- and `objects` IS the Table's own domain. Its selection half, meanwhile, recorded in its own `why` that the tab 'is re-derived ... on TABLE_DEFINITIONS_UPDATED, which the `objects` domain dispatches'. One half excused exactly what the other half depended on. AND THE RENAME: the shrinker was right that removing `sheet.rename` made the trace pass, but it is not load-bearing to the DEFECT -- it was load-bearing to the SETUP. The walker's `table.create` used a raw `create_table` invoke, which leaves the frontend cache empty, so `table.select-into` could not register the tab at all; the rename fired SHEET_CHANGED, whose Table handler calls `refreshCache()`, and that -- nothing about renaming -- is what made the tab appear so it could then survive.
**Fix:** fixed — The STORE announces its own domain change: `announceObjectCascade` becomes `announceTableObjectChange` and names ["objects", "slicer", "ribbonFilter"], so the delete and the convert-to-range make the identical announcement an out-of-band MCP table delete already made. The two hand-written TABLE_DEFINITIONS_UPDATED emits in TableDesignTab are DELETED -- they were a second source of truth for 'the table list changed' that only the button path had. THE CENSUS IS REPAIRED SO THE CLASS CANNOT RECUR: `SelectionOwner` gains `reconcileDomain` (the refresh domain whose fan-out reaches the reconciliation) and a mandatory `reconcileDomainWhy` for the null case, and a new rule `unannouncedReconciliations` requires EVERY delete route for that kind to announce it -- own domain or not, which is precisely the check `auditRoutes` structurally cannot make. Non-vacuity is asserted (exactly one owner exercises the rule today) and a synthetic-source teeth case proves the detector fires, that the fixed shape is accepted, that `ownDomain` + a self-refresh does NOT excuse it, and that a null domain with no reason is reported. The new rule immediately found a SECOND, INDEPENDENT instance -- but it was a false report: `announceObjectCascade` in `scriptHost/host.ts` reaches "objects" through a SECOND hop (`announceObjectsChanged`) that the analyser's one-hop rule did not follow. `domainsAnnounced` now follows announcement helpers transitively, which keeps the closure at 'the announcement helpers this route reaches' rather than the whole module. HARNESS: `table.create` in the walker goes through `createTableAsync` and emits TABLE_CREATED, the way Insert > Table does. The raw invoke it replaces left the frontend not knowing a table existed, which made the walk blind to the whole contextual-tab surface unless an unrelated action happened to refresh the cache. PROVED LIVE from a cold app: the ribbon is asserted to be SHOWING 'Table Design' before the delete (so 'it is gone' cannot pass vacuously), the backend is asserted to hold zero tables after it, and the rendered ribbon no longer offers the tab. TEETH: with the fix sabotaged on the running build and the WebView reloaded, the same live test reports `["Home", "Page Layout", "Controls", "Table Design"]` with zero tables; the file was then restored byte-identically (sha256 8ac813074b0b42914c0044001e22801efa3fa1940d88d30402d40199c10dc2bf) and it passes again. The committed 4-action trace `repros/BUG-0051.trace.json` now replays clean.
  Files: app/extensions/Table/lib/tableStore.ts, app/extensions/Table/components/TableDesignTab.tsx, app/src/api/__tests__/cascadeAnnouncementCensus.test.ts, app/e2e/walker/actionCatalog.ts, app/e2e/journeys/sheet-tab-state-undo.spec.ts

## BUG-0052 `[fixed]`

**Found:** 2026-08-13 (manual)
**Oracle:** rendered-frame-consistency

A SHEET SWITCH IS NOT ATOMIC ON SCREEN: for a two-frame window the tab strip paints the NEW sheet as active while the canvas is still painting the OLD sheet's cells. Measured per animation frame, with the strip's own painted font-weight and a canvas `getImageData` probe of a cell that has ink on one sheet and not the other: undo-of-an-off-sheet-edit 21-71 ms, an ORDINARY TAB CLICK 60-67 ms, across three runs on the same build. It is therefore NOT undo's defect -- `applyRestoreToTheView` deliberately reuses the tab click's channel and measures no worse than it, usually better -- but a property of every sheet switch in the app. `followBackendSheetActivation` dispatches sheet:beforeSwitch -> the grid sheet context -> sheet:normalSwitch with no `await` between them, so the tab strip repaints on the next frame; the canvas cannot repaint until `refreshCells()` has completed a backend round trip.

**Repro:** TWO ACTIONS, no scripting. (1) On Sheet2, type something into D1 so the two sheets are visually distinguishable. (2) Click Sheet1's tab, then click Sheet2's tab while sampling. For ~60 ms the Sheet2 tab is bold and the grid below it still shows Sheet1. Automated: `E2E_MANUAL=1 npx playwright test --project=journey e2e/journeys/sheet-tab-state-undo.spec.ts --grep BUG-0043`, which PRINTS both windows ('[BUG-0043] torn window: tab click N ms, undo M ms') and asserts only that undo is no worse than the click plus 50 ms, and that both settle. Raising that to the absolute claim -- no torn frame at all -- fails today, which is how this was found.
**Triage:** product-bug (confidence 0.95) — The switch is atomic in STATE and sequential in PAINT. The tab strip derives from the grid sheet context and re-renders synchronously with it; the canvas has to fetch the new sheet's cells before it can draw them. Closing it means either fetching the target sheet's viewport BEFORE the context swap and committing both in one batch, or suppressing the canvas paint until the fetch lands (which trades a wrong sheet for a stale one -- no better). The first is a change to the ONE switch path every tab click, script, MCP tool and undo goes through, and the register's standing warning about that path is that the last time it had two hydration paths, the second was mount-only and painted sheet 1's panes on sheet 2.
**Fix:** fixed — FIXED 2026-08-14, by the register's own prescription -- fetch the target sheet's viewport BEFORE the context swap and commit both in one batch -- and the measurement found the tear had TWO mechanisms, not one. (1) THE CANVAS HALF: every switch initiator (SheetTabs' click/add/delete/applySheetsResult and applyRestoreToTheView for undo/redo) now awaits `primeSheetSwitch()` AFTER the backend switch and BEFORE the first dispatch; the prime runs GridCanvas's own registered prefetcher (same calculateFetchRange, same getViewportCells+getSpillRanges) into a one-shot handoff slot (`app/src/core/lib/sheetSwitchPrefetch.ts`, exposed to Shell via @api), and the EXISTING `sheet:normalSwitch` handler -- no second channel, no second hydration path -- takes the payload and commits it synchronously inside the dispatch turn; a `useLayoutEffect` then paints the canvas before the browser paints, so React commits strip and cells in ONE flush. The dispatch sequence itself still contains no `await` (the §13 atomicity discipline; the prime sits strictly before the first dispatch), a same-sheet undo primes nothing, and a missing/failed prime degrades to the old fetch-after-swap path, never to a wrong paint. A sheet-epoch guard in fetchCells discards any viewport response that straddled a switch (the backend answers for whichever sheet is active when it SERVES the call) and re-fetches via the existing deferral machinery. (2) THE STRIP HALF, which the prime alone exposed at 33 ms on undo: the strip's bold weight rendered from SheetTabs' LOCAL activeIndex, which a backend-initiated switch only reaches through the 'Syncing activeIndex with Redux' useEffect -- one flush AFTER the canvas painted. `paintedActiveIndex` now renders the highlight from the grid sheet context, making followBackendSheetActivation's standing comment ('the tab strip has no state of its own... it syncs its highlight from this') true of the pixels; local activeIndex remains for behaviour only. MEASURED with the committed probe, cold app, consistent bundle: before 60-67 ms (click) / 21-71 ms (undo); after 0 ms / 0 ms across five consecutive runs, and the spec now asserts the ABSOLUTE claim (tornWindowMs === 0 for both gestures) it could not before. Switch latency unmoved: landed-at 90-156 ms (click) / 52-83 ms (undo) vs the pre-fix canvas catch-up at ~98 ms. TEETH: with the prime sabotaged to a no-op on the running build, the raised test fails with 'tab click 64 ms' and samples showing the strip at t=112 ms over ink at t=191 ms -- the filed symptom exactly; restored byte-identically (sha256 a6b85099f95e07ebd43a6b1c0d8eba42484ad8f4139e3a961e955b60906873b7) and green again. 10 unit tests pin the handoff slot (exactly-once take, foreign-sheet refusal, staleness, disposal ownership, renderSignal bracketing). The atomicity unit suite (undoSheetActivation) passes unchanged -- the event sequence and detail shapes are untouched. CAUTION FOR FUTURE MEASUREMENT: under `tauri dev`, HMR cycles that reload sheetSwitchPrefetch.ts can split its module state across stale importer instances (prime writes into one, take reads another), which resurrects a ~30 ms tear that does NOT exist on a cold bundle -- measure this bug only on a verified cold launch.
  Files: app/src/core/lib/sheetSwitchPrefetch.ts, app/src/core/components/Grid/GridCanvas.tsx, app/src/core/components/Spreadsheet/useSpreadsheetSelection.ts, app/src/shell/SheetTabs/SheetTabs.tsx, app/src/api/index.ts, app/src/core/lib/__tests__/sheetSwitchPrefetch.test.ts, app/e2e/journeys/sheet-tab-state-undo.spec.ts

## BUG-0053 `[fixed]`

**Found:** 2026-08-13 (manual)
**Oracle:** screenshot-baseline

THE `grid-scroll-before` GOLDEN WAS CAPTURED FROM A CONTAMINATED WORKBOOK, so the test it guards can only pass when an unrelated spec has run first in the same app session. `scrolling.spec.ts > scroll down via mouse wheel changes viewport` writes "Top" into A1 and screenshots the grid. MEASURED: on a freshly launched app with `new_file` invoked immediately before the run, the diff shows the golden containing TWO CELLS THE LIVE GRID DOES NOT HAVE -- "First second" at B6 and "original" at B10 -- and both are written by `e2e/tests/inline-editor-live.spec.ts` (setCellValueDirect("B10", "original"), grep-confirmed). In the FULL ordered run the same test fails the other way as well: the live grid then carries residue the golden lacks (tables at M1:O6 and R1:S10, "Col1/Col2", "Item/Amount"). So neither a clean workbook nor a differently-contaminated one matches, and the only state that would is the exact accumulated state the capture happened to see. NOT CAUSED BY THIS PASS: nothing in it writes those cells or touches the renderer, and the failing comparison is between a golden and a wiped grid.

**Repro:** TWO STEPS. (1) Launch the app cold. (2) `E2E_MANUAL=1 npx playwright test --project=functional e2e/tests/scrolling.spec.ts`. The first test fails on `grid-scroll-before.png`; the diff at `test-results/scrolling-*/grid-scroll-before-diff.png` shows only cells the GOLDEN has and the live grid does not. Invoking `new_file` immediately before the run does not change the diff, which is what proves the extra content is in the baseline rather than in the session.
**Triage:** test-bug (confidence 0.95) — A screenshot golden captured mid-suite with whatever the previous specs had left in the workbook. This is the golden-provenance class §12 opened (`goldenCorpus.ts` reads three provenance axes) reaching a screenshot that lives in the FUNCTIONAL project rather than the visual one, and so never went through `validate-baselines.mjs`. The test is wrapped in `softly()`, which does not suppress a mismatch -- only the missing-snapshot case was ever believed to be soft.
**Fix:** fixed — THE RECORDED ONE-LINE FIX, THEN A DELIBERATE RECAPTURE. (1) scrolling.spec.ts test 1 now calls resetToNewWorkbook before its captures, so the golden is a function of the test alone. (2) Both contaminated goldens recaptured 2026-08-14 on a COLD app under the asserted capture environment (webview2Args pins; isolated instance on CDP 9223 because the shared 9222 machine was contested -- a first recapture attempt on 9222 photographed ANOTHER suite mid-run and was caught by ink attribution and reverted). DIFF ATTRIBUTED PIXEL-FOR-PIXEL: grid-scroll-before lost exactly the two contamination clusters -- "First second" at B6 (x90-156, y124-132) and "original" at B10 (x90-132, y204-215), 256 ink px, matching the comparator's measured 255 differing px -- and kept "Top" at A1 (x27-45); grid-scroll-after-wheel-down lost 1,300 dark px of multi-spec residue (rows 3-20) and now shows the clean scrolled grid at the same row-3 offset the old baseline framed. grid-scroll-distant-cell-z100 and grid-scroll-row-5000 were never contaminated and are byte-identical (a mid-recapture rewrite of row-5000 by a FAILING test on the contested app was detected and reverted). VERIFIED: two cold runs agree (4/4 then 4/4 with unchanged hashes); goldenCorpus provenance 44/44 (both new files read dpr2, 39,430/39,511 hairline px, no-chrome profile population); validate-baselines.mjs review flow: 4 PASS / 0 CONCERN / 0 FAIL. Residual, stated plainly: the wipe now runs MID-SUITE in the full ordered functional pass; downstream specs that encoded pre-scrolling residue in their own goldens would surface on the next full pass, which was not runnable this session because another agent held the machine.
  Files: app/e2e/tests/scrolling.spec.ts, app/e2e/tests/__screenshots__/scrolling.spec.ts/grid-scroll-before.png, app/e2e/tests/__screenshots__/scrolling.spec.ts/grid-scroll-after-wheel-down.png

## BUG-0054 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** backend-cascade-integrity

A ROW/COLUMN DELETE THAT FULLY COVERS A PIVOT REGION DELETES THE PIVOT WITHOUT THE CASCADE, WITHOUT AN UNDO RESTORE AND WITHOUT AN ANNOUNCEMENT. `shift_pivot_regions_for_row_delete` / `_col_delete` (app/src-tauri/src/commands/structure.rs, the two `pivot_tables.remove(&pid)` sites) remove the pivot RAW. Compare `delete_pivot_table` (pivot/commands.rs), which (1) runs `object_deps::cascade_deleted_sources` so the slicers, timeline slicers and ribbon-filter targets bound to the pivot die with it, (2) records `pivot_delete` + the cascade restores on the undo stack, and (3) cleans `views` and `active_pivot_id`. The structural path does NONE of these: slicers/timelines survive pointing at a dead pivot id (the exact orphan §3bt's cascade exists to prevent), `pivot_state.views` keeps the dead entry, undoing the row delete restores the pivot's CELLS (via the structural snapshot / engine RestoreSnapshot, which carry cells only) but not the pivot object -- a region that looks like a pivot and answers to nothing -- and no announcement reaches the frontend stores. The object_deps census does not see it because these sites are not a delete COMMAND; the same reasoning that hid `tables` from §3bn's sweep (BUG-0047: 'it has its own re-key, so it looked handled').

**Repro:** FOUR STEPS, no scripting needed for the backend half. (1) Create a pivot whose destination occupies, say, rows 10-15. (2) Bind a slicer to it. (3) Select rows 8-20 (whole rows) and Delete Sheet Rows. (4) Observe: get_pivot_tables no longer lists the pivot; the slicer still lists it as its source (orphan); Ctrl+Z brings the ROWS back with the pivot's rendered cells but `get_pivot_at_cell` finds nothing -- the pivot object is gone for good. The FRONTEND ghost-tab half (Analyze/Design tabs surviving under a stationary cursor when the rows are deleted by a script's api.deleteRows) was fixed 2026-08-14 by `reconcileActivePivotAfterShift` in the Pivot selection handler, pinned by activePivotReconciliation.test.ts; the backend halves are what stay open.
**Triage:** product-bug (confidence 0.9) — The structural shift helpers pre-date the object-dependency cascade and were never onboarded: they hold `protected_regions` + `pivot_tables` for their whole pass, so the cascade (which takes the slicer/timeline/ribbon locks) cannot run inline -- it has to collect the removed pivot ids and run after the drops, the way `delete_pivot_table` runs it FIRST for the same lock-discipline reason. The undo half needs the removed pivots recorded as `pivot_delete` restores inside the row-delete's own transaction (cascade restores first, then the pivot, then the rows -- reverse-replay order), and the frontend told via the `pivot`+`slicer`+`ribbonFilter` domains. `delete_rows`/`delete_columns` already thread pivot_state and ribbon_filter_state; slicer_state and timeline_state would need to join the signatures (delete_rows_impl exists over plain references, so the behaviour is unit-testable).
**Fix:** fixed — The structural delete paths now give a fully-covered pivot the SAME death delete_pivot_table gives one: collect_doomed_pivots captures the pivot_delete payloads and the DeletedSource list using the exact covered-region predicate the remove sites use; cascade_deleted_sources runs after every refusal gate and before the lock phase (the delete_pivot_table ordering -- the undo lock is never held across a store lock); the restores land inside the ONE structural transaction (cascade first, so reverse replay recreates the pivot before its slicers); the shift helpers clean views/active_pivot_id under the pivot_tables guard and prune the object script after every guard drops; and the frontend routes announce the slicer/pivot/ribbonFilter domains on both the active-sheet and off-sheet branches. Declared gap inherited from delete_pivot_table: timeline slicers still have no restore arm (TimelineSlicerState is not persisted), so a cascaded timeline does not come back on undo -- noted on delete_timeline_slicer.
  Files: app/src-tauri/src/commands/structure.rs (collect_doomed_pivots predicts the two remove sites; delete_rows_impl / delete_columns_impl / off_sheet_structural_edit run the slicer/timeline/ribbon-filter cascade BEFORE their lock phase and record cascade + pivot_delete restores INSIDE their transaction; shift_pivot_regions_for_row_delete / _col_delete clean views + active_pivot_id and prune object scripts; insert paths + all four Tauri wrappers gain slicer_state/timeline_state pass-through), app/src-tauri/src/object_deps.rs (record_source_cascade_undo_into: same recording over a guard the caller already holds -- the structural paths hold the undo lock for their whole transaction), app/src-tauri/src/undo_commands.rs (pivot_delete_snapshot_bytes: the restore payload built through the ONE struct the restore arm deserializes), app/src/core/lib/tauri-api.ts (deleteRows/deleteColumns announce MUTATION_REFRESH [slicer, pivot, ribbonFilter] on both branches -- the deleteSheet recipe), app/src-tauri/src/commands/pivot_structural_delete_tests.rs (new, 4 tests), app/src/core/lib/__tests__/structuralDeleteCascadeAnnouncement.test.ts (new, 4 tests), app/src-tauri/src/commands/cross_sheet_recalc_tests.rs + d8_structural_recalc_tests.rs + off_sheet_tests.rs (harness gains the timeline state; call sites extended)

## BUG-0055 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** recalc-consistency

A ROW INSERTED INSIDE A FORMULA'S RANGE NEVER JOINS THAT FORMULA'S DEPENDENCY EDGES, so writing into the inserted row recalculates NOTHING. Measured on a clean workbook: A900=10, A901=20, A902=30, A903=SUM(A900:A902)=60. insert_rows at 900 shifts everything correctly and re-points the formula to =SUM(A900:A903) - the D8 re-pointing works. Then update_cell A901=15: the SUM's display STAYS 60 (expected 75). Control 1: writing 25 over the shifted 20 at A902 (a cell the range covered BEFORE the insert) recalculates to 80 - and that 80 = 10+15+25+30 proves the engine's value store has the 15; only the EDGE is missing. Control 2: a fresh =SUM(A900:A903) entered afterwards computes 80 immediately. So insert_rows re-points the AST and shifts the EXISTING edges but registers no edges for the cells the range GREW over; every formula whose range was expanded across an insert is deaf to the inserted cells until some originally-covered cell changes.

**Repro:** FOUR STEPS on a cold app. (1) A840=10, A841=20, A842=30, A843=SUM(A840:A842). (2) insert_rows at 841. (3) update_cell into the inserted A841 = 15. (4) Read A844's display: stays at its pre-write value instead of 75. Automated: E2E_MANUAL=1 npx playwright test --project=functional e2e/tests/regression-scenarios.spec.ts --grep 'inserting a row expands' - re-enabled 2026-08-14 and DELIBERATELY LEFT FAILING (no fixme, no suppression); its first assertion (range expansion to SUM(A840:A843)) passes, its second (recalc on write to the inserted row) is this bug.
**Triage:** product-bug (confidence 1) — The D8 family, one edge over: D8 shipped re-pointing + re-evaluating the formulas structural edits MOVE, but the dependency-map maintenance for a RANGE that grew across the inserted rows registers edges only for the shifted copies of edges that already existed - the inserted cells were never inside the old range, so no edge is shifted into place for them, and nothing walks the re-pointed AST to register the widened span. The fix direction is to re-register (normalize_cross_sheet_refs-aware, one shared helper) the dependents of every formula whose range a structural insert widened - or simply re-register the re-pointed formula's dependencies wholesale after the rewrite, which is what entering the same formula fresh does (control 2 proves that path is correct).
**Fix:** fixed — The active-sheet insert paths were the only structural edits maintaining dependency edges by SHIFTING alone, and a shifted copy cannot exist for a cell the range never covered before -- so a range that GREW across the insert had no edge for the inserted cells. They now rebuild the edge maps from the re-pointed stored ASTs via rebuild_all_dependencies -- the same one-authority call the off-sheet structural path has always made -- which also expands defined names and structured references, so a name-backed formula keeps its expanded cell edges (the regression a raw-parse re-extraction would have introduced). Deliberately after the table-boundary shift (structured refs must expand against post-shift tables) and after every guard is dropped (the rebuild takes the grid lock). Deletes need no change: a delete only shrinks or shifts ranges, and the shifted edges remain complete -- pinned by the partial-cover test staying green.
  Files: app/src-tauri/src/commands/structure.rs (insert_rows_impl + insert_columns_impl call crate::undo_commands::rebuild_all_dependencies after the table-boundary shift and before recalc_structural_side_effects), app/src-tauri/src/commands/d8_structural_recalc_tests.rs (3 new tests, section 7), app/e2e/tests/regression-scenarios.spec.ts (the repro spec itself had an off-by-one: it inserted at RAW row 841 -- display A842 -- but wrote into A841, overwriting the 20, so its 75 expectation could never hold; corrected to write into the actually-inserted row)

## BUG-0056 `[fixed]`

**Found:** 2026-08-14 (walk, seed 1786456498740)
**Oracle:** no-console-errors

THE WALKER RESET NEVER TEARS DOWN FLOATING RANGES, AND THE STALE FRONTEND STORE POISONS EVERY LATER WALK IN THE SESSION. deepResetForWalk tears down slicers/charts/sparklines/pivots/tables through their own APIs and relies on new_file for the rest -- but resetToNewWorkbook invokes new_file RAW and never emits AFTER_NEW (the product's File > New does a full window.location.reload(), so the product never sees this state). new_file clears the backend floating-range rows and tells the FloatingRange extension nothing: its store keeps the dead rows, the overlay keeps their regions, and on the next redraw frRenderer fetches cells for ids the backend no longer has -- '[FloatingRange] Failed to fetch cells for <uuid>: No floating range with id <uuid>' x3, tripping no-console-errors at step 1 of WHATEVER walk runs next. Measured: seed 1786456498740's walk 1 created 3 FRs (the new fr.create cap) and passed; its rapid-fire walk then failed at step 1 fr.create, and seed 90010001 -- run next in the same app session, generating NO fr actions at all -- failed both tests at step 1 (table.create / sparkline.create) on the same three stale uuids. BUG-0004's exact class (reset leaves frontend object stores unnotified), recreated for the newest object family.

**Repro:** `tests/regression/repros/BUG-0056.trace.json` (1 actions)
**Triage:** test-bug (confidence 1) — reset.ts's own header states the rule -- 'new_file leaves ... frontend object stores not notified, so walks explicitly tear down all objects through their own APIs first' -- and the FloatingRange extension (landed 2026-08-13) was never added to that teardown list. The product is unaffected: every product route to a new/opened document either reloads the window (File > New) or emits AFTER_NEW/AFTER_OPEN (file-api), both of which resync the store.
**Fix:** fixed — deepResetForWalk tears floating ranges down through the ANNOUNCED @api wrapper (listFloatingRanges + deleteFloatingRange, the same route the product's menus use), then unconditionally resets and reloads the extension's store (resetFloatingRangeStore + loadFloatingRangesFromBackend + syncFloatingRangeRegions) so a session ALREADY poisoned by an earlier raw new_file is healed even when the backend has nothing left to delete. Verified live on the poisoned session itself: re-running seeds 1786456498740 and 90010001 immediately after the fix, without restarting the app, went from '1 failed / both failed at step 1' to green.
  Files: app/e2e/walker/reset.ts

## BUG-0057 `[fixed]`

**Found:** 2026-08-14 (walk, seed 1786456498740)
**Oracle:** no-console-errors

A BACKEND-INITIATED FLOATING-RANGE DELETE LEAVES A REPAINT WINDOW THAT RENDERS -- AND FETCHES -- THE DELETED ROW, LOGGING console.error FOR A BENIGN RACE. The @api wrapper's deleteFloatingRange (and a script's api.deleteFloatingRange through the broker -- both routes confirmed in source) deletes the backend row, announces MUTATION_REFRESH['floatingRanges'], and emits CELLS_UPDATED. The FloatingRange extension prunes its store in an ASYNC reload (reloadForBackendChange awaits a backend list), but the CELLS_UPDATED handler has already invalidated caches and requested an overlay redraw -- so the overlay renders the store's still-present row, frRenderer fetches its cells, the backend answers 'No floating range with id <uuid>', and frRenderer.ts logged console.error. The extension's OWN delete path (menus, keyboard, provider seam) prunes the store synchronously before any repaint, which is why the product UI never showed it; a restricted script deleting an on-screen FR hits it every time. Severity: transient one-repaint ghost + console noise; self-heals when the reload lands.

**Repro:** Walk-level, pre-fix: E2E_MANUAL=1 INVARIANT_SEED=1786456498740 npx playwright test --project=invariant --grep 'rapid create-delete' (with the BUG-0056 reset fix in place, so the failure left is this one) -- fails no-console-errors on the id fr.delete removed at step 18. Product-level: run a script that calls api.deleteFloatingRange on a floating range currently on screen and watch the console.
**Triage:** product-bug (confidence 0.9) — The announce-then-reload architecture makes a stale-render window unavoidable for backend-initiated deletes; the defect is that the renderer treats a fetch failure inside that window as an error. The distinction that matters is not the failure but its PERSISTENCE: a row gone from the store by the time the dust settles lost a delete race (silent); a row still cached is a real store/backend inconsistency (loud -- that loud path is what caught BUG-0056 and must not be weakened).
**Fix:** fixed — fetchFrCells DEFERS the verdict on a failed fetch (1 s) instead of softening it: if the row has left the store by then, the fetch lost a delete race -- prune its cache silently and redraw; if the row is STILL in the store, console.error exactly as before, so a persistent stale store (BUG-0056's class) keeps failing walks loudly, one settle later. Two unit cases pin both branches (fake timers; the loud branch asserts the error still names the id). Verified live: the rapid-fire walk on seed 1786456498740 that failed on this exact race is green after the fix, with fr.create/fr.delete exercised.
  Files: app/extensions/FloatingRange/rendering/frRenderer.ts, app/extensions/FloatingRange/rendering/__tests__/frRendererFetchVerdict.test.ts

## BUG-0058 `[fixed]`

**Found:** 2026-08-14 (walk, seed 90140202)
**Oracle:** recalc-consistency

A CROSS-SHEET DEPENDENT ON A NON-ACTIVE USER SHEET GOES STALE WHEN A FLOATING-RANGE CELL (OR ANY OFF-SHEET-WRITTEN CELL) CHANGES. Measured on the minimized trace: two grid cells hold ='Float1'!A1+4 on two different sheets; a script-path write rewrites Float1!A1 while a THIRD sheet is active; full recalculation then changes exactly one cell -- 'sheet 1 cell 60:48: 46 -> 4' -- and the post-F9 probe proves F9's value is the correct one (Float1!A1 = 'Sheet1 (2)'!B2*2 = 0, so +4 = 4). The dependent on the ACTIVE sheet updated; its twin on the non-active sheet held the pre-write value. MECHANISM (read from source, then pinned by a unit test): `recalc_after_off_sheet_write` (app/src-tauri/src/commands/data.rs) re-evaluates the WRITTEN sheets, every OBJECT-backed sheet, and the ACTIVE sheet, twice -- and never any other user sheet. The cross-sheet dependents map is not consulted at all, so a registered edge into a non-active, non-written user sheet is simply outside the pass's scope. Every off-sheet write route funnels here: floating-range cell writes, a script's update_cell_on_sheets/clear_range_on_sheets, the QuickJS bulk path.

**Repro:** `tests/regression/repros/BUG-0058.trace.json` (17 actions)
**Triage:** product-bug (confidence 1) — CONFIRMED from source: recalc_after_off_sheet_write's sheet set is written+object+active, a hand-fixed scope that predates cross-sheet edges reaching arbitrary user sheets. GAP A made the edges exist; nothing made this pass walk them. The class is the standing CLAUDE.md rule ('a command that writes without recalculating dependents'), one function over.
**Fix:** fixed — recalc_after_off_sheet_write now extends its recalc set with every sheet holding a cross-sheet DEPENDENT of anything on the written/object sheets, computed to a TRANSITIVE fixpoint over sheet names (a repaired dependent can feed a formula on yet another sheet). Lock use is strictly sequential -- snapshot sheet_names, drop; lock the dependents map, collect, drop -- so no nesting is added to the crate's lock graph (the three-deadlocks history is why that is stated in the code). Regression pinned by a_non_active_sheet_dependent_follows_a_floating_range_write (fails pre-fix on both the one-hop and the two-hop assertion); verified live by replaying the 17-action trace (fails pre-fix, passes post-fix) and re-running the finding walk seed 90140202 to completion.
  Files: app/src-tauri/src/commands/data.rs, app/src-tauri/src/commands/floating_range_recalc_tests.rs

## BUG-0060 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** rust-unit

SCRIPT-BATCH RECLAIM DESTROYS ITS OWN HEALING RECORD, AND CONNECTION TEARDOWN STRANDS THE BATCH INTERLOCK. reclaim_expired_script_batches (bi/model_editor.rs) removed EVERY expired entry from the process-global script_batch_registry() and only then attempted rollback_model_batch against the CALLER's BiState -- so a rollback that fails (connection not resolvable by that caller) left `in_batch` set in the global model_undo_store with the registry record already gone: nothing can ever reclaim it again. That is the PERMANENT form of the exact wedge the deadline exists to heal -- every later batch on that ModelKey refuses to open and record_model_undo silently suppresses every later edit's undo snapshot; ModelKey outlives both connections and documents, so a reconnect or a reopened workbook inherits the wedge. Reachable in production two ways: (1) bi_delete_connection removed a connection with a batch open (trusted OR script) and never released it; (2) reset_bi_connections (File > New / Open) drained all connections while a batch was open, leaving both globals set. Manifested in the test binary as the watched flake: many BiStates run concurrently against the same globals, and a sibling test's reclaim stole the freshly-expired zombie batch of an_abandoned_script_batch_is_reclaimed_and_rolled_back in the window between the test back-dating the deadline and its own healing edit; the thief's rollback failed against the wrong BiState ('Connection not found'), the record was gone, and the zombie's model stayed wedged -> 'the wedge must be cleared' at model_editor.rs:9103.

**Repro:** Deterministic: bi::model_editor::tests::reclaim_leaves_batches_whose_connection_it_cannot_resolve pins the exact interleaving (two BiStates; B's reclaim runs while A's expired batch is on record -- pre-fix it steals and strands, post-fix it must not touch it). Stochastic pre-fix: full app-lib suite at default parallelism (12 threads) failed ~1 in 3 runs; NOT cold-I/O -- the 2026-08-14 strike was on the third (warm) consecutive run, and it is not wall-clock/tokio-timing either: the deadline is back-dated a full second, the window is scheduling-order between threads sharing the global registry.
**Triage:** app-bug (confidence 1) — CONFIRMED from source. Three defects, one root: the reclaim consumed the registry record before knowing the rollback could succeed; rollback resolves the batch's connection_id in the caller's BiState; and neither bi_delete_connection nor reset_bi_connections released a live batch while a rollback was still possible. The 20c characterisation ('load-sensitive test isolation, not product behaviour') was HALF right: the trigger is test-binary concurrency, but the record-then-heal gap it exposes is a real production wedge via connection delete / document reset.
**Fix:** fixed — The reclaim can no longer steal what it cannot heal, a failed rollback clears the interlock instead of stranding it, and the two connection-teardown paths release batch state while release is still possible. Semantics preserved: a reclaimed batch is still rolled back (never committed); when no engine remains to reinstall into, the pre-batch snapshot stays as one undoable step. Deleting a connection while a LIVE script batch owns the model is now refused with the same bounded error the trusted end/cancel paths give.
  Files: app/src-tauri/src/bi/model_editor.rs (reclaim_expired_script_batches now takes ONLY batches whose connection lives in the calling BiState -- removing an entry it cannot roll back is destroying the last healing record; on a rollback that still fails, clear_batch_interlock() heals the in_batch flag instead of logging and stranding it; new prepare_connection_delete() gate: reclaims expired script batches, REFUSES the delete while a live script batch owns the model (same rationale + bound as guard_no_live_script_batch), and rolls back a trusted batch left open on the model's last connection; new forget_batches_for_model_key() for document reset; + 4 pinning tests), app/src-tauri/src/bi/commands.rs (bi_delete_connection runs prepare_connection_delete before removing the connection; reset_bi_connections forgets batch state per torn-down connection's model key)

## BUG-0061 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** live-route-probe

A DATE NUMBER FORMAT APPLIED THROUGH THE FORMAT CELLS DIALOG RENDERS THE FORMAT STRING ITSELF, NOT A DATE. Measured live: type 45000, Ctrl+1 > Date > '2024-01-15 (ISO)' > OK -- the cell displays the literal text 'yyyy-mm-dd' (backend display AND rendered pixels agree; crop saved). MECHANISM (read from source, then pinned by unit tests): the dialog stores its date presets LOWERCASE ('yyyy-mm-dd', styles.rs parse_number_format), while core/engine/src/number_format.rs::format_date_number was a hand-rolled .replace chain that only knew the UPPERCASE spellings YYYY/YY/MM/DD/M/D -- any other spelling fell through every replace untouched and the format string became the display. The engine's own presets::date_iso() uses 'YYYY-MM-DD', which is why nothing in-repo ever hit it. Same class one level down: xlsx builtin numFmt 15-17 ('D-MMM-YY') produced digit salad (MMM is not a token the chain knew), datetime combos ('M/D/YYYY HH:mm') left the time half raw, and lowercase 'hh:mm' rendered 13:00 as '01:00' (h was unconditionally 12-hour; Excel's rule is 24-hour unless AM/PM is present). SECOND DEFECT found while pinning the fix: the full custom-format engine (custom_format.rs), whose lexer is case-insensitive and was the delegation target, DROPPED '/' and '.' in date sections -- '/' lexes as FractionSeparator and '.' as DecimalPoint, and format_datetime_section rendered neither -- so 'MM/DD/YYYY' came out '01152024'.

**Repro:** Live: cold app, type 45000 into any cell, Ctrl+1 > Date > ISO > OK -- pre-fix the cell shows 'yyyy-mm-dd'. Unit, no app: number_format::tests::date_format_lowercase_dialog_spelling_formats_a_date (45000 + Date{'yyyy-mm-dd'} must be '2023-03-15'), date_format_never_echoes_the_format_string, date_format_xlsx_builtin_month_names, time_format_h_is_24_hour_without_am_pm; custom_format::tests::test_date_format_slash_and_dot_separators_are_literal. All fail pre-fix.
**Triage:** product-bug (confidence 1) — CONFIRMED from source: two date formatters existed -- a token-replace toy in number_format.rs (uppercase-only, no month names, no datetime combos, 12-hour always) and the real case-insensitive engine in custom_format.rs -- and the dialog's lowercase presets only ever reached the toy. One formatter now: Date/Time delegate to custom_format, which also had its date-section '/' and '.' literals restored.
**Fix:** fixed — format_date_number and format_time_number now delegate to custom_format::format_custom_value -- the case-insensitive, full-token engine (month/day names, datetime combos, elapsed [h], AM/PM-driven 12/24h) -- instead of an uppercase-only .replace chain; their dead helpers (chrono_lite_date, is_leap_year, the token chain) are gone. format_datetime_section renders FractionSeparator as literal '/' and DecimalPoint as literal '.' in date/time context ('m/d/yyyy', 'dd.mm.yyyy', 'mm:ss.0'). Pinned by 5 new unit tests across the two files; full core workspace green (engine 551 passing incl. 20 number_format + 66 custom_format). Verified live post-fix on a cold isolated app: dialog Date ISO on 45000 renders '2023-03-15' (pixels + backend), dialog Time renders '13:30:00', every ribbon route unchanged.
  Files: core/engine/src/number_format.rs, core/engine/src/custom_format.rs

## BUG-0062 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** visual-golden

THE RIBBON'S FONT BOX REPORTS 'system-ui' FOR EVERY EMPTY CELL -- a font no cell in the document renders in. Measured live: backend get_style(0) returns fontFamily 'Calibri', fontSize 11 (the Excel-parity default; the canvas renderer paints exactly that via theme.cellFontFamily) -- yet selecting any EMPTY cell shows 'system-ui' in the combo, and a cold-booted document (A1 empty) shows it from the first frame, which is how it got baked into three committed goldens (core-empty-grid, ribbon-core-default-ribbon, workflow-multisheet-sheet1) and flagged as a validator CONCERN. MECHANISM: BUG-0028's fix made useHomeTabState set currentStyle = null when getCell resolves null (an empty cell), so stale formatting stopped leaking -- but null is not the truth either: the font combo's fallback was '?? "system-ui"' (the fontSize combo's was already the correct '?? 11'). Cleared and default are different things; the ribbon showed neither the previous cell nor the actual default.

**Repro:** Live: cold app, click any empty cell (or just boot -- A1 is empty), read the ribbon font combo: 'system-ui' pre-fix, 'Calibri' post-fix. Unit: homeTabStyleFollowsDocument.test.tsx -- 'reports the DEFAULT style over an empty cell' (asserts getStyle(0) is loaded and fontFamily/fontSize read Calibri/11); the BUG-0028 clearing arm now asserts the default style rather than null, so both defects stay pinned together.
**Triage:** product-bug (confidence 1) — CONFIRMED from source: the empty-cell branch of useHomeTabState's style read set null where the document default belongs, and the font combo's fallback named a font that exists nowhere in the document. BUG-0028 (ribbon state must be a function of the document) was fixed by clearing; the truthful function of the document for an empty cell is style index 0.
**Fix:** fixed — An empty cell now loads the DOCUMENT DEFAULT style (getStyle(0)) instead of null -- BUG-0028's clearing is preserved (nothing lit, currentCellData stays null) and the combo reports Calibri 11, matching what typing into the cell would produce. The font combo's fallback for styles without an explicit family is 'Calibri' (what the renderer actually paints via theme.cellFontFamily), mirroring the fontSize combo's '?? 11'. HomeTab vitest 60/60; SIX goldens were deliberately recaptured with every changed pixel attributed to the ribbon combo/alignment-toggle band: core-empty-grid, ribbon-core-default-ribbon, menu-file-open, menu-edit-open, workflow-multisheet-sheet1 (visual) and empty-grid-full-window (functional). The corpus product-state axis (goldenCorpus.ts) was re-aimed with the fix: an empty workbook on the FIXED build now paints exactly ONE lit toggle (Center Vertically -- VerticalAlign::Middle IS the document default), so each empty-document golden pins its expected pressed-accent reading (546 visible / ~29 covered, +/-150) instead of asserting unlit, and the axis fires in BOTH directions -- below the band is the pre-BUG-0062 null-style face, above it a BUG-0028-style latch. goldenCorpus suite 37/37, full vitest 106,807 green.
  Files: app/extensions/BuiltIn/HomeTab/components/useHomeTabState.ts, app/extensions/BuiltIn/HomeTab/components/HomeTabGroupComponent.tsx, app/extensions/BuiltIn/HomeTab/__tests__/homeTabStyleFollowsDocument.test.tsx, app/e2e/goldenCorpus.ts, app/e2e/__tests__/goldenCorpus.test.ts

## BUG-0063 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** functional-e2e

MCP START CLAIMS SUCCESS BEFORE THE SERVER CAN ACCEPT, AND A BIND FAILURE IS INVISIBLE. mcp_start (app/src-tauri/src/mcp/mod.rs) set running=true, returned 'MCP server started on port N', and only THEN -- on a freshly spawned thread that first creates a whole Tokio runtime -- bound the TCP listener. A client following the documented start -> status -> connect sequence can beat the bind and get ECONNREFUSED from a server that mcp_status reports as running:true with a valid bearer token. Under a loaded machine (test 265 of a 554-test ordered run) the E2E spec lost exactly that race; the run's own backend log shows the spec's cleanup mcp_stop ('Server stop requested', line 8267) landing BETWEEN 'Server starting' (8266) and 'Server listening' (8268), after the fetch had already been refused -- the newborn server then shut down having served nothing. Worse: an actual bind failure (port in use) was only a background log line -- the command had already returned success and `running` silently flapped back to false. A real user pasting the MCP config into Claude Desktop hits the same refused connection with no error surfaced anywhere.

**Repro:** Stochastic in-suite: full --project=functional, the first MCP spec in alpha order fails its handshake when the server thread loses the race (passes isolated -- 1/1 cold, 1.6m). Deterministic contract pin: mcp::bind_tests::a_successful_bind_accepts_before_any_accept_loop_and_a_conflict_is_an_err (a bound listener accepts with NO accept loop running; a port conflict is an Err naming the port).
**Triage:** app-bug (confidence 1) — CONFIRMED from source and from the run's interleaved backend log: the bind happened asynchronously after the success reply, so 'started' and running:true never meant 'accepting'. The 1.2s in-suite failure, the green isolated replay, and the 8266/8267/8268 log interleaving are all the same mechanism.
**Fix:** fixed — Success from mcp_start (and running:true from mcp_status) now means the port accepts connections at that instant -- the kernel backlog holds connections arriving before the accept loop spins up, so the race is structurally gone rather than narrowed. Port conflicts became caller-visible errors. No protocol or security-model change: token issuance, origin/host hardening and shutdown are untouched.
  Files: app/src-tauri/src/mcp/mod.rs (bind_mcp_listener(): the listener is bound -- and listening, kernel backlog live -- SYNCHRONOUSLY inside mcp_start before running/token/cancel state is touched; the bound socket is handed to the server thread and converted with TcpListener::from_std; a bind failure is now the command's own Err naming the port; + bind_tests contract test)

## BUG-0064 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** live-route-probe

THE FORMAT CELLS DIALOG'S PRESET LABELS AND EXAMPLES DO NOT USE THE CURRENT LOCALE'S SEPARATORS. Measured live on a sv-SE document: the Number category advertises '1,234.00' while clicking that preset renders '1 234,00' in the cell, and General's example showed '1234.5' against a grid that displays '1234,5' -- the sample text lies about the result it produces. Excel's samples are locale-correct. MECHANISM: getNumberFormatCategories(dec, thou) (numberFormats.ts) is locale-aware and unit-tested with locale arguments, but the ONLY production consumer was the NUMBER_FORMAT_CATEGORIES constant built with the US-English defaults; @api/locale (getLocaleSettings/getCachedLocale/onLocaleChanged) existed and no dialog code called it.

**Repro:** Live: on a sv-SE locale, select a cell, Ctrl+1, Number category -- pre-fix the presets read '1,234.00', post-fix '1 234,00'. Pinned by numberTabLocale.test.tsx (5 tests, both locale directions + async load + locale-change re-render + fallback); sabotage-verified (reverting the categories.find to the static constant fails 3 of 5).
**Triage:** app-bug (confidence 1) — CONFIRMED from source: the locale-aware builder had no production caller with locale arguments; the tab rendered the US-default constant unconditionally.
**Fix:** fixed — The Number tab's preset labels/examples now carry the document locale's separators, matching what clicking them actually produces. Preset VALUES are unchanged (locale-independent identifiers), so applied formats, persistence and the backend are untouched.
  Files: app/extensions/BuiltIn/FormatCellsDialog/tabs/NumberTab.tsx (categories are rebuilt from getNumberFormatCategories(locale.decimalSeparator, locale.thousandsSeparator): cached locale synchronously at mount, async getLocaleSettings when nothing is cached, onLocaleChanged re-render; US defaults only as the cannot-read-locale fallback), app/extensions/BuiltIn/FormatCellsDialog/tabs/__tests__/numberTabLocale.test.tsx (new, 5 pins)

## BUG-0065 `[fixed]`

**Found:** 2026-08-14 (manual)
**Oracle:** live-route-probe

AN UNTOUCHED OK IN FORMAT CELLS SILENTLY CORRUPTS THE CELL'S NUMBER FORMAT -- opening the dialog on a formatted cell to change ANYTHING (font, border) and clicking OK destroys the number format. Measured live, two shapes: (1) 'Number (2 decimals, with separators)' RESET to General ('-500,00' became '-500'); (2) 'Date (yyyy-mm-dd)' degraded to a Custom format that rendered '15ate (2023-03-15)' -- the leading D of the NAME consumed as a day token -- while get_style still reported 'Date (yyyy-mm-dd)' (Custom serializes as its format string), so even the style read-back hid the corruption. MECHANISM: get_style emits DISPLAY NAMES via format_number_format_name (api_types.rs); FormatCellsDialog.loadCurrentStyle stores that name and handleOK sends it back through apply_formatting -> parse_number_format (commands/styles.rs), which only knew preset values and format codes: names without format characters fell to General, names with them ('Percentage (0 decimals)', 'Date (yyyy-mm-dd)') became garbage Custom formats. The serializer's vocabulary did not round-trip through its own parser -- the exact defect class parse_text_rotation in the SAME FILE documents having fixed for text rotation. FRONTEND HALF: the dialog compared display names against preset values AT MOUNT (before the async style load), so a formatted cell always reopened on the General category with no preset highlighted.

**Repro:** Live: format a cell via Ctrl+1 (e.g. Number with separators, or Date ISO on 45000), reopen Ctrl+1, click OK without touching anything -- pre-fix the format resets to General or renders the display name as a custom format ('15ate (2023-03-15)'). Deterministic pins: every_display_name_the_serializer_emits_round_trips + the_untouched_ok_corruption_shapes_stay_dead + custom_format_strings_still_parse_as_custom (commands/styles.rs); numberTabDisplayName.test.tsx (6 pins) for category selection/highlight.
**Triage:** app-bug (confidence 1) — CONFIRMED from source and live in both directions: parse_number_format could not read format_number_format_name's output, and the dialog round-trips that output on every OK.
**Fix:** fixed — parse_number_format now accepts everything its serializer emits, so the dialog's read-modify-write is lossless; the dialog additionally opens on the owning category with the preset lit. No wire-format change: the emitted names are byte-identical (re-emission canonicality asserted in the round-trip test).
  Files: app/src-tauri/src/commands/styles.rs (try_parse_display_name: the full inverse of format_number_format_name, tried before format-code guessing; symbol_position reconstructed 'kr'->After, fixed-fraction max_digits from the preset table; + 3 pinning tests), app/src-tauri/src/api_types.rs (format_number_format_name pub(crate) + the round-trip contract documented on the serializer), app/extensions/BuiltIn/FormatCellsDialog/utils/numberFormats.ts (normalizeToPresetValue + categoryForFormat: display names -> preset value/category, prefix mapping for no-preset shapes like 'Number (5 decimals)'), app/extensions/BuiltIn/FormatCellsDialog/tabs/NumberTab.tsx (category re-derives when the async style load lands; preset highlight and preview match display names too), app/extensions/BuiltIn/FormatCellsDialog/tabs/__tests__/numberTabDisplayName.test.tsx (new, 6 pins)

## BUG-0066 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** excel-parity-overflow

A NEGATIVE value under a Date or Time format renders a PLAUSIBLE LIE instead of Excel's width-independent '####'. MEASURED, not read: core/engine/src/number_format.rs probe `negative_serials_under_date_and_time_formats_render_a_plausible_lie` prints -1.0 as Date(YYYY-MM-DD) -> "1900-01-01" and -0.5 as Time(HH:MM:SS) -> "12:00:00". Both are ordinary-looking values that a user cannot tell from real ones -- a subtraction that came out backwards reads as the first of January 1900 rather than as a refusal. Excel lists "formulas that return dates and times as negative values" as a distinct cause of '#####' and its remedy is NOT "widen the column" but "verify that dates and times are positive values": the refusal is SEMANTIC and width-independent, because a serial below zero has no representation in the 1900 date system. Mechanism: format_datetime_section (core/engine/src/custom_format.rs:1536) takes `days = value.floor()`, and for `days < 1` falls back to `(1900, 1, 1)` while the time half uses `value.fract().abs()` -- so the sign that made the value impossible is discarded before any string exists. WHY THE 2026-08-15 PARITY PASS COULD NOT CLOSE IT: every other rung of Excel's ladder is decidable from `CellData.display` plus `StyleData.numberFormat`, which is why that pass shipped entirely inside the renderer. This rung is not: `CellData` carries only the formatted display string, so by the time the renderer sees "1900-01-01" the negativity is gone and NO renderer-side rule can recover it. Not a missing check -- missing data.

**Repro:** Format a cell as Date (any pattern) and put a negative number in it -- e.g. =-1, or =A1-B1 where B1 is the later date. Calcula shows 1900-01-01 at every column width; Excel shows ##### at every column width and clears it only when the value becomes positive. Same with a Time format and =-0.5 (Calcula: 12:00:00). Unit-level: the engine probe named in the violation, which RECORDS today's answer rather than asserting Excel's.
**Triage:** product-bug (confidence 1) — CONFIRMED from source and from a measured probe. Two independent halves: (1) the engine formats an unrepresentable value instead of refusing it, and (2) the CellData transport carries no value type, so the renderer cannot re-derive the refusal. Fixing (1) alone would put '####' into `display`, which then travels into copy/paste, CSV export and the formula bar -- the marker is a RENDERING outcome, not a value, and must not become one.
**Fix:** fixed — FIXED AS FILED, on the integration pass that owned all three files at once. `OverflowClass { Text | Numeric | Unrepresentable }` added to api_types.rs with struct-level rename_all camelCase, `#[serde(default)]` + `skip_serializing_if` on the Text default so text cells cost nothing on the wire. The decision itself is ONE function, `overflow_class_for(&CellValue, &CellStyle)`, called from ONE place: `format_cell_value_with_color` (lib.rs), the only function in the app holding both the value and the style. Its inner match was split out as `format_cell_value_parts` so the class is stamped once on the way out rather than in each of seven arms -- an arm that forgot would have fallen back to Text (no marker) silently. ALL 56 CellData construction sites across 18 files were made to decide, by the compiler: the field carries no struct-literal default, so `cargo check` enumerated every one. Sites binding `let display = format_cell_value(&x.value, style, &locale)` were rewritten to `let (display, overflow) = format_cell_value_and_class(...)` rather than given a second call, because `format_cell_value(&a.value..)` beside `overflow_class_for(&b.value..)` type-checks perfectly while describing two different cells -- a mismatch that would surface only as a missing '####' in one workbook. Two tuple-destructure sites (commands/utils.rs, commands/data.rs get_viewport_cells) had the class threaded through the tuple so BOTH branches must decide. Renderer side: `classifyCellContent` takes an optional transported class and prefers it over its inference; `fitNumericDisplay` short-circuits to the marker on `unrepresentable` ABOVE the width test, so widening never clears it. THE SECOND HALF THE ENTRY PREDICTED IS ALSO CLOSED: text under an explicitly numeric format now reads as text (it read as numeric if it contained a digit), and digits under the `@` Text format read as text. The engine still renders -1.0 as "1900-01-01" ON PURPOSE -- per this entry's own triage, the marker is a RENDERING outcome and must not enter `display`, where it would travel into copy/paste, CSV export and the formula bar. The engine probe `negative_serials_under_date_and_time_formats_render_a_plausible_lie` still records that and still passes. TEETH PROVEN IN BOTH LANGUAGES, by sabotage on a built tree: replacing the Rust `is_date_or_time && *n < 0.0` guard with `false` turned exactly one of the eight new Rust tests red and left the other seven green; disabling the renderer short-circuit turned exactly two of the new TS tests red. Both restored and re-run green.
  Files: app/src-tauri/src/api_types.rs, app/src-tauri/src/lib.rs, app/src-tauri/src/animation_commands.rs, app/src-tauri/src/calculation.rs, app/src-tauri/src/commands/data.rs, app/src-tauri/src/commands/print.rs, app/src-tauri/src/commands/search.rs, app/src-tauri/src/commands/styles.rs, app/src-tauri/src/commands/utils.rs, app/src-tauri/src/consolidate.rs, app/src-tauri/src/data_tables.rs, app/src-tauri/src/goal_seek.rs, app/src-tauri/src/merge_commands.rs, app/src-tauri/src/named_styles_cmd.rs, app/src-tauri/src/persistence.rs, app/src-tauri/src/scenario_manager.rs, app/src-tauri/src/solver.rs, app/src-tauri/src/undo_commands.rs, app/src/core/types/types.ts, app/src/core/lib/gridRenderer/rendering/overflowMarker.ts, app/src/core/lib/gridRenderer/rendering/overflowMarker.test.ts, app/src/core/lib/gridRenderer/rendering/cells.ts

## BUG-0067 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** excel-parity-overflow

NUMBERS SPILLED ACROSS THEIR EMPTY NEIGHBOURS like text. The spill gate (cells.ts, pre-fix line 1000) read `!shouldWrapEarly && !isMergedMaster && (textAlign === 'left' || textAlign === 'center')` -- alignment only. It never asked Excel's second spill condition, "the cell value is text", so any numeric carrying an EXPLICIT Align Left (or Center) took the text path and ran across its empty neighbours instead of showing '####'. Numbers escaped only by accident: General alignment right-aligns them (cells.ts:935), and 'right' happens not to be in the list. So the defect was invisible until someone left-aligned a numeric column -- an ordinary thing to do to a column of ids or years -- and then a value too wide for its column silently annexed the cells beside it. This is a distinct wrong BEHAVIOUR, not the missing-marker parity gap §21c described: the register recorded only "Calcula ellipsizes; Excel shows ####" and did not mention that Calcula also SPILLS numerics.

**Repro:** Put 1234.5678 in a narrow column, format it Number (4 decimals), apply Align Left, and leave the column to its right empty. Pre-fix the value ran across the neighbour; Excel shows ######. Pinned by 'BUG-0067: a left-aligned NUMBER must not spill' in app/src/core/lib/gridRenderer/rendering/overflowRendering.test.ts, whose second case puts the identical string under the Text format '@' and requires it to STILL spill -- so the test proves the gate turns on the value's type and not on the alignment.
**Triage:** product-bug (confidence 1) — CONFIRMED from source. The gate encoded a proxy (alignment) for the property it actually needed (the value's type), and the proxy holds only while alignment is left at General.
**Fix:** fixed — The spill gate now carries Excel's condition 2 explicitly. Shrink-to-fit was excluded in the same edit: it is the competing remedy, so a cell that shrinks its font to fit has no overhang left to spill.
  Files: app/src/core/lib/gridRenderer/rendering/overflowMarker.ts (new -- classifyCellContent), app/src/core/lib/gridRenderer/rendering/cells.ts (spill gate requires contentKind === 'text'), app/src/core/lib/gridRenderer/rendering/overflowRendering.test.ts (new), app/src/core/lib/gridRenderer/rendering/overflowMarker.test.ts (new)

## BUG-0068 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** excel-parity-overflow

A NEIGHBOUR HOLDING ="" DID NOT BLOCK A SPILL, so text ran straight through a cell that was not empty. The occupancy test (cells.ts, pre-fix line 1014) was `if (adjCell && (adjCell.display ?? '') !== '') break;` -- a display test. Excel's condition is deliberately stricter and is worded to exclude exactly this case: the adjacent cell must be "absolutely empty -- does not contain spaces, non-printing characters, empty strings, etc." A cell holding ="" displays nothing and blocks the spill in Excel; in Calcula the long value painted over it, hiding the fact that a formula lives there. Not mentioned in §21c at all.

**Repro:** Put a long text value in a narrow column and ="" in the cell to its right. Pre-fix the text spilled across it; Excel clips at the cell edge. Pinned by 'BUG-0068: a neighbour holding ="" blocks the spill' in overflowRendering.test.ts, paired with a case proving a neighbour that carries ONLY a style still does not block -- the tightening must not go too far the other way.
**Triage:** product-bug (confidence 1) — CONFIRMED from source. 'Displays as nothing' and 'is empty' were treated as the same question; a formula cell that evaluates to the empty string separates them.
**Fix:** fixed — `blocksSpill` asks both questions: anything visible blocks, and so does a cell that displays nothing but carries a formula producing that nothing.
  Files: app/src/core/lib/gridRenderer/rendering/overflowMarker.ts (new -- blocksSpill), app/src/core/lib/gridRenderer/rendering/cells.ts (neighbour test calls blocksSpill), app/src/core/lib/gridRenderer/rendering/overflowRendering.test.ts (new)

## BUG-0069 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** live-route-probe

THE HOME > NUMBER DROPDOWN COULD NOT REPORT ITS OWN RESULT, AND WAS MISSING SIX OF EXCEL'S ELEVEN ENTRIES. Two defects in one control. (1) THE LIST: six entries -- General / Number / Thousands / Percentage / Scientific / Text -- of which 'Thousands' is not an Excel entry name at all, with NO Currency, NO Accounting, NO Short Date, NO Long Date, NO Time, NO Fraction and no 'More Number Formats...' row. Excel's list is eleven entries plus that row. (2) THE READ-BACK: the option VALUES were format codes ('0.00', '#,##0.00', '@') while get_style reports DISPLAY NAMES ('Number (2 decimals)'). Two disjoint vocabularies, so applying Number and re-reading the cell put the box on a greyed-out, unselectable 'Number (2 decimals)' row -- MEASURED live: the dropdown could not show the format it had just applied on any entry but General. (3) THE REGION: format codes cannot express five of Excel's eleven entries, because Excel writes Short Date / Long Date / Time as [$-x-sysdate] / [$-x-systime] handles and takes Currency / Accounting from the OS currency pattern. Shipping the missing entries as codes would have hard-coded 'm/d/yyyy' and '$' into a sv-SE product -- BUG-0064 a second time.

**Repro:** Live on sv-SE: type 1234,5678 in A1, pick 'Number' from the Home > Number dropdown -- the cell renders 1234,57 but the dropdown box shows a greyed 'Number (2 decimals)' that cannot be re-picked. Post-fix the box shows 'Number'; moving the selection away and back keeps it. Deterministic pins: every_ribbon_preset_resolves_to_a_typed_format, every_ribbon_preset_round_trips_through_the_display_name, regional_presets_follow_the_locale, non_regional_presets_are_locale_invariant, swedish_ribbon_presets_render_excel_shapes, us_ribbon_presets_render_excel_shapes, the_dropdown_is_excels_list_in_excels_order (app/src-tauri/src/commands/styles.rs); numberFormatDropdown.test.tsx (18 pins).
**Triage:** app-bug (confidence 1) — CONFIRMED from source and live. The dropdown spoke format codes; the style API speaks preset keywords on the way in and display names on the way out. The dropdown was the only surface that had never been given the preset vocabulary.
**Fix:** fixed
  Files: app/src-tauri/src/commands/styles.rs (RIBBON_NUMBER_FORMAT_PRESETS: Excel's eleven entries in Excel's order; parse_number_format_with_locale resolving currency/accounting/date_short/date_long/time/text against the locale; get_ribbon_number_formats command returning preset + displayName + sample), app/src-tauri/src/lib.rs (command registration), app/src/core/lib/tauri-api.ts + app/src/api/numberFormats.ts + app/src/api/lib.ts + app/src/api/index.ts (getRibbonNumberFormats seam), app/extensions/BuiltIn/HomeTab/components/numberFormatOptions.ts (labels, order mirror, selectedPresetFor), app/extensions/BuiltIn/HomeTab/components/HomeTabGroupComponent.tsx (the dropdown itself, More Number Formats..., Custom read-back)

## BUG-0070 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** unit-pin

FORMATTING A NUMERIC CELL AS TEXT MADE THE CELL GO BLANK. Excel's Text format is the one-section code '@', and the ribbon's Text entry sent exactly that. In the custom-format engine a section carrying the text placeholder and no digit placeholders had nothing for the numeric renderer to fill: apply_custom_format_number fell through to the number path and produced the EMPTY STRING, so the value vanished from the grid while still sitting in the cell. Excel shows the value's General rendering, left-aligned, and keeps it. One click from the ribbon, and the same for '@' typed into Format Cells > Custom.

**Repro:** Type 1234,5678 in A1, apply number format '@' (Home > Number > Text, or Format Cells > Custom). Pre-fix the cell renders as empty. Pins: text_format_on_a_number_shows_the_value_not_nothing, text_format_on_actual_text_is_unchanged (core/engine/src/custom_format.rs).
**Triage:** app-bug (confidence 1) — CONFIRMED from source. The `!has_digits && !has_text_placeholder` literals-only guard deliberately let the text-placeholder case through to a renderer that only knows digit placeholders.
**Fix:** fixed
  Files: core/engine/src/custom_format.rs (apply_custom_format_number: a text-placeholder section with no digit placeholders substitutes the value's General rendering for @, keeping surrounding literals)

## BUG-0071 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** unit-pin

SCIENTIFIC NOTATION HAD THE WRONG SHAPE IN BOTH ENGINES, AND IGNORED THE LOCALE. Excel's Scientific preset is the format code 0.00E+00: a signed exponent, zero-padded to the placeholder count, with the locale's decimal separator. (1) The TYPED path (NumberFormat::Scientific) handed the value to Rust's {:e}, which prints '1.23e3', and only upper-cased the e -- so 1234.5678 displayed as '1.23E3' where Excel shows '1,23E+03' on sv-SE, missing the sign, the zero padding AND the decimal comma. The engine's own test asserted '1.23E3', pinning the defect. (2) The CUSTOM path (a '0.00E+00' code) counted digit placeholders across the WHOLE section, so the exponent's two zeros were folded into the mantissa: 1234 rendered as '1.2340E+3' -- four mantissa decimals and a one-digit exponent, both wrong -- and its test asserted only that the output contained 'E+', which that string does. Two surfaces, two different wrong answers for one user intent.

**Repro:** sv-SE: 1234,5678 formatted Scientific rendered '1.23E3'; the same value under the custom code 0.00E+00 rendered '1.2340E+3'. Both now render '1,23E+03'. Pins: test_format_scientific, scientific_uses_the_locale_decimal_separator (core/engine/src/number_format.rs); test_scientific_notation, scientific_sections_split_mantissa_from_exponent_width, scientific_sections_localize_the_mantissa (core/engine/src/custom_format.rs).
**Triage:** app-bug (confidence 1) — CONFIRMED from source. Rust's {:e} is not Excel's E+00, and count_digit_placeholders has no notion of the E boundary.
**Fix:** fixed
  Files: core/engine/src/number_format.rs (render_scientific: one shared renderer for Excel's exponent rules -- sign, zero-padded width, locale decimal separator; format_scientific now takes the locale), core/engine/src/custom_format.rs (format_scientific_section splits the tokens at the E and delegates to render_scientific)

## BUG-0072 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** source-review

MONTH AND WEEKDAY NAMES WERE ENGLISH IN EVERY LOCALE, AND NO LONG-DATE OR TIME PATTERN EXISTED AT ALL. mmm / mmmm / mmmmm / ddd / dddd are LANGUAGE tokens -- Excel renders them in the system locale, so a Swedish machine shows 'den 15 januari 2024'. The custom-format engine resolved them through four English-only free functions with no locale parameter, so every format carrying a month or weekday name rendered in English on all eighteen supported locales. Separately, engine::LocaleSettings carried only a SHORT date pattern: Excel's Long Date and Time entries are the [$-x-sysdate] / [$-x-systime] handles, and neither pattern existed in the product in any language, so those two dropdown entries could only have shipped a hard-coded US pattern.

**Repro:** sv-SE: a cell formatted 'mmmm d, yyyy' on serial 45306 rendered 'January 15, 2024' instead of 'januari 15, 2024'. Live post-fix on CDP 9223: Home > Number > Long Date renders 'den 15 januari 2024'. Pins: calendar_names_are_localized_per_language, calendar_tables_are_complete_and_out_of_range_is_safe, every_supported_locale_carries_a_long_date_and_a_time_pattern, swedish_long_date_and_time_match_the_os_patterns, us_long_date_and_time_match_the_os_patterns (core/engine/src/locale.rs); month_and_day_names_follow_the_locale_not_the_format_string, swedish_long_date_renders_the_os_pattern, us_long_date_renders_the_os_pattern, one_letter_month_token_never_slices_mid_codepoint (core/engine/src/custom_format.rs).
**Triage:** app-bug (confidence 1) — CONFIRMED from source. format_datetime_section took no locale, so the names could not have been localized even if the tables existed.
**Fix:** fixed
  Files: core/engine/src/locale.rs (long_date_format + time_format on every one of the eighteen locale arms; CalendarNames tables for sixteen languages with English fallback; LocaleSettings::calendar()), core/engine/src/custom_format.rs (format_datetime_section takes the locale and resolves every name through it), core/engine/src/lib.rs, app/src-tauri/src/api_types.rs, app/src/api/locale.ts (LocaleSettings mirror: longDateFormat / timeFormat)

## BUG-0073 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** excel-parity-number-format-seam

THE RIBBON CAN NOW APPLY FORMATS THE FORMAT CELLS DIALOG CANNOT REPORT. MEASURED, not read: feeding the eleven display names `get_ribbon_number_formats` resolves on sv-SE through the dialog's own `normalizeToPresetValue` returns null for three of them -- `Date (YYYY-MM-DD)` (Short Date), `Date ("den "d mmmm yyyy)` (Long Date) and `Currency ( kr, 2 decimals)` (Currency). The dialog lands on the right CATEGORY (its prefix table is unaffected) and then highlights NOTHING, so a user who applied Short Date from the ribbon opens Format Cells and cannot see which format the cell carries. MECHANISM, and it is the same one twice: `DISPLAY_NAME_TO_PRESET` (app/extensions/BuiltIn/FormatCellsDialog/utils/numberFormats.ts) is a HAND-WRITTEN TypeScript mirror of a Rust serializer's output, and the §21c item-1 pass added display names it does not contain. `Date (YYYY-MM-DD)` misses on CASE alone (the locale table spells the pattern uppercase; the dialog's own `date_iso` preset spells the same pattern lowercase, and the engine's date formatter is case-insensitive, so these are the SAME format failing to compare equal). `Currency ( kr, 2 decimals)` misses on the sv-SE currency symbol's LEADING SPACE (" kr" vs "kr"). Long Date genuinely has no dialog row at all. This is the defect BUG-0065 and BUG-0069 were each an instance of -- a surface that cannot report its own result -- reappearing through a route the item-1 pass opened. That pass deliberately avoided the trap on the ribbon side by asking the backend for the mapping; the dialog was left inverting it in TypeScript. NOT DATA CORRUPTION, and the entry says so rather than inflating it: the store still holds the display name, Rust's `try_parse_display_name` round-trips all three losslessly, and pressing OK preserves the format. What is lost is the dialog's ability to TELL THE USER what is applied. It is also unbounded in the direction it was heading: hard-coding the three sv-SE names would leave the same hole open for the other 17 locale arms, each with its own date pattern and currency symbol.

**Repro:** On a sv-SE document: select a cell, pick Short Date (or Currency) from Home > Number, then open Format Cells > Number. The Date (or Currency) category is selected and no row is highlighted. Unit-level: normalizeToPresetValue('Date (YYYY-MM-DD)') === null while normalizeToPresetValue('Date (yyyy-mm-dd)') === 'date_iso' -- the same pattern.
**Triage:** product-bug (confidence 1) — CONFIRMED by measurement. A second source of truth for the backend's display-name vocabulary, living in TypeScript, drifting the first time the Rust side gains a name.
**Fix:** fixed — Fixed in the direction that closes the class, not the three instances. (1) The dialog now RESOLVES ribbon display names through the backend, exactly as the ribbon does: `NumberTab` fetches `getRibbonNumberFormats()` alongside the locale it already fetched, and `normalizeToPresetValue` takes that resolved list and consults it FIRST. No display name was hard-coded, so the other 17 locale arms are covered by construction rather than by a table someone has to remember to extend. (2) The five ribbon presets with no dialog row -- Currency, Accounting, Short Date, Long Date, Time -- are now ROWS in their categories, sourced from that same backend response (label from the preset, example from its `sample`). This is also the Excel shape: Excel's Format Cells Date list leads with the locale-responsive Short/Long Date entries, and its Currency list leads with the regional symbol. (3) The static `DISPLAY_NAME_TO_PRESET` map stays as the OFFLINE fallback for the dialog's own presets (date_iso/date_us/...), which the ribbon list does not contain, and its lookup was made case-insensitive so `Date (YYYY-MM-DD)` and `Date (yyyy-mm-dd)` -- the same pattern to a case-insensitive engine -- stop being two different formats to the dialog. A test spells the whole seam out: every one of the eleven display names the ribbon resolves must map to a preset the dialog can HIGHLIGHT, so the next entry added to either side without the other fails here rather than in a user's dialog.
  Files: app/extensions/BuiltIn/FormatCellsDialog/utils/numberFormats.ts, app/extensions/BuiltIn/FormatCellsDialog/tabs/NumberTab.tsx, app/extensions/BuiltIn/FormatCellsDialog/__tests__/ribbonFormatSeam.test.ts

## BUG-0074 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-golden-stability

THREE GOLDENS IN comments-notes.spec.ts GAVE THREE ANSWERS FROM ONE BUILD, AND THE VARIABLE IS A TABLE ANOTHER SPEC LEFT IN THE SHARED WORKBOOK -- NOT THE SELECTION. The filed cause (a repaint race between the selection and the annotation refresh) is REFUTED by measurement, and the refutation is the cheapest part to check: the frame carries the selection's own evidence. The capture parks the selection in the SAME COLUMN as the range (parkSelectionAwayFrom uses parkCol = min(a.col,b.col)), so the clip's top 4 px are the selected-column header highlight #E3ECF7. In the failing cold run those 4 rows are BYTE-IDENTICAL to the golden -- 284/284 pixels, 256 of them the highlight -- in both -cell-with-indicator goldens. The selection was in the same place in both pictures. Replaying the exact capture sequence 8 times in one session gave 8 byte-identical frames (sha 38c2aa112ed3). There is no race. WHAT THE PIXELS ACTUALLY SAY: every differing pixel is Excel table chrome. Expected #4472C4 -> actual white, 829 px; expected #D9E2F3 -> white, 264 px; plus 186 px where the golden's caption is WHITE BOLD on blue and the actual's is dark on white. #4472C4/#D9E2F3 exist in exactly one place in the tree: HEADER_BG and BAND_EVEN_BG in extensions/Table/lib/tableStyleInterceptor.ts. Bounding boxes confirm it -- the comments diff starts at y=5 (below the header band), and the notes diff is confined to x[0..3], the 4 px of padding that shows W1's right edge. THE SOURCE: tables.spec.ts's 'rename a table' builds a table over V1:W2 (startCol 21, endCol 22) -- inside comments-notes.spec.ts's declared W-X ground -- and never deletes it. Measured after one full cold functional run, get_all_tables returned SalesData r0-1 c21-22, Table1 r0-3 c17-19 and TotalsTest r6-9 c17-18: three tables, three tests, no cleanup. A table is neither cell state (clear_range_with_options cannot reach it) nor a floating object (the residue guard could not see it); its colours are repainted every frame from the DEFINITION by a style interceptor. WHY ONE BUILD GAVE THREE ANSWERS: 'tables' sorts AFTER 'comments-notes', so the residue can only reach the capture on the SECOND or later run against one app process. Reproduced exactly, on one build, one machine: cold ordered run -> all 3 fail (1363 / 52 / 1338 px); the file alone on an app that had just completed a full run -> 6 passed, 1 failed, which is the ledger's own observation (c) reproduced to the test; and `--update-snapshots` on a warm app wrote the contamination as truth. The third golden (indicators, W6) needs a table reaching row 5, which one full run does not produce -- so that file encodes a workbook MORE contaminated than the ordered suite can even reach.

**Repro:** cd app; launch a COLD app; E2E_MANUAL=1 npx playwright test --project=functional tests/comments-notes.spec.ts --reporter=./e2e/collectionGuard.ts,list -> 3 failed / 4 passed. Then run the whole --project=functional once (which runs tables.spec.ts) and repeat the single-file command WITHOUT restarting the app -> 6 passed / 1 failed. Same build, same machine, two answers. `get_all_tables` after the full run lists SalesData over V1:W2.
**Triage:** test-bug (confidence 1) — Cross-spec, cross-RUN residue: a table left in the shared workbook by a spec that sorts later, repainted by Table's style interceptor over another spec's photographed cells. Not a repaint race and not the selection.
**Fix:** fixed — Fixed at the cause, then defended, then guarded, then re-recorded. (1) CAUSE: tables.spec.ts's 'rename a table' moves from V1:W2 into its own R-T ground (R17:S18), and a test.afterAll now deletes EVERY table the file creates and clears its cells -- the charts.spec.ts shape, including the app:table-definitions-updated event without which the chrome keeps painting from the extension's own cache after the backend has forgotten the table. (2) DEFENCE: comments-notes.spec.ts reclaims its own ground in beforeEach (deletes any table overlapping W1:X10, clears formats there), so the golden is a function of the test even if a future spec squats. (3) GUARD, which is worth more than either: zz-workbook-residue.spec.ts gains 'no spec left a table in the backend'. Tables were the one residue class with no guard -- floating objects, charts and open panels each had one -- which is exactly why this was invisible for as long as it was. (4) The general half of the original filing was right and is fixed too: takeGridRegionScreenshot ran waitForGridStable BEFORE the park and then dispatched up to three navigate events, each followed by a FLAT sleep, so the last thing before the shutter was a sleep. Comparison mode retries and hides that; --update-snapshots takes one frame and writes it. It now stabilises AFTER the park, before the shutter. VERIFIED, and never by the run that wrote the goldens: recaptured on a cold app, then TWO further cold runs (fresh app.exe each, PID changed each time) both 7/7 with the golden bytes unchanged (04887c89f178 / 090d396df34b / c57d60ff6d69). The property that was broken -- repeat runs against one app -- was then measured directly: cold 7/7, warm 7/7, warm 7/7.
  Files: app/e2e/tests/tables.spec.ts, app/e2e/tests/comments-notes.spec.ts, app/e2e/tests/zz-workbook-residue.spec.ts, app/e2e/helpers/screenshots.ts, app/e2e/tests/__screenshots__/comments-notes.spec.ts/grid-comments-cell-with-indicator.png, app/e2e/tests/__screenshots__/comments-notes.spec.ts/grid-comments-indicators-visible.png, app/e2e/tests/__screenshots__/comments-notes.spec.ts/grid-notes-cell-with-indicator.png

## BUG-0075 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-golden-stability

A CHART FROM ONE SCENARIO IS IN FRAME IN ANOTHER SCENARIO'S GOLDEN. `scenario-budget-model-title` captures the budget-model scenario's title row; the verify runs photograph the 'Monthly Sales' bar chart from the monthly-report scenario sitting over columns E..H of the budget sheet. Measured: 173,986 differing pixels, glyph-dark pixels 728 -> 19,161, glyph centroid +208px. That is not the 2026-08-15 parity work -- the parity signature in this same file is a ONE-pixel glyph row shift plus the ribbon's lit toggle moving from x=427 to x=460, and both are present as well and account for a few thousand of those pixels, not 174,000. REPRODUCED TWICE on two consecutive clean runs, so it is not a one-off. THE PRE-PASS GOLDEN HAS NO CHART, so the contamination is not baked into the old baseline -- it appears in what the suite produces NOW. Same family as BUG-0074 and found the same way: `--update-snapshots` wrote a chart-free frame and the comparison runs then rejected it, so the recapture was not self-validating. The mechanism differs though, and that is why this is a separate entry: BUG-0074 is a repaint RACE inside one spec, this is an OBJECT that outlives the scenario that created it. `e2e/walker/reset.ts` tears objects down for walks (the BUG-0004/BUG-0035 discipline); the scenario harness evidently does not do the equivalent between scenarios.

**Repro:** cd app; cold app on 9222; E2E_MANUAL=1 npx playwright test --project=scenario --reporter=./e2e/collectionGuard.ts,dot  -> 'Scenario: budget-model > 06 title with merged cells and bold' fails; the -actual.png shows a 'Monthly Sales' chart the budget scenario never creates.
**Triage:** test-bug (confidence 1) — CONFIRMED BY MEASUREMENT, not by reading: a chart that the FRONTEND store never learned about survives the reset's teardown, and is then loaded INTO that store by the reset's own table teardown, 500ms before new_file empties the backend. The store is what paints. The product is clean on both halves of the same question and both were measured: new_file clears state.charts (live: get_charts 1 -> 0), and the product's File > New / File > Open both window.location.reload() after the backend switch, so no frontend store can survive a document boundary the product can take.
**Fix:** fixed — THE CAUSAL CHAIN, MEASURED END TO END ON 2026-08-15 with an in-page instrument (timestamps are from one run): +0ms backend=1 store=0 (the residue a raw save_chart leaves); +10251ms charts:refresh (the reset's TABLE teardown announces the objects domain -- ObjectKind::Table -> UiDomain::Objects -- and the Shell translator fans that out to charts:refresh); +10331ms backend=1 store=1 (the extension reloads its store from a backend that STILL HOLDS the outgoing document's chart); +10794ms backend=0 store=1 (new_file cleared the backend, nothing re-syncs the store). From there a chart with no document behind it paints over the next scenario and was photographed by scenario-budget-model-title. REPRODUCED AND EXTINGUISHED AT WILL: with both fixes removed the failure returns at EXACTLY 173,986 differing pixels -- the filed number, to the pixel -- and with them in place the run is green. 

THREE CHANGES. (1) ROOT CAUSE, monthly-report.scenario.ts: the chart is created through the product's chartStore.createChart (what Insert > Chart calls), not a raw save_chart invoke -- the same lesson the walker's chart.create learned as BUG-0031, one directory over. A new assertion pins that the backend and the store AGREE after the create, so the shortcut cannot come back silently. (2) DEFENCE, walker/reset.ts: the chart teardown now also enumerates the BACKEND's list (the store is not a census of the document), and -- the half that closes the CLASS rather than the trigger -- a new resyncChartStoreToBackend() runs AFTER new_file and ASSERTS the store reached zero, throwing a sentence that names the state if it cannot. Proved necessary and sufficient in isolation: with the backend enumeration still disabled and monthly-report still store-blind, the re-sync alone makes the run green. (3) TEETH, soak/walker-action-effects.spec.ts: a regression test that builds the precondition deliberately (a table, then a raw save_chart) and asserts both stores are empty after the reset. It fails when either half of the fix is removed. 

VERIFIED THE WAY THIS PROGRAMME REQUIRES -- by runs that did not write anything: scenario 24/24 on a cold app, 24/24 again on a second cold app, and 24/24 warm in between (the second consecutive run is the one that used to fail). The golden was NOT recaptured and needed no recapture: it is correct as committed, and the diff was the phantom chart, not a stale baseline. Also green after the change: soak/walker-action-effects 6/6, invariant 2/2, journeys/cascade-announcement-live 2/2, soak/synthetic-click-drag 3/3, tsc -p e2e, check:line-endings.
  Files: app/e2e/scenarios/monthly-report.scenario.ts, app/e2e/walker/reset.ts, app/e2e/soak/walker-action-effects.spec.ts

## BUG-0076 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** manual-live-pixel

FORMATTING A CELL THROUGH ANY ROUTE THAT DOES NOT HAND-DISPATCH `styles:refresh` PAINTS THE DOCUMENT DEFAULT, NOT THE CELL'S OWN STYLE. Ctrl+B on a red cell turned it WHITE and left it unbolded, while `get_style` reported bold + the colour. MEASURED LIVE, one app, one binary, one document, the fix stripped for one page only via `page.route` (another session was running a suite against the same Vite server, so the tree could not be reverted on disk): HEAD  before Ctrl+B styleIndex 47 bold=false, painted 2277 fill px / 784 white px; HEAD  after  Ctrl+B styleIndex 48 bold=TRUE,  painted 0 fill px / 3052 white px (of 3248) -- the cell reverted to the default style. FIXED before Ctrl+B styleIndex 48 bold=false, painted 2277 fill px / 784 white px; FIXED after  Ctrl+B styleIndex 49 bold=TRUE,  painted 2184 fill px / 784 white px -- the fill survives and the glyphs cover 93 px more, which is the bold. MECHANISM: the renderer resolves a cell with `getStyleFromCache(styleCache, cell.styleIndex)` (app/src/core/lib/gridRenderer/styles/styleUtils.ts:33), and that lookup FALLS BACK to index 0 -- the document default -- for an index the cache does not hold. `apply_formatting` MINTS an index (`get_or_create`, commands/styles.rs), and the cache is refreshed by exactly ONE event: `styles:refresh`. `grid:refresh` refetches CELLS only. So every route that refreshed cells without hand-dispatching `styles:refresh` painted the default. THE BACKEND WAS ALREADY HANDING OVER THE CURE AND IT WAS BEING THROWN AWAY: `FormattingResult.styles` is documented in api_types.rs as 'New or updated styles that the frontend should cache', collected from `used_style_indices`; the only frontend reference to it was `console.log(..., result.styles.length)`. PRODUCT ROUTES AFFECTED (not a probe artifact): (1) Core's own keyboard shortcuts -- Ctrl+B / Ctrl+I / Ctrl+U / Ctrl+5 and Ctrl+2/3/4, plus the seven Ctrl+Shift number-format shortcuts -- `useGridKeyboard` -> `handleCommand` -> `applyFormattingToSelection` (useSpreadsheetSelection.ts:1230), which calls `canvas.refreshCells()` + `redraw()` and announces nothing; (2) the script host's `applyRangeFormat` for the ACTIVE sheet (api/scriptHost/host.ts:7791) -- `afterCellDataChange` schedules `grid:refresh` only, while the OFF-sheet arm correctly emits the `styles` mutation domain; (3) MCP `format_range` (mcp/tools.rs:388) mints in Rust and emits only `grid:refresh`; (4) every direct-invoke route in the e2e harness (`setNumberFormatDirect`, alignment.spec, workflow-invoice.spec). The ribbon was correct only because `useHomeTabState.applyFormat` dispatches `styles:refresh` BY HAND, and the Reports extension had already hit the same wall and patched it at ITS call site with the comment 'the style cache must refetch too or the report renders unstyled'. WHY 106,931 UNIT TESTS AND 550 FUNCTIONAL TESTS MISSED IT: every formatting test reads the format back from the BACKEND (`get_cell` -> `get_style`), which was always right. The only assertions that could have seen it are pixels, and every visual formatting golden applies its format through the RIBBON. The walker drives Ctrl+B (actionCatalog.ts:824) but its oracles compare state, not paint. NOT VISIBLE FOR EVERY FORMAT, WHICH IS WHY IT SURVIVED CASUAL CHECKING: a format that changes only the DISPLAY STRING (a number format on a cell wide enough to fit) paints identically under the default style, because `display` comes from the backend. Measured: replaying core-visual.spec.ts's number-format block through the direct route and then repairing the cache changed 0 of 20,592 pixels. It becomes visible the moment the style decides how the cell is DRAWN -- fill, font, colour, alignment, borders, or the overflow ladder. §27i's own probe hit the ladder: an explicitly Number-formatted cell in a 60px column painted `1,23E+08` (General's scientific rung) instead of `########`. Re-measured after the fix on the same probe: `#####` vs `2E+08`, two different pictures, as Excel has it.

**Repro:** LIVE: launch an isolated app (own WEBVIEW2_USER_DATA_FOLDER, CDP 9223). Give A4 a background colour no other style has, force one `styles:refresh` so the cache holds it, then press Ctrl+B on the grid container (WebView2 swallows the real accelerator -- dispatch the keydown on [data-focus-container='spreadsheet'], as e2e/helpers/grid.ts does). Before the fix the cell turns WHITE and unbolded while get_style reports bold + the colour. A FRESH style matters: `get_or_create` dedupes, so re-applying a format the registry already holds returns an index the cache already has and the defect hides -- that is how an earlier attempt at this measurement came out green on the unfixed build. UNIT: `npx vitest run src/core/components/Spreadsheet/__tests__/formatStyleCachePrime.test.tsx`. Instruments (untracked, app/e2e/results/): style-repaint-ab.mjs (the A/B, which strips the fix for ONE page with `page.route` so a shared dev server is not disturbed), style-repaint-probe.mjs (§27i's own probe re-run), golden-impact-probe.mjs (the changed-pixels-vs-goldens measurement). HARNESS NOTE (open-decisions §28k): the FIRST run of this probe silently migrated onto ANOTHER session's app -- its own instance died and that session's pinned app took the freed CDP port, so `connectOverCDP` attached to theirs and the probe wrote cells and reloaded the page inside their document. The numbers above are from a re-run behind an OWNERSHIP GATE (app/e2e/results/cdp-owner.ps1: resolve the PID listening on the port, refuse anything whose --user-data-dir is not this probe's), checked at start AND end. A CDP port is not an identity.
**Triage:** product-bug (confidence 0.99) — NOT a hypothesis -- measured in both directions. One cache with one refresh event, and the refresh wired to CALL SITES instead of to the IPC wrapper. Identical in shape to the outline / hyperlink / validation / annotation defect this tree already fixed (see the header of app/src/api/__tests__/refreshAnnouncements.test.ts, 'a refresh event wired to one caller is how the ribbon path stayed correct while the script path silently left the cache -- and therefore the painted grid -- describing a document that no longer existed'). The lookup's silent fallback to index 0 is what makes the failure a plausible-looking wrong answer instead of a blank cell or a crash.
**Fix:** fixed — TWO mechanisms, because the class has two directions. (1) PRIME, at the one door: `applyFormatting` / `applyBorderPreset` in tauri-api emit `AppEvents.STYLE_ENTRIES_UPDATED` carrying `FormattingResult.styles`, and `useSpreadsheetStyles` merges the entries into the cache. Emitting from the WRAPPER cannot be routed around -- there is no other door to the command -- and it costs NO IPC, because the backend already returns exactly the indices it used or created. It also lands BEFORE the repaint, so no frame is ever painted with the wrong style (a heal alone would flash the default first, which a screenshot can photograph). (2) HEAL, for the mints that never touch a frontend wrapper (MCP, .calp pulls, backend recalculations, and the e2e direct-invoke helpers): on `grid:refresh` and on `sheet:normalSwitch` the cache compares its size against `get_style_count()` -- a scalar IPC beside the viewport cell fetch those events already pay for -- and re-reads the registry in full only when the two disagree. `!==` rather than `>`: a document replaced under the frontend can leave the registry SHORTER, and a cache holding entries the backend no longer has is just as wrong. Coalesced with an in-flight guard so a burst of refreshes costs one probe. PINNED by app/src/core/components/Spreadsheet/__tests__/formatStyleCachePrime.test.tsx (6 cases), which mounts the REAL `useSpreadsheetStyles` + `useSpreadsheetSelection` against the REAL tauri-api (only Tauri's `invoke` is doubled, by a fake registry that dedupes like `get_or_create`), drives a real Ctrl+B keydown on the grid container, and asserts the RENDERER's own question -- `getStyleFromCache(cache, cell.styleIndex).bold`. PROVED BY SABOTAGE IN BOTH DIRECTIONS: disabling the prime turns exactly the two prime cases red ('expected false to be true') and leaves the heal cases green; disabling the heal turns exactly the heal case red. Two of the six are cost assertions -- a format costs no `get_all_styles`, and a quiet `grid:refresh` costs the scalar probe and no full re-read.
  Files: app/src/core/lib/events.ts, app/src/core/lib/tauri-api.ts, app/src/core/components/Spreadsheet/useSpreadsheetStyles.ts, app/src/core/components/Spreadsheet/__tests__/formatStyleCachePrime.test.tsx

## BUG-0077 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-golden-stability

FIVE RESIDUE CLEANUPS -- INCLUDING THE SUITE'S OWN resetGrid -- HAD NEVER RUN, BECAUSE THEY SPELL AN ENUM VARIANT IN A CASING serde REJECTS. ClearApplyTo (app/src-tauri/src/api_types.rs) carries #[serde(rename_all = "camelCase")], so the wire vocabulary is all|contents|formats|hyperlinks|removeHyperlinks|resetContents. Measured live against the running backend on CDP 9223: "All" -> REJECTED `unknown variant`, "all" -> ACCEPTED; same for Formats/formats and Contents/contents. Every one of the five call sites wraps the invoke in .catch(() => {}) because 'the command may not exist', so the rejection was swallowed and the clear silently did not happen: e2e/helpers/screenshots.ts resetGrid (the suite's reset -- its used-range + formatting clear never ran, leaving Ctrl+A + Delete as the whole of it), and the test.afterAll cleanups in animation.spec.ts, charts.spec.ts, dimensions.spec.ts and edge-cases.spec.ts. Those four were written specifically to close the cross-spec residue class recorded as sections 3a/3b of the register -- so the register's own remediation for that class was, in fact, unenforced. The cost is measurable: on one cold app, one build, the ordered functional run went from 27 failures to 20 when the five were corrected, with 8 tests fixed (all 5 comments-notes, clipboard copy/paste, and both dimensions tests) and the two whole-grid evaluate-formula goldens revealed as photographs of edge-cases.spec.ts's uncleaned AE-column residue.

**Repro:** In the running app: invoke('clear_range_with_options', { params: { ..., applyTo: 'All' } }) rejects with `unknown variant \`All\``; the same call with 'all' resolves. Then grep the e2e tree for applyTo: "All".
**Triage:** test-bug (confidence 1) — A hand-written TypeScript spelling of a Rust serde vocabulary, with a catch-all around the call that turns a rejected command into silence.
**Fix:** fixed — All five call sites corrected to the accepted lower-case spelling, and the class closed rather than the five instances: e2e/__tests__/clearApplyToVocabulary.test.ts PARSES the variants out of the Rust enum, camelCases them the way serde does, and fails the build naming file and line for any applyTo literal in the e2e tree that the backend would reject. It was proved to fail before it was trusted (it flagged its own failure-message template on the first run, and then dimensions.spec.ts:48 when one site was deliberately reverted). CONSEQUENCE, attributed rather than absorbed: the two whole-grid evaluate-formula goldens changed, because they had been photographing residue their owner's afterAll was written to remove. Attribution is one-directional and exact -- 3,379 ink pixels present in the golden are absent now and ZERO new ink appears, all of it in the column band that resolves to AE after the spec's own navigateTo('AI1') scroll, which is edge-cases.spec.ts's declared AE1:AH200 ground. grid-evaluate-formula-init was already 112,174 px wrong on a cold app before this pass and is 2,498 px after it. Both were re-recorded in the ordered cold run and verified by two further cold runs.
  Files: app/e2e/helpers/screenshots.ts, app/e2e/tests/animation.spec.ts, app/e2e/tests/charts.spec.ts, app/e2e/tests/dimensions.spec.ts, app/e2e/tests/edge-cases.spec.ts, app/e2e/__tests__/clearApplyToVocabulary.test.ts, app/e2e/tests/__screenshots__/evaluate-formula.spec.ts/grid-evaluate-formula-init.png, app/e2e/tests/__screenshots__/evaluate-formula.spec.ts/grid-evaluate-formula-constant.png

## BUG-0078 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-golden-stability

THE APP UNDER TEST IS ON THE EDITOR'S HOT-RELOAD CHANNEL, AND A SOURCE SAVE RESETS THE GRID MID-RUN WITH NOTHING ABLE TO OBSERVE IT. The E2E app is a `tauri dev` build served by Vite, which pushes to whatever is connected. Measured 2026-08-15 while another agent saved app/src/core/lib/events.ts: Vite pushed `hmr update` for ~170 modules and `page reload (circular import invalidate)` for several; React fast-refresh remounted the provider tree, so GridProvider's useReducer restarted from getInitialState() and the selection snapped from row 6 col 22 back to A1 with scrollX 286 -> 0, about 2.5 s after the harness had parked it. THE PART THAT MAKES IT A BUG RATHER THAN AN ANNOYANCE: there is no navigation. performance.timeOrigin is unchanged and a window marker installed before the update survives it, so neither the page nor Playwright can tell the app was reset underneath a capture -- the resulting golden is a photograph of the editor and is indistinguishable from a product change. Cutting the channel made the same state hold still for 10 s and the same capture come back byte-identical 8 times out of 8. This is a standing hazard on a machine where more than one agent works, which is how this program is run.

**Repro:** With an E2E app running against the Vite dev server, park the selection (dispatch app:navigate-to-cell {row:6,col:22,select:true}) and poll __CALCULA_GRID_STATE__.selection while saving any file under app/src. The selection returns to 0,0 and viewport.scrollX to 0 with no framenavigated event and with window markers intact.
**Triage:** test-bug (confidence 1) — The E2E launchers start a normal dev server; nothing tells Vite that this instance is under test and must not be hot-reloaded.
**Fix:** fixed — Both launchers now set CALCULA_E2E=1 on the `tauri dev` child, and vite.config.ts resolves `server.hmr` to false under it -- so an E2E instance has no hot-reload channel while interactive development is untouched. Pinned by e2e/__tests__/hmrDisabledForE2E.test.ts, which checks BOTH halves (the config reads the flag and resolves to false; each launcher still sets it), because a guard that only checks one half is gone the first time the other drifts. Proved to fail before the change: the test reported 'vite.config.ts does not mention CALCULA_E2E' and 'global-setup.ts spawns tauri dev without CALCULA_E2E'. During diagnosis the same effect was obtained from the harness side by routing the websocket away, which is what made the 8-of-8 identical replay possible.
  Files: app/vite.config.ts, app/e2e/global-setup.ts, app/e2e/launch-app.mjs, app/e2e/__tests__/hmrDisabledForE2E.test.ts

## BUG-0079 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-golden-stability

comments-notes.spec.ts COULD NOT RUN TWICE AGAINST ONE APP, AND THE SECOND RUN'S FAILURES WERE NOT SCREENSHOTS. Measured after the BUG-0074 fix: a second run of the file on the same app process failed FOUR tests, none of them a pixel comparison. add_comment and add_note refuse a cell that already carries one, so expect(result.success).toBe(true) failed at W1, W2, W3 and X1 against the annotations the FIRST run left. Annotations are a residue class of their own: they are not cell state (clear_range_with_options does not touch them) and not floating objects (the residue guard cannot see them), so nothing in the suite has ever removed one. Same family as BUG-0074 -- a spec that creates state and does not remove it -- but the state is the file's OWN, which is why it shows up as a failed assertion rather than a changed picture.

**Repro:** cd app; cold app; run tests/comments-notes.spec.ts twice WITHOUT restarting the app. Before the fix the second run reports 4 failed / 3 passed, all on expect(result.success).toBe(true).
**Triage:** test-bug (confidence 1) — The spec creates comments and notes and never deletes them.
**Fix:** fixed — A test.afterAll deletes every comment and note in W1:X10 -- the ground the file's header claims -- and clears the cells, then announces app:annotations-changed so Review's indicator cache stops painting triangles for annotations the backend has already forgotten. DELIBERATELY NOT a beforeEach: the notes-cell-with-indicator golden clips X1 with 4 px of padding, and those pixels contain 15 px of the COMMENT triangle that test 1 puts on W1. That cross-test dependency is real and documented; wiping annotations between tests would have deleted it from the golden silently. The file's tests may depend on each other; the RUN may not depend on the previous run. Verified: cold 7/7, then warm 7/7 and warm 7/7 against the same app.
  Files: app/e2e/tests/comments-notes.spec.ts

## BUG-0080 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** manual-unit-pixel

A NAMED STYLE APPLIED THROUGH `@api` PAINTS THE DOCUMENT DEFAULT. BUG-0076 primed the frontend style cache from `FormattingResult.styles` at the `applyFormatting` / `applyBorderPreset` wrappers in `src/core/lib/tauri-api.ts`, and that function's own comment asserted there was 'no other door to the command'. True of `apply_formatting`; FALSE of the style cache. `apply_named_style` and `apply_named_style_range` return the SAME `FormattingResult` with the same `styles` array, their wrappers live in `src/api/backend.ts`, and both are exported through `@api` (`src/api/index.ts`, `src/api/lib.ts`) for extensions to call. Neither announced anything, so an extension that applies a named style mints a registry index the cache does not hold and `getStyleFromCache` falls back to index 0 -- the document default. MEASURED at the unit tier on the real hooks (`formatStyleCachePrime.test.tsx`, real `tauri-api`, fake backend with a real dedup registry): after `applyNamedStyleRange('Accent1', 0,0,0,0)` with no `grid:refresh`, the renderer's own lookup returned backgroundColor '#ffffff' while the backend reported '#00A650'. SCOPE, STATED HONESTLY: no LIVE reproduction was achieved. Every caller in the tree today happens to compensate -- the ScriptNotebook host hand-dispatches `styles:refresh` (a third local patch of this same class, after `useHomeTabState` and `Reports/lib/reportRefresh.ts`), and the script host's `afterCellDataChange` schedules `grid:refresh`, which fires BUG-0076's count-based heal. A staged live probe (`e2e/results/named-style-prime2.mjs`, three stages, with a primed-door control) found both doors painting the fill, because the heal had already covered the index. The defect is therefore reachable by any NEW `@api` caller and by no current one.

**Repro:** vitest run src/core/components/Spreadsheet/__tests__/formatStyleCachePrime.test.tsx -- the case 'a NAMED style primes the cache too'. Before the fix: AssertionError, expected '#ffffff' to be '#00A650'. The case deliberately dispatches no `grid:refresh`, because that event triggers the heal and would mask which DOOR announced.
**Triage:** product-bug (confidence 0.9) — The prime was attached to two of the four wrappers that receive a FormattingResult, and the comment recording that decision named the COMMAND rather than the style cache, so the remaining doors were never looked for.
**Fix:** fixed — `announceStyleEntries` is EXPORTED and both named-style wrappers in src/api/backend.ts call it, so every wrapper that receives a FormattingResult announces through one function. Its doc comment is corrected: the claim was about `apply_formatting`, not about the style cache, and the class is the DOOR rather than the command. Pinned by a new case in formatStyleCachePrime.test.tsx that models `apply_named_style_range` on named_styles_cmd.rs (rewrites the cells' style index wholesale, returns cells + styles) and asserts the renderer's own `getStyleFromCache` lookup. Red before (paints #ffffff), green after; 7/7.
  Files: app/src/core/lib/tauri-api.ts, app/src/api/backend.ts, app/src/core/components/Spreadsheet/__tests__/formatStyleCachePrime.test.tsx

## BUG-0081 `[fixed]`

**Found:** 2026-08-15 (manual, seed 20260811 and 20260815)
**Oracle:** e2e-oracle-coverage

THE INVARIANT PROJECT'S SAVE/RELOAD ORACLE COULD NOT RUN ON ANY SEED. `OracleBattery` fires the save/reload round-trip when `checkpointCount % saveReloadEvery === 0` and defaults `saveReloadEvery` to 4. `state-consistency.spec.ts` runs 75 actions at a cadence of 25 (3 checkpoints) and a rapid-fire walk of 50 at 25 (2 checkpoints). 3 % 4 and 2 % 4 are never 0, so the oracle had NEVER executed in that project -- not 'did not fire this run', but could not, by arithmetic, on any seed or any weighting. Every run printed its own '[WARNING] the save/reload round-trip never ran -- persistence was not exercised' and then reported '2 passed', so the warning read like bad luck. MEASURED on two cold runs before the fix (seeds 20260811 and 20260815): 'save/reload round-trip: 0 run(s)' on all four walks. This is the exact failure mode the collection guard exists for, one layer down: a suite reporting a clean pass for coverage it structurally did not have.

**Repro:** cd app; INVARIANT_SEED=20260811 npm run e2e:invariant. Before the fix the coverage line reads 'recalc consistency: 3 run(s); save/reload round-trip: 0 run(s)' on the main walk and '2 run(s) ... 0 run(s)' on the rapid-fire walk, and the run passes. After: '1 run(s)' on both.
**Triage:** test-bug (confidence 1) — The walk length, the checkpoint cadence and the oracle cadence live in three files and nothing had ever compared them. The default was chosen for the soak project (150/25 = 6 checkpoints, where it does fire) and inherited by a project whose walks are half that length.
**Fix:** fixed — Two halves, and the second is worth more than the first. (1) Both invariant walks now STATE their cadence -- SAVE_RELOAD_EVERY_MAIN=3, SAVE_RELOAD_EVERY_RAPID=2 -- so persistence is exercised exactly once per walk (more often would cost the undo oracle its baseline, which save/reload resets). (2) `WalkRunner.run` now REFUSES a walk whose cadence cannot come due: `plannedCheckpointCount` (an UPPER bound, so a wall-clock budget can only make it over-count) is compared against `saveReloadEvery` by `describeUnreachableSaveReloadCadence`, which names all three numbers and prescribes the three legal fixes -- including `saveReloadEvery: 0` to say 'this walk deliberately does not exercise persistence'. A guard that cannot run is not a guard that passed. Pinned by oracleCadenceReachable.test.ts (10 cases: the arithmetic, the boundary at exactly one checkpoint short, the 0-means-opt-out case, and the real spec's configuration READ OUT OF ITS SOURCE plus an assertion that both batteries state the cadence rather than inheriting the default). Sabotage: setting SAVE_RELOAD_EVERY_MAIN back to 4 turned exactly one case red with the measured sentence; restored byte-identical (sha256) and 10/10.
  Files: app/e2e/oracles/index.ts, app/e2e/walker/walkRunner.ts, app/e2e/tests/state-consistency.spec.ts, app/e2e/__tests__/oracleCadenceReachable.test.ts

## BUG-0082 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-harness-startup

A COLD E2E LAUNCH CAN COME UP WITH AN EMPTY #root, AND THE HARNESS REPORTS IT AS N PRODUCT FAILURES. Measured from the failing run's own trace: the page had loaded from Vite (frameUrl http://localhost:5173/, the full index.html DOM present, <title>app</title>) and `#root` was EMPTY -- the frontend module never executed, so React never mounted. The backend was alive and had logged its first three lines (AppState created, locale sv-SE detected). Every test then failed identically on `page.waitForSelector("[data-focus-container='spreadsheet']")` after 60s: 12 failed / 1 skipped, including specs with no dependence on the seed. The report contains twelve Playwright TimeoutErrors and NOTHING that says the application never mounted, so the run is indistinguishable at a glance from a product regression -- and the screenshot attachment is a blank white page. NOT REPRODUCIBLE: an immediate cold re-run of the same seed from a verified-zero process table passed 12/1 skipped. Two hypotheses were CHECKED AND ELIMINATED: (a) a leaked app.exe or WebView2 browser process from the previous run -- the process table was zero app.exe, and the 30 surviving msedgewebview2 processes were inspected and all belong to Windows SearchHost, not Calcula; (b) an HTTP failure fetching the module -- the trace's network log is empty because tracing starts after load, so this is UNRESOLVED rather than excluded. Vite's dependency re-optimisation on a cold start is the leading unproven candidate. FOURTH OBSERVATION, AND IT REFRAMES THE ENTRY (measured 2026-08-15 on the app left running for the next session, an ordinary cold launch): the page reached http://localhost:5173/ and then sat with #root EMPTY for approximately 55 SECONDS before React mounted, polled at 5s intervals -- 11 consecutive readings of {root:0, spreadsheet:false} followed by {root:1, spreadsheet:true}. The fixture ceiling is 60s. So the empty-#root state is NOT exotic: it is the ordinary cold-start condition while Vite transforms the module graph for the first time, and the three failures are that transform running just past a ceiling it normally clears by ~5 seconds. That also explains why every re-run passed (Vite's cache was warm) and why it struck the FIRST project run after an idle period. What remains unproven is whether the overrun is purely transform time or a stall.

**Repro:** REPRODUCIBLE, and the original 'not reproducible on demand' was measured wrongly: the re-runs that passed met a dev server that was ALREADY UP, and the cost is paid once per dev-server START, not per launch. To reproduce: kill every app.exe and whatever holds port 5173, launch `yarn tauri dev --config src-tauri/tauri.e2e.conf.json` with the Rust binary already built, and poll the page. 5 of 5 such launches came up with #root EMPTY for 16.8-28.4 seconds after reaching localhost:5173. Cheaper still, with no app at all: start `vite` alone and crawl the module graph over HTTP from /src/main.tsx at concurrency 8 -- 1,420 modules in 37 s cold against 0.4 s on a second traversal of the same server.

THE DOSE-RESPONSE IS THE TELL, and it is what to re-measure if this ever comes back: time from DEV-SERVER READY to mount is a CONSTANT ~38 s regardless of when the browser navigates. Vite idle 10.4 s -> #root empty 28.4 s; idle 22.1 s -> empty 16.8 s; idle 211.8 s -> empty 3.0 s. Idle + window = 38.0-39.5 s across eight launches. A cost triggered by the page could not behave that way.

Two signatures to check BEFORE blaming the product, unchanged and still good: (a) every test failing on the same waitForSelector with a blank-white failure screenshot, and a trace frame-snapshot whose <div id="root"> has no children; (b) window.__calcImport ABSENT, which dates the failure before main.tsx ran its body.
**Triage:** test-bug (confidence 0.99) — MEASURED 2026-08-15, no longer a hypothesis. Vite's dev-server file watcher is `chokidar.watch(root)` with root = `app/`, and `app/src-tauri/` holds an in-repo Cargo `target/` of 100,175 files in 9,118 directories. Chokidar registers a native fs.watch per entry AFTER `server.listen()` has printed "ready in 380 ms" and begun answering HTTP, and that registration blocks the dev server's ONE thread. So the server serves the static index.html instantly and then starves every /src/*.ts request behind the walk: the page sits at http://localhost:5173/ with <div id="root"> present and EMPTY and window.__calcImport absent, which is exactly the state the failing run's trace recorded. A CPU profile of the slow traversal put 74.5% of samples in one stack (_addToNodeFs -> _watchWithNodeFs -> createFsWatchInstance -> node:fs.watch -> native FSWatcher.start). NOT transform time, NOT a deadlock, and NOT the dependency optimizer, which logged "Hash is consistent. Skipping."
**Fix:** fixed — DIAGNOSIS LANDED; the rest is scoped and open. (1) DONE: describeUnmountedApp distinguishes three states -- #root empty ("a HARNESS/STARTUP failure, NOT a product failure; nothing after this is a test result"), #root populated ("the frontend DID mount; that is a product question") and unreadable ("UNKNOWN", because calling an unreadable page empty would be a claim). It dates the failure with window.__calcImport (installed early in main.tsx, so its absence means main.tsx never ran its body) and carries a console tail collected from the fixture onward, saying so explicitly when there is none. Five cases pin the wording, as appGoneMessage.test.ts does for the app-gone arm. This turns N identical waitForSelector timeouts -- 18 of them in the visual project, where it reads as the entire golden corpus breaking -- into one named cause. (2) OPEN, and now the LEADING fix given the ~55s measurement: raise the first-mount ceiling well clear of a cold Vite transform (the current 60s has ~5s of margin), and/or warm the dev server before the first fixture waits. Deliberately not done in this pass: it changes the timeout that bounds every project and deserves its own measured baseline of cold mount times rather than one sample. (3) OPEN: capture the Vite request log from global-setup so an overrun can be told from a stall -- Playwright's trace starts after load and its network log is empty. (4) A retry stays LAST: a retry that hides the state deletes the evidence (2) and (3) need.

=== ROOT CAUSE FOUND AND FIXED 2026-08-15 (second pass) ===
IT STILL REPRODUCED, and far more often than 'not reproducible on demand' suggested: 5 of 5 cold launches of the real app with the Rust binary already built came up with #root EMPTY for 26.1 / 26.3 / 27.9 / 28.4 / 16.8 SECONDS after the page reached localhost:5173. It is the ORDINARY cold-start condition, not an exotic one. The single launch that did NOT show it is the one where cargo recompiled for 3m20s first, giving the dev server 200 s of idle in which to finish its watcher walk before the WebView navigated -- mount then took 3.0 s. That contrast is the whole mechanism.

MEASURED against the dev server ALONE (no cargo, no app.exe, no WebView2, so CPU contention is excluded), crawling the 1,420-module graph over HTTP the way the browser does, order-controlled:
  watcher default        1,420 modules in 37.0 s / 37.4 s / 62 s / >120 s
                         (one run served THREE modules in 120 seconds)
  ignoring src-tauri     1,420 modules in  3.4 s /  3.3 s /  3.7 s
  chokidar watched       9,720 dirs / 113,460 entries  ->  545 / 3,443
  SECOND traversal       0.4-0.6 s in BOTH  <- the transform is never the cost, which is why every immediate re-run 'passed': it met a server that was already up.

The 37-120 s spread brackets the ~55 s fourth observation exactly, and shows the window is UNBOUNDED rather than a fixed ~55 s -- so raising the fixture ceiling was never going to be a fix, only a bigger number to overrun.

FIX: `server.watch.ignored` in app/vite.config.ts excludes **/src-tauri/**, e2e/results, the *-snapshots golden corpora, test-results and dist. Nothing there is a frontend module; src-tauri is Rust and Tauri's own dev watcher owns it. Pinned by e2e/__tests__/viteWatchExclusions.test.ts, which asserts on the RESOLVED config (so a mistyped key or an option placed outside `server` fails) and in BOTH directions -- the build tree excluded AND src/, extensions/, index.html still watched, so the exclusion cannot quietly grow until it breaks interactive HMR. Verified with teeth: deleting the watch block turns 3 of its 5 cases red.

STILL OPEN, deliberately: (a) the harness guard that fails the RUN instead of N product tests -- owned separately, and still wanted, because this fix makes the window small rather than impossible; (b) item (2) of the original plan, raising the first-mount ceiling, should be re-decided against the NEW baseline rather than the old one.

=== THE HARNESS GUARD LANDED 2026-08-15 (third pass, open-decisions §32) ===
The residual the root-cause pass left open -- "(a) the harness guard that fails the RUN instead of N product tests" -- is DONE. A BARRIER in global-setup (app/e2e/startupBarrier.ts) connects over the same CDP the fixtures use, reads the page once a second, and refuses the run before the first test, so a never-mounted launch now reports ZERO test results instead of 12 or 18 product-looking failures. The decision and every sentence live in app/e2e/startupGuard.ts, which imports nothing from Playwright and is unit-tested (33 cases on a virtual clock); the mid-run half writes a marker that the EXISTING collection-guard reporter reads in onEnd, so there is one first-position reporter, one assertCollectionGuardPresent handshake protecting both duties, and no second reporting channel. Escape hatch STARTUP_GUARD=off, loud. Six named failures: never-mounted-stalled, dev-server-not-answering, never-mounted-cap, no-page, wrong-origin, no-tauri-bridge (the documented tauri.e2e.conf.json symptom, which fires in 0.0s).
THE BOUND IS NOT A MOUNT DEADLINE, deliberately. It is a quiet window whose budget is chosen by document.readyState: 45s when the document is COMPLETE and the resource count is frozen (nothing outstanding, so nothing is coming -- BUG-0082's exact signature), 300s while the document is still LOADING (the page is blamed on the dev server, not the frontend), and a 900s absolute cap as an anti-hang backstop. That split is measured: on the pre-watcher-fix tree the page went 218 SECONDS with nothing changing -- two resources at 12.4s, the next at 230.9s -- while blocked on one /src/main.tsx request, and a stall window over network activity alone would have failed that healthy-if-pathological launch at 57s. readyState was "interactive" throughout that silence and "complete" in the dead state.
PROVED BOTH WAYS, LIVE: --project=visual (the project this bug turned into "18 of 18 failed") against a genuinely dead launch -> exit 1, ZERO tests reported, one named failure, zero test lines in the log; the same project against a healthy app -> "[startup-guard] the frontend mounted after 0.0s" and 18/18 passed, exit 0. Also fired at 45.5s against a controlled never-mounting stub and at 0.0s against a stub with window.__TAURI__ undefined. Sabotage-checked: const stalled=false fails exactly the firing case, const stalled=true fails the four patience cases, and removing one assertAppMounted call or flipping the fixture's <=0 fails the wiring pins.
FOUND WHILE MEASURING, AND STILL UNEXPLAINED: with app/node_modules/.vite deleted on the pre-fix tree, the page reached readyState complete with 250 resources loaded and #root empty, and stayed that way for TEN MINUTES. A reload with console listeners attached caught "Warning: Invalid hook call" plus "TypeError: Cannot read properties of null (reading 'useReducer')" in <GridProvider> -- a SECOND React instance, i.e. a mid-flight dependency re-optimisation whose full-reload could not be delivered because CALCULA_E2E=1 disables HMR. That is a different failure from the starved watcher: it makes an ARRIVED page render nothing, and waiting never recovers it. The same recipe after the watcher fix mounted in 7.8s, so it may need a long serve window to reproduce. Repro recipe and console signature are in the guard's own failure message.

=== CLOSED 2026-08-15 (integration pass, open-decisions §34) ===
Both items this entry left open are now resolved, and the entry is CLOSED.
(a) 'the harness guard that fails the RUN instead of N product tests' -- DONE in the third pass and re-verified here on the real product, twice, from a cold auto-launch: a launch that cannot mount gives exit 1 with ZERO test lines in the log and one named failure, and a healthy launch gives '[startup-guard] the frontend mounted after 4.1s' with 18/18 passed and exit 0. The two live outcomes come from the SAME command on the SAME project, so the sabotage is demonstrably load-bearing rather than a no-op.
(b) 'raising the first-mount ceiling should be re-decided against the NEW baseline' -- DECIDED: no change. Re-measured after the watcher fix, cold mount is now 2.0-4.1 s (four cold auto-launches this pass: 4.1 s, 2.2 s, 2.0 s, 2.0 s) against a 60 s fixture ceiling. The ~55 s sample that made a raise look urgent was a symptom of the starved watcher, and the margin is now ~15x. Raising a ceiling to accommodate a defect that no longer exists would only delay the next real detection.
REPRODUCTION EVIDENCE, which is the honest answer to 'is it gone': across this pass every cold launch that was supposed to mount, mounted -- and none of them approached the old window. The only unmounted launches were the two deliberately sabotaged ones. 
NOT CLOSED BY THIS ENTRY, and tracked separately: the second mechanism recorded above (a duplicated React from a mid-flight Vite dep re-optimisation, which makes an ARRIVED page render nothing) was NOT reproduced in this pass and remains unexplained. It is a different failure from the starved watcher, the guard names it in its own message, and it is called out in §32c rather than being quietly folded into this fix.
  Files: app/e2e/fixtures.ts, app/e2e/__tests__/unmountedAppMessage.test.ts, app/vite.config.ts, app/e2e/__tests__/viteWatchExclusions.test.ts, app/e2e/startupGuard.ts, app/e2e/startupBarrier.ts, app/e2e/global-setup.ts, app/e2e/collectionGuard.ts, app/e2e/__tests__/startupGuard.test.ts, app/e2e/__tests__/startupBarrierWired.test.ts, docs/design/open-decisions-2026-08.md

## BUG-0083 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** product-blank-window

THE FRONTEND HAS NO ERROR BOUNDARY ANYWHERE, SO A RENDER-TIME EXCEPTION SHOWS THE USER A BLANK WINDOW WITH NO MESSAGE. Measured by exhaustive search: zero matches for componentDidCatch / getDerivedStateFromError / ErrorBoundary / react-error-boundary across all of app/src and app/extensions. There are FIVE React roots (main.tsx plus chartSpecEditorMain, objectScriptMain, modelEditorMain, packageInspectorMain), and all five HTML shells are `<div id="root"></div>` and nothing else -- no fallback content, no <noscript>. React 18 unmounts the entire tree when a render throws with no boundary above it, so the window goes white and stays white. There is also no window.onerror or unhandledrejection handler at app level (the only two matches are inside the script-host worker sandbox). IN A BROWSER THIS IS MERELY BAD; UNDER TAURI IT IS WORSE: a production build has no devtools and no address bar, so a blank window is indistinguishable from a hung app, and the two things the user could usefully do -- reload, or report what happened -- are the two things nothing tells them are possible. This is the PRODUCTION half of BUG-0082's question. BUG-0082's own mechanism is dev-server-specific and has no production analogue (a bundled build serves static hashed assets over Tauri's asset protocol -- no module graph, no transform, no watcher), but 'the frontend silently fails to mount' is reachable in production by any throw during boot: a module-level throw in an eagerly imported module, the non-null assertion on getElementById("root"), a corrupt or CSP-blocked bundle, or any component throwing on first render.

**Repro:** Make any component in the initial tree throw (e.g. `throw new Error('x')` at the top of App's render) and start the app. Before the fix: a white window, forever, with nothing on screen. After the fix: a named failure panel with the error, the component stack, a Reload button and a Copy details button.
**Triage:** product-bug (confidence 1) — No root error boundary was ever added; the HTML shells carry no fallback content either, so there are two independent routes to a blank window and neither had anything in it.
**Fix:** fixed — A RootErrorBoundary now sits above every one of the five React roots, OUTSIDE StrictMode and above every provider (a boundary nested under the thing that threw catches nothing). It renders a named failure panel -- which window failed, the error, the stack, the component stack -- with Reload and Copy details.

IT IMPORTS REACT AND NOTHING ELSE, on purpose: no styled-components, no theme, no store, no @api. Every one of those is a plausible CAUSE of the error being caught, and a fallback that renders through the broken subsystem is not a fallback. All styling is inline literals.

Two test tiers. Behaviour: a throwing child leaves the container NON-empty and names both the surface and the message; the injected reporting hook is called; a reporting hook that ITSELF throws does not replace the original failure with its own. Census: the entry points are ENUMERATED FROM DISK (every app/src/*.tsx containing createRoot) and each must wrap its root and name its surface -- because the fix is one wrapper element per file and a sixth window added later would otherwise ship blank with every test still green. The census has a non-vacuity case (it must find at least 5) and was verified with teeth: unwrapping packageInspectorMain turns it red.

SCOPED, NOT DONE -- the OTHER route to a blank window. A React boundary cannot catch a failure BEFORE React runs: a module-level throw, or a bundle that never loads, leaves #root empty with the boundary never constructed. The bounded fix is fallback content in the five HTML shells plus a boot watchdog that reveals it if #root is still empty after N seconds. It is NOT done here because it collides with the E2E startup guard landing in the same pass: that guard reads `#root.childElementCount > 0` as proof that the frontend mounted, and a watchdog that injects children into #root would make it report a startup failure as a product question. Doing it needs a marker both sides agree on (e.g. `data-calc-boot-failed` on #root) and should be settled with the guard's owner.

Also NOT done and deliberately so: window.onerror / unhandledrejection reporting. Those do not blank the window, which is the failure this entry is about.

PENDING: proof in the live app. The boundary is proven under jsdom (11 cases). It has not yet been shown catching a real throw in a running Tauri window, because the E2E app and port 5173 were held by a concurrent pass throughout.

=== LIVE PROOF OBTAINED 2026-08-15 (integration pass, open-decisions §33c) ===
The PENDING item -- 'it has not yet been shown catching a real throw in a running Tauri window, because the E2E app and port 5173 were held by a concurrent pass throughout' -- is DONE, and the status moves from fixed-pending-live-proof to fixed.
A deliberate `throw new Error(...)` at the top of App's render, launched cold as an ordinary E2E auto-launch: the boundary caught it and rendered the named panel, and the harness read its text back verbatim -- 'Calcula could not start', 'error: Error: SABOTAGE-0084: deliberate boot-time throw', component stack 'at App (http://localhost:5173/src/shell/App.tsx:8:9)' / 'at RootErrorBoundary (.../RootErrorBoundary.tsx:69:8)'. Before this fix that window was blank and silent. Sabotage reverted; App.tsx verified byte-identical to its backup afterwards.
ALSO CORRECTED HERE: this pass's report claimed a section had been appended to docs/design/open-decisions-2026-08.md. It had NOT -- the register contained zero occurrences of 'BUG-0083' and zero of 'RootErrorBoundary', because two agents appended to the same 1.2 MB file concurrently and one whole-file write landed on top of the other. Every CODE change survived; only the register section was lost. It has been reconstructed as §33 from the surviving artifacts plus this pass's own live verification, and the concurrency hazard is recorded there as a standing lesson.
STILL OPEN, unchanged and still deliberate: the other route to a blank window (a module-level throw, or a bundle that never loads, leaves #root empty with the boundary never constructed). §34 turns the collision that deferred it into a measured fact rather than a prediction, so the marker both sides need is now well defined.
  Files: app/src/shell/RootErrorBoundary/RootErrorBoundary.tsx, app/src/shell/RootErrorBoundary/index.ts, app/src/shell/RootErrorBoundary/__tests__/RootErrorBoundary.test.tsx, app/src/main.tsx, app/src/chartSpecEditorMain.tsx, app/src/objectScriptMain.tsx, app/src/modelEditorMain.tsx, app/src/packageInspectorMain.tsx, docs/design/open-decisions-2026-08.md

## BUG-0084 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** e2e-harness-startup

THE STARTUP GUARD READ A CRASHED BOOT AS A HEALTHY MOUNT, so BUG-0082's misreporting came back through BUG-0083's front door. The guard's ONLY mount signal was `#root.childElementCount > 0`, chosen when the only two states were 'React rendered the app' and 'React never ran'. BUG-0083 then added a `RootErrorBoundary` above all five React roots whose failure panel is ITSELF a child of `#root`. So after both fixes, an app that throws during first render satisfies the mount check: the barrier prints 'the frontend mounted after Xs', lets the run proceed, and every spec then times out on `[data-focus-container='spreadsheet']` -- N product-looking failures for ONE systemic fact, which is precisely the reporting failure BUG-0082 exists to delete. The same signal is read a second time in `e2e/fixtures.ts` for the mid-run case, where it was worse than useless: `rootChildCount > 0` selected the arm that says 'the frontend DID mount and the spreadsheet container specifically is missing -- that is a product question, not a startup one', sending the reader to hunt a missing container while the page was displaying the stack trace of the actual throw. Neither report claimed this was handled; BUG-0083's own fix note flagged the collision ('#root.childElementCount > 0 has a second meaning now') and left it to the guard's owner, and the guard shipped before that note existed. It was nobody's defect and therefore nobody's fix.

**Repro:** Make any component in the initial tree throw (BUG-0083's own repro), then run any project. BEFORE: `[startup-guard] the frontend mounted after 0.4s`, the run proceeds, and every spec fails on the spreadsheet selector -- 18 of 18 in `visual`, reading as a total product collapse. AFTER: the run ends in global-setup with kind `boot-error`, ZERO tests reported, and the panel's own text (surface, error, both stacks) printed in the failure message.

Unit-level: `waitForMount` over a probe with `rootChildCount: 1, bootErrorText: 'boom'` must return `{ok: false, kind: 'boot-error'}`, not `{ok: true}`.
**Triage:** test-bug (confidence 1) — MEASURED, not hypothesised. Two correct fixes, built in parallel, sharing one signal that only one of them knew the meaning of. `#root` having children meant 'the app is up' until the moment a boundary was allowed to render into `#root`, and nothing re-derived the meaning because neither change looked like it touched the other. The general shape is worth keeping: a boolean whose definition is owned by one module and whose truth conditions are set by another.
**Fix:** fixed — `StartupProbe` gains `bootErrorText: string | null`, read from `[data-testid='root-error-boundary']` -- the marker BUG-0083's panel already carried, so no product change was needed. A new `MountFailureKind` of `boot-error` is checked INSIDE the mounted branch of `waitForMount` and BEFORE the Tauri-bridge check, because a crash early enough to precede the bridge would otherwise be reported as a launcher misconfiguration and send the reader to the wrong file.

THE VERDICT SENTENCE IS NO LONGER SHARED, and that is the substance of the fix rather than a detail of it. Every other arm ends 'this is a HARNESS/STARTUP failure, NOT a product failure -- re-launch and re-run'. For `boot-error` both halves of that are false: it IS the product, and re-running cannot help a deterministic boot throw. Printing the standard footer over a real crash would have exonerated the code that broke -- the same lie as BUG-0082's, pointed the other way. The arm instead says 'this IS a product failure, but it is ONE failure, reported once' and prints what the boundary reported.

`fixtures.ts` gets the matching fourth arm for the mid-run case, and -- the part that actually matters there -- is now EXCLUDED from writing the startup marker when a boundary is on screen, because that marker triggers the banner asserting the failures are not product failures.

Teeth verified by three sabotages, each producing a targeted red and no other: disabling the boot-error branch failed exactly the two barrier cases; collapsing the verdict split failed exactly the attribution case; disabling the fixtures arm failed exactly the two message cases. Guarded further by source pins in `startupBarrierWired.test.ts` (the barrier must probe for the boundary; its unreadable-page fallback must report `null`, never `""`, which would claim a boundary that reported nothing).
  Files: app/e2e/startupGuard.ts, app/e2e/startupBarrier.ts, app/e2e/fixtures.ts, app/e2e/__tests__/startupGuard.test.ts, app/e2e/__tests__/startupBarrierWired.test.ts, app/e2e/__tests__/unmountedAppMessage.test.ts

## BUG-0085 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** save-reload-roundtrip

THE .XLSX EXPORT WRITES DOCUMENT PROPERTIES AND THE IMPORT THREW THEM AWAY. core/persistence/src/xlsx_writer.rs fills rust_xlsxwriter's DocProperties from Workbook::properties on every save (title, author, subject, description, keywords, category, plus a dcterms:created stamp), and core/persistence/src/xlsx_reader.rs built its Workbook with `properties: crate::WorkbookProperties::default()` -- it never opened docProps/core.xml at all. A one-way loss THROUGH THE APP'S OWN EXPORT: save as .xlsx, reopen the file Calcula itself just wrote, and every document property is blank, with nothing anywhere saying it was dropped. Not a fidelity preference against a foreign producer -- the bytes are present in the file and were written by this code.

**Repro:** Unit: build a Workbook with non-empty `properties`, `save_xlsx` it, `load_xlsx` it back, read `properties`. BEFORE: every field is the empty string. AFTER: every field round-trips. Pinned by `xlsx_writer::tests::document_properties_survive_the_xlsx_round_trip`, which fails with `left: ""` / `right: "Quarterly Report"` when the reader wiring is reverted (verified by sabotage, 2026-08-15).

Product route: File > Save As > .xlsx on a workbook with document properties set, then File > Open on that same .xlsx -- the properties are gone.
**Fix:** fixed — docProps/core.xml is parsed with quick-xml by LOCAL NAME (dc:title, dc:subject, dc:creator -> author, cp:keywords, dc:description -> description, cp:category, dcterms:created/modified), never by namespace prefix, so a producer that binds the same namespaces to different prefixes is still read. A missing or unreadable part leaves every field the Default empty string and never fails the load: document metadata is advisory and must not cost a user their cells. The element names are taken from what the writer (rust_xlsxwriter/src/core.rs) actually emits rather than from the Calcula field names, because the two disagree in two places (author <- dc:creator, description <- dc:description via set_comment).
  Files: core/persistence/src/xlsx_style_reader.rs (XlsxStyleData.properties + parse_doc_properties, called from parse_xlsx_styles), core/persistence/src/xlsx_reader.rs (Workbook.properties comes off the parsed style data instead of Default), core/persistence/src/xlsx_writer.rs (two tests)

## BUG-0087 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** startup-mount

THE DEV SERVER SERVES MORE THAN ONE COPY OF REACT TO ONE PAGE, AND UNDER E2E IT CAN NEVER REPAIR IT. Vite pre-bundles dependencies into node_modules/.vite/deps_temp_<hash>/ and RENAMES that directory onto deps/. Measured on this tree by clearing the cache and running the optimiser in a loop: 3 of 10 runs FAILED with `EBUSY: resource busy or locked, rename '...\.vite\deps_temp_78efe380' -> '...\.vite\deps'`, each leaving a deps_temp_* behind. A failed rename leaves no usable deps/, so the server discovers dependencies request-by-request WHILE THE PAGE IS LOADING and serves /node_modules/.vite/deps/react.js?v=<hash> under more than one hash. Different URL = different module instance, so hooks are registered against one React dispatcher and read through another: `Warning: Invalid hook call` + `TypeError: Cannot read properties of null (reading 'useReducer')` in <GridProvider> -- the exact console lines §32 recorded and could not explain. Vite's own repair is a `full-reload` over the HMR channel, and CALCULA_E2E=1 disables HMR deliberately (a stray fast-refresh resets GridProvider's useReducer mid-capture), so the page stays dead at readyState=complete with an empty #root indefinitely, and the harness reports it as N product failures. Measured rate: 4 of 11 cold-cache launches served react.js under FOUR hashes in a single page load. Contributing cause on this machine: app/node_modules sits inside the Dropbox-synced tree and, unlike app/src-tauri/target and core/target, carried NO com.dropbox.ignored marker, so Dropbox held handles on the freshly written bundles -- 3/10 failures with it synced, 1/12 with it ignored.

**Repro:** rm -rf app/node_modules/.vite/deps*, start the dev server with CALCULA_E2E=1, then crawl the module graph over HTTP exactly as the browser does and count the distinct `?v=` hashes per dep (no browser needed -- app/e2e/depOptimizer.ts crawlModuleGraph does this). BEFORE: 4 of 11 launches produced 2-4 hashes for react.js, 18-30 refused requests, and the server log showing two `optimized dependencies changed. reloading` lines nobody could receive. AFTER (with scripts/ensure-dep-cache.mjs run first): 0 of 8 launches duplicated anything; two of the eight hit the EBUSY and the retry cleared it.

Faster reproduction of the underlying event: clear the cache and run `npx vite optimize --force` in a loop -- 3 of 10 fail with EBUSY on the rename.
**Fix:** fixed — PREVENTION plus DETECTION, because prevention can be skipped. Prevention: ensureDepCache() reads the cache off disk, deletes the deps_temp_* a failed rename leaves behind, and runs `vite optimize --force` with bounded retries until deps/ holds its bundles AND a parseable _metadata.json -- exit status alone is not the answer, since the measured failures exited non-zero AND left an empty directory. It runs from predev, i.e. before a dev server exists, so every discovery round happens while nothing depends on the result. It NEVER fails the command: a machine where the rename can never succeed still gets its run, and gets the EBUSY printed verbatim. Detection: the startup probe reports every /node_modules/.vite/deps/*?v=* URL from performance.getEntriesByType('resource'), and the barrier fails the run by name -- immediately, not after the 45s stall window, because two copies of React are terminal -- printing the duplicated deps and the one-command remedy. It fires even when the page APPEARS to have mounted, since every spec after that would be running against an incoherent module graph.

NOT a retry of the thing that failed (BUG-0082's fix note keeps retries last): nothing is re-run and no evidence is discarded, a DIFFERENT step is completed first, before the step that would fail is asked to start.
  Files: app/scripts/ensure-dep-cache.mjs (NEW: complete the optimisation before any server exists, retrying the transient EBUSY; wired as npm predev, which Tauri's beforeDevCommand runs, so it covers interactive dev, the E2E auto-launch and the manual launcher from one place), app/scripts/dropbox-ignore.mjs (NEW: mark the long-lived build trees com.dropbox.ignored; app/node_modules was the one that was missing), app/package.json (predev/predev:data, dropbox:ignore, dropbox:check), app/e2e/depOptimizer.ts (NEW: parseDepVersion / collectDuplicateDepVersions / crawlModuleGraph + re-export of the repair), app/e2e/pageState.ts (probe reports depUrls from the page's own resource timings), app/e2e/startupGuard.ts (new MountFailureKind `duplicate-deps`, decided FIRST and without waiting out a stall window), app/e2e/global-setup.ts (note recording why the repair is NOT run from here), app/e2e/__tests__/depOptimizer.test.ts, app/e2e/__tests__/depCacheWired.test.ts (NEW)

## BUG-0088 `[fixed]`

**Found:** 2026-08-15 (manual)
**Oracle:** harness-integrity

THE ENTIRE E2E HARNESS WAS OUTSIDE THE TYPE GATE. `npm run check-types` compiled tsconfig.check.json -- src/, extensions/, scripts/ -- and NOT ONE of the 212 TypeScript files under app/e2e/. Playwright and vitest both transpile with esbuild, which strips types WITHOUT checking them, so a type error anywhere in the harness (fixtures, helpers, the collection guard, the app-died marker, the startup barrier) was invisible until it threw at runtime -- inside a run whose entire purpose is to say whether the PRODUCT is broken. The same shape as every other defect this programme has paid for: an instrument that can stop working and say nothing.

**Repro:** `cd app && npx tsc --noEmit -p tsconfig.check.json --listFiles | grep -c e2e/` -> 0. Write `const x: number = "nope";` into any file under app/e2e/ and run `npm run check-types`: BEFORE it exits 0. AFTER it fails with TS2322 (verified by doing exactly that, 2026-08-15).
**Fix:** fixed — A SEPARATE project rather than an addition to the existing one, because the two need different `types`: merging them would put Node globals into the app's program, which is exactly how a `process.env` read type-checks its way into the WebView bundle. The whole harness was already clean -- 197 files, zero errors -- so this is a gate that costs nothing to keep and would have caught the type error the pass that filed it introduced.
  Files: app/tsconfig.e2e.json (NEW: a Node-typed project covering e2e/**, with noUnusedLocals/Parameters off because fixture signatures legitimately carry unused parameters), app/package.json (check-types runs both projects), app/e2e/__tests__/e2eIsTypeChecked.test.ts (NEW: pins the wiring, and that the app project has not gained Node types)

## BUG-0089 `[fixed]`

**Found:** 2026-08-16 (invariant)
**Oracle:** undo-round-trip

THE UNDO ROUND-TRIP ORACLE COULD NOT DECIDE. It captured its baseline at the start of a 25-action window and only asked at the END of the window whether that baseline was still reachable -- so ONE history-ending action anywhere in the window forfeited all 25 actions' worth of evidence. Measured live with the fix switched off (12 walks, seeds 20260815001-007, `invariant` project): 5 of 28 checkpoints decided, and 6 of 11 completed walks decided ZERO, while every one of them reported '[OK] Walk passed'. Undo is where this programme found a sheet's whole cell map replaced by another sheet's snapshot, an undo stack outliving its document across File > Open, and undo recalculating no dependents at all -- and the instrument aimed at that surface was structurally unable to fire over it.

**Repro:** Set AB_NO_UNDO_REBASE=1 (the A/B switch used to measure this, since removed) and run `npx playwright test --project=invariant` on seeds 20260815004 and 20260815006: both walks report 'undo round-trip: 0 decided ... 3 undecided' and, before the meta-assertion landed, still printed '[OK] Walk passed'. The generator-level measurement is in app/e2e/__tests__/undoOracleDecidability.test.ts: 6 of 120 windows in the invariant project's own shape contain no history-ending action.
**Fix:** fixed — The walker re-captures the undo baseline the MOMENT it becomes unreachable, measured after every action from `get_undo_state` plus the snapshot's floating-range ids, instead of discovering 25 actions later that the window died at action 2. The verdict is not relaxed: the baseline digest is captured at the rebase point and the round trip pops exactly the ids above it, so a decided window is a genuinely replayable one. The action that forced each rebase is recorded and checked against the set of actions ENTITLED to end the history, because a rebase would otherwise absorb 'an ordinary cell edit cleared the undo stack' in silence. And a walk that decides zero round-trips now FAILS with `undo-evidence-missing` rather than printing a warning under '[OK] Walk passed'.
  Files: app/e2e/oracles/undoRoundTrip.ts (undoBaselineUnreachableReason + ACTIONS_THAT_MAY_END_UNDO_HISTORY), app/e2e/oracles/index.ts (OracleBattery.rebaseUndoBaselineIfUnreachable, rebase census, undoEvidenceFailure, coverage line), app/e2e/walker/walkRunner.ts (per-action rebase; undo-evidence-missing verdict on the pass path), app/e2e/__tests__/undoOracleDecidability.test.ts (NEW), app/e2e/__tests__/undoHistoryHorizon.test.ts, oracleCoverage.test.ts (extended), app/e2e/tests/state-consistency.spec.ts, app/e2e/soak/soak-walk.spec.ts, app/e2e/soak/replay-trace.spec.ts (requireUndoEvidence: false on the trace-replay paths only)

## BUG-0090 `[fixed]`

**Found:** 2026-08-16 (invariant, seed 20260815001)
**Oracle:** undo-round-trip

Undoing 22 steps did not restore the checkpoint state. 1 differences; first: activeSheet: 2 -> 0

**Repro:** invariant seed 20260815001: sheet.copy, sheet.add (baseline re-captured here, activeSheet 2), structure.insert-row, sheet.switch {tabIndex: 0}, then 22 ordinary transactions on Sheet1. Winding all 22 back restores every cell, style and object -- exactly ONE digest difference in the whole workbook, `activeSheet: 2 -> 0`, which is the walk's own sheet switch.
**Fix:** fixed — `activeSheet` is excluded from the UNDO profile and still compared under saveReload, where it is real document state (`workbook.active_sheet` is persisted, so a save/open that lost it is a defect). What is lost is stated rather than glossed: undo's own activation behaviour is covered at a tighter tier by app/src-tauri/src/undo_sheet_activation_tests.rs (undo, redo, hidden target, every per-sheet store, the dependency maps), and frontend/backend disagreement about which sheet is active is still caught per action by the cheap invariants.
  Files: app/e2e/oracles/digest.ts (UNDO_EXCLUDED_SECTIONS = activeSheet, undo profile only), app/e2e/__tests__/digestDiffProfiles.test.ts (NEW)

## BUG-0091 `[fixed]`

**Found:** 2026-08-16 (manual)
**Oracle:** harness-integrity

THE WALK RAN ITS ENTIRE ORACLE BATTERY TWICE AT THE SAME STEP. The in-loop checkpoint fires on `step % cadence === 0 || isLastStep`, and the 'final off-cadence checkpoint' after the loop fired on `step % cadence !== 0` -- both true at the last step of any walk whose length is not a multiple of the cadence. A 40-action walk at cadence 25 therefore ran three checkpoints, not two: digests, the undo round-trip and the save/reload round-trip, a second time, over a state nothing had touched in between. It also made `plannedCheckpointCount` UNDER-count, which is the one direction its own docstring promises it never goes -- and that number is what `assertCadenceReachable` uses to refuse an unreachable save/reload cadence.

**Repro:** Any walk whose `maxActions` is not a multiple of `oracleEveryNActions` (e.g. SOAK_ACTIONS=40, SOAK_ORACLE_EVERY=25) reported one more checkpoint than `plannedCheckpointCount(40, 25) = 2`.
**Fix:** fixed — The final off-cadence checkpoint is skipped when the in-loop one already ran at that step. The shipped projects (75/25, 50/25, 150/25) were all multiples and never hit it, which is why it survived; SOAK_ACTIONS is an env var and the 2026-08-13 soak run that measured '40 actions, two checkpoints' was one setting away from it.
  Files: app/e2e/walker/walkRunner.ts (lastCheckpointStep guards the final checkpoint), app/e2e/__tests__/oracleCadenceReachable.test.ts (pins that the count is now exact)

## BUG-0092 `[fixed]`

**Found:** 2026-08-16 (manual)
**Oracle:** script-tier-provenance

AN IMPORTED FILE CHOSE THE PRIVILEGE TIER OF THE CODE IT INSTALLED. `importTemplate` (app/extensions/ScriptableObjects/lib/templateManager.ts) was `JSON.parse(json) as ObjectTemplate` -- a CAST, which validates nothing -- and the parsed object was persisted to %APPDATA%/Calcula/templates/ verbatim, `accessLevel` included. That field is load-bearing, not decorative: `stampFromTemplate` copies it onto the stamped `ObjectScriptDefinition`, and `buildHandleFromDefinition` (app/src/api/scriptHost/broker.ts) converts `accessLevel === "unlocked"` into `tier: "unlocked"`, which in ALLOWLIST terms is whole-workbook reach: api.getCellValue, api.setCellValue, api.updateCellsBatch over 100,000 cells, api.executeCommand. Object scripts run on their object's events, so no further gesture was required to execute it. Worse, the import carries no `provenance`, so `isDistributed` is false and the script lands in the MOST trusted bucket -- ui.html auto-granted, declared-capability ceiling taken from its own source pragmas -- rather than the consent-gated distributed one. The dialog's own empty state invites the user to 'import a .calcula-template file', so the user believes they are choosing a document and are in fact choosing executable code AND the privilege level it runs at. Nothing in the flow states the tier. The rule was already written one file away: `draftToScriptDefinition` (lib/scriptDrafts.ts) forces "restricted" because 'an AI-authored script must never arrive pre-escalated to the unlocked tier; raising it is a separate, deliberate human action in the editor.' The template import door simply never got it.


## BUG-0093 `[fixed]`

**Found:** 2026-08-16 (walk, seed 20260816102)
**Oracle:** undo-evidence-missing

THE NEW UNDO-EVIDENCE META-ASSERTION TURNED THE INVARIANT PROJECT RED ON A SEED NOBODY CHOSE. `state-consistency.spec.ts`'s rapid-fire walk failed with `the undo round-trip oracle DECIDED NOTHING across 2 checkpoint(s): 0 had nothing to undo and 2 were undecidable`, after 11 mid-window baseline rebases. It is structural, not bad luck: that walk runs at `rapidFireProbability: 0.5`, so half its actions are rapid create/delete pairs drawn from exactly the families that END the undo history (10 `fr.*` and 6 `sheet.*` in 50 actions). `WalkRunner` re-captures the baseline after every NON-checkpoint action, so a window only stays dead when the CHECKPOINT'S OWN action is a history-ender -- about 11/50 = 0.22 here. The walk had only 2 checkpoints (50 actions at the main walk's cadence of 25), giving ~0.22^2 = 5% per run of zero undo evidence. The undo track measured 0-of-12 zero-evidence walks on its own six seeds and shipped the per-walk requirement without giving the rapid-fire walk a cadence that could satisfy it.


## BUG-0094 `[fixed]`

**Found:** 2026-08-16 (manual, seed 20260816102)
**Oracle:** shrinker

THE SHRINKER SPENT ITS WHOLE BUDGET ON A VIOLATION IT COULD NOT POSSIBLY REPRODUCE. When the rapid-fire walk failed `undo-evidence-missing`, `writeFailureBundle` handed the 50-action trace to ddmin, which ran 16+ candidate replays with EVERY ONE reported `-> pass`. It could not have gone otherwise: all three replay paths (`state-consistency`, `soak-walk`, `replay-trace`) construct their OracleBattery with `requireUndoEvidence: false` -- correctly, so a one-action shrink candidate is not judged on undo evidence -- so the verdict being minimized is one the replay function is structurally incapable of returning. ddmin reduced nothing, would have burned up to 30 replays and a 15-minute budget per failing seed, and writes a bundle whose `could not reduce` reads to the next human like a failed reproduction. It is also the wrong question: `undo-evidence-missing` is a property of the walk's CONFIGURATION (cadence vs history-ender density), not of the trace, so no subset of the actions is a smaller reproducer.


## BUG-0095 `[fixed]`

**Found:** 2026-08-16 (manual)
**Oracle:** function-catalog-completeness

GATHER.AT IS A WORKING FORMULA FUNCTION THAT NO CATALOG KNOWS ABOUT. `BuiltinFunction::all_catalog_entries()` (core/parser/src/ast.rs:1867) is documented in its own header as "the single source of truth for the function catalog", and it lists four of the five GATHER functions -- GATHER, GATHER.FROM, GATHER.COUNT, GATHER.SUBMITTERS (ast.rs:2412-2415) -- but not GATHER.AT. GATHER.AT is otherwise fully shipped: it lexes/parses (ast.rs:1023), renders back (ast.rs:1572), and evaluates (core/engine/src/evaluator.rs:1987, `fn_gather_at`, with five dedicated evaluator tests). Both consumers of the catalog are therefore wrong about it: `build_full_catalog` (app/src-tauri/src/formula.rs:35) omits it from the function list that drives Insert Function / autocomplete, and `get_function_template` (formula.rs:126) finds no entry and falls through to its `format!("={}()", upper)` fallback, so inserting it yields a bare `=GATHER.AT()` with no argument placeholders while its four siblings get a generated template. Nothing catches this: there is no test asserting the enum and the catalog agree (the only catalog test, `test_get_controlvalue_catalog_entry_and_aliases` in core/parser/src/tests.rs:1684, checks one unrelated function), and the deliberate-hiding mechanism that does exist -- `FunctionMeta::alias()`, ast.rs:2540, which sets `is_alias` so `build_full_catalog` filters the entry out -- was NOT used here, so this is an omission rather than an intentional hide. GATHER.AT is the one GATHER function docs/design/calp-writeback.md singles out as load-bearing ("the primitive that makes per-line-item / tabular consolidation possible"), and it is the only one a user cannot discover.


## BUG-0096 `[fixed]`

**Found:** 2026-08-16 (manual)
**Oracle:** drill-through-fail-safe

A PIVOT IN script DRILL MODE WHOSE SCRIPT DOES NOT REGISTER onDrillThrough SWALLOWS THE DOUBLE-CLICK ENTIRELY -- no drill, no fallback, no message. The interceptor in app/extensions/Pivot/index.ts (lines 1725-1748) branches on `behavior?.kind === "script" && hasObjectScript("pivot", pivotId)`, emits the app event `pivot:drillThrough`, and RETURNS -- skipping the built-in drillThroughToSheet path. Its own comment states the invariant it is protecting: the hasObjectScript guard exists 'so a double-click is never a silent no-op'. But hasObjectScript (app/src/api/objectScriptBadge.ts:38) only answers 'does this object have a script attached', not 'does that script handle this event'. The handler side is opt-in: the forwarder is installed only when the script calls pivot.onDrillThrough (app/src/api/scriptHost/host.ts:12090, case "pivot.onDrillThrough"), so a pivot script that registers only, say, onRefresh leaves the emitted event with no subscriber. The double-click is consumed by the interceptor (it returns true), so the user gets no drill sheet, no built-in fallback and no toast -- the exact silent no-op the guard was written to prevent, and a violation of requirement 7 of docs/design/pivot-drillthrough-customization.md section 6 ('Consent denied, capability missing, script error, or timeout -> fall back to builtin (or no-op with a toast) ... The drill never breaks the pivot').


## BUG-0097 `[fixed]`

**Found:** 2026-08-16 (manual)
**Oracle:** script-surface-taxonomy-accuracy

THE SCRIPT-SURFACE TAXONOMY STILL DESCRIBES A RETIRED BEARER-TOKEN AUTHORIZATION MECHANISM THAT NO LONGER EXISTS IN THE CODE. `app/src/api/scriptSurfaces.ts` is the single queryable source of truth backing the Transparency pillar -- it feeds the transparency panel and is the answer to "what can this surface reach and how is it gated". Its `script-library` row is stale in two fields. (1) `gate` (scriptSurfaces.ts:158) ends: "Calls in are authorized by an unguessable host-issued token, which is delegation-transparent but not caller-identifying -- see docs/design/script-package-manager.md §10.4". (2) `containment` (:130) says exported names are routable "through one token-gated entry point". Neither is true any more. The token design was REPLACED by `base.callImport`, which authorizes on caller IDENTITY: the broker row + validator are at allowlist.ts:105 (`vCallImport`), dispatch at host.ts:2645, and the authorization is `authorizeImportCall` (host.ts:2304-2328), which resolves the alias in `scriptImports` (host.ts:2216) -- host state keyed by the CONSUMER's authoritative mount id and written only by trusted linker code -- then caps the call by that caller's own declared capabilities via `requireCallerCoversLibrary` (host.ts:2331). The token machinery is gone, not merely bypassed: `app/src/api/scriptLibraries/linker.ts:38-39` states "There is no bearer token any more. The previous design minted a 128-bit token per (realm, consumer) and baked it into the prelude", and host.ts:2174-2177 records the consequence -- "There is no credential left to leak. Handing a peer the alias string achieves nothing: the peer's own table is consulted, not the sender's." The doc section the row cites, script-package-manager.md §10.3/§10.4, has been marked CLOSED by the 2026-08-16 documentation audit, so the row now points a reader at a section that explicitly says the mechanism it describes was deleted.


## BUG-0098 `[open]`

**Found:** 2026-08-16 (manual)
**Oracle:** backend-answers-invokes

THE BACKEND STOPPED RETURNING FROM TAURI COMMANDS MID-RUN WHILE EVERY LIVENESS SIGNAL STAYED GREEN. UNREPRODUCED -- filed for attribution, not as a diagnosed defect; see the evidence and the explicit non-claims below.

WHAT WAS OBSERVED. A journey run failed 64 consecutive tests, every one on timeout, never on an assertion. The first failing spec (document-store-leak) contains ZERO Playwright locators -- a grep for locator(/getBy/.click(/.fill(/waitForSelector over its 839 lines returns nothing, so no actionability wait exists that could burn 300 s. Every step goes through page.evaluate, either __TAURI__.core.invoke directly or one of the app own modules imported via __calcImport (:79-91, :94-115). Its 300 s timeouts therefore cannot be explained by a selector, a repaint, or anything in the DOM. Playwright destroys and rebuilds the worker after every failed test, and all 64 rebuilds reported a healthy app. To be precise about WHICH check ran: assertAppMounted runs once per RUN from global-setup (:128, :288), not per rebuild; what re-ran was the worker-scoped sharedPage fixture -- connectWithRetry plus a 60 s waitForSelector on the spreadsheet container (fixtures.ts:244, :316). Same shape, equally blind: CDP accepted a connection and a DOM node was visible, and both remained true throughout. Total cost 5.4 hours; the report showed 64 failures with no indication they were one event.

WHAT WAS RULED OUT, by running it. document-store-leak in isolation: 7/7 pass. Specs 7-8 plus dsl: 13 pass. Specs 1-6 plus dsl: 33 pass. THE FULL JOURNEY PROJECT, unmodified: 156 passed in 27.8 minutes. So it is not that spec, not that ordering, and not deterministic. All three harness death-modes were also excluded because each fails FAST (3 s, instantly, and 60 s respectively), whereas these burned the full 300 s.

WHAT IS NOT CLAIMED. The mechanism. A lock-order inversion in the grid locks is the leading candidate on this project's history, but nothing here proves it. Two pieces of evidence that would have decided it were destroyed by design: init_log_file OPENED the app log with .truncate(true) on EVERY app start (fixed 2026-08-17 -- it now rotates; see notes), so four later runs overwrote the only account the backend gave of itself, and results.json is overwritten per run.

WHAT WAS DONE INSTEAD OF GUESSING. Fixing product locks with no reproduction would be speculation. The response makes the next occurrence cheap, attributed and diagnosable: e2e/wedgeGuard.ts probes the backend with a real invoke before every test (double-raced, because page.evaluate has no timeout in Playwright's API), latches after two consecutive unanswered probes, and fails every later test immediately -- turning 5.4 hours into minutes -- while global-teardown prints a banner saying the failures are ONE fact, and archives the app log per run so the evidence survives. Decision logic pinned by app/e2e/__tests__/wedgeGuard.test.ts (8 tests; sabotage measured, not estimated -- latching on the first bad probe reds 5 of 8, and putting the counter back in module memory reds exactly the restart test).

TO CLOSE THIS. A reproduction is still needed -- and the evidence needed to get one now SURVIVES (the log-truncation problem above was fixed 2026-08-17; see notes). The next occurrence will leave e2e/results/app-logs/app-dev-<stamp>.log and an APP-WEDGED.txt naming the test it was first seen before -- start there.


## BUG-0099 `[fixed]`

**Found:** 2026-08-17 (manual)
**Oracle:** inline-media-hazard-refused

BUG-0086 WAS FIXED ONLY FOR THE LOWERCASE SPELLING. A decompression bomb whose base64 tag is capitalised -- data:image/png;BASE64,<4 KB 30000x30000 PNG> -- or whose base64 BODY is percent-escaped -- data:image/png;base64,%69VBORw... -- was classified as a POLICY refusal ("safe on screen, leave inline") and delivered to the WebView decoder, allocating ~3.6 GB of RGBA, exactly as BUG-0086 described.

ROOT CAUSE, and it is the interesting part: the hazard/policy split is only as good as the agreement between the HOST parser and the parser that ACTUALLY RUNS, and the host was STRICTER. decode_image_data_url (core/calcula-format/src/media.rs) required a byte-exact lowercase ";base64" and its hand-rolled decode_base64 returns None at the first "%". The WHATWG data-URL processor WebView2 implements matches the base64 tag ASCII case-insensitively and PERCENT-DECODES the body BEFORE base64-decoding. Every string in that gap took the None arm of judge_inline_image, and that arm treated "could not parse" as "harmless" -- so no byte, dimension or pixel cap ever looked at the payload, while paintableUrl (app/extensions/Controls/Image/imageRenderer.ts:214) hands any non-handle string straight to img.src (:246). The widened exceeds_byte_cap_encoded added by the 1.6 work repeated the same wrong literal, so there were TWO copies of one wrong spelling.

REPRODUCTION: publish a .calp whose controls.json holds src = "data:image/png;BASE64,<base64 of a 30000x30000 single-colour PNG>" and sign it normally. Subscribe and pull (calp_commands.rs:2698). Before the fix: report.refused == 1, the string is materialised verbatim, written into the subscriber own .cala, and decoded by the renderer. The percent-escaped variant needs no case difference at all.

FIXED 2026-08-17, two parts. (1) The host now agrees with the browser: one shared ends_with_base64_tag (case-insensitive) used by both call sites so they cannot drift again, plus percent-decoding of the body. Both are pure widenings of what the host can READ, so they strictly increase inspection coverage. (2) The default is INVERTED: LeaveInline now requires a payload the host could actually read, and an undecodable string declaring a RASTER format is dropped -- "we could not parse it" is not evidence the renderer cannot. SVG keeps its tolerance, being the one declared type this host never decodes by design.

WHY THE SUITE COULD NOT SEE IT: the BUG-0086 regression test builds its fixture with the data_url() helper, which is format!("data:image/png;base64,{}") -- lowercase only. Now covered by a_bomb_spelled_with_an_uppercase_base64_tag_is_dropped, a_bomb_with_a_percent_escaped_base64_body_is_dropped, an_undecodable_raster_declaration_is_dropped_not_tolerated and svg_keeps_its_policy_tolerance_under_the_inverted_default; reverting the three fixes reds 4 tests.


## BUG-0100 `[fixed]`

**Found:** 2026-08-18 (e2e)
**Oracle:** toast-swallows-grid-click

A TOAST NOTIFICATION IS A CLICK SURFACE OVER THE GRID, SO ANY ON-GRID CONTROL IN THE BOTTOM-RIGHT IS DEAD FOR THE 5 SECONDS THE TOAST LIVES. ToastContainer already sets pointerEvents:'none' on the container, and its comment explains why (a layout box at z-index 9999 over the grid swallows clicks). The fix was applied to the container only: each ToastItem re-enabled pointerEvents:'auto' on its whole 380x~58 box, so the same defect survived at a smaller size. WHAT WAS OBSERVED: the product announces 'Button created at P63'; a click on that button 312 ms later never reaches the grid, and the macro's output cell stays empty for a full 60-second poll. WHY IT WAS HARD TO ATTRIBUTE: the test observed only the cell, and an empty cell is equally consistent with 'the click never arrived', 'the click arrived and the run path broke', and 'the macro ran and wrote nothing'. Two separate investigations reconstructed the geometry from source rather than measuring it and reached opposite conclusions; one computed the toast's top border at y=705.6 against a click at y=705.0 and concluded the toast missed by 0.6 px. WHAT SETTLED IT: instrumenting the spec to ask the PRODUCT instead of arithmetic. hitTestOverlays (app/src/api/gridOverlays.ts:252), the same function the canvas click path consults, returns HIT button id=control-0-62-15 at r62c15 for the click point -- so the click was deliverable -- while document.elementFromPoint at the same page point returns a <div> inside [data-toast]. Blink's point hit test lands in the toast despite the 0.6 px arithmetic. CONTROLLED EXPERIMENT (same spec, same lever, only Toast.tsx changed between rows): before the fix, toast-dismissal OFF = 1 failed / 4 passed and ON = 5 passed; after the fix, toast-dismissal OFF = 5 passed. NOT A REGRESSION from any recent commit: the toast body has been a surface since the container-only fix, and nothing clicked that corner of the grid until this test did.

**Repro:** `app/test-results/macro-live-edit-Live-macro-68537-ed-macro-no-save-no-reopen--functional/trace.zip` (1 actions)
**Triage:** product-bug (confidence 1) — ToastItem set pointerEvents:'auto' on the toast body to keep its OK button clickable. Only the OK button needs to be a surface; the rest of the toast is text.
**Fix:** fixed — Toast body -> pointerEvents:'none'; the OK button re-enables 'auto' so the toast stays dismissable. New unit test asserts all three layers (body none / OK auto / container none), each sabotage-checked independently: reverting any one reds exactly its own assertion and no other. macro-live-edit.spec.ts additionally gained a delivery probe that asserts hitTestOverlays finds the control BEFORE the outcome poll, and attaches a diagnosis on failure -- so this class can never again cost a day of attribution.
  Files: app/src/shell/Toast/Toast.tsx, app/src/shell/Toast/__tests__/toastClickThrough.test.tsx, app/e2e/tests/macro-live-edit.spec.ts

## BUG-0101 `[fixed]`

**Found:** 2026-08-18 (e2e)
**Oracle:** manual-live-pixel

EVERY CELL BORDER PAINTED AT HALF ITS NOMINAL WEIGHT, AND VERTICAL ONES SMEARED ACROSS TWO DEVICE PIXELS AT HALF ALPHA. Two stacked defects in app/src/core/lib/gridRenderer/rendering/cells.ts. (1) THE CLIP. The per-cell clip exists to cut overlong TEXT, and the border pass ran inside it. A 'medium' border is 2px centred on the boundary, so the clip discarded the outer half: borders rendered at half weight, wholly inside the cell, instead of straddling the boundary the way Excel draws them. (2) NO DEVICE-PIXEL SNAPPING. drawGridLines has always snapped its hairline (rendering/grid.ts:189-191); drawBorderLine never did. Row boundaries are integral (defaultCellHeight 20) but column boundaries are not (defaultCellWidth 64.29, core/types/types.ts:256), so horizontal borders happened to land on one whole device pixel while vertical ones split across two. WHAT WAS OBSERVED: the test sampled a 5x12 band on the right edge of a red 'medium' outline and got EXACTLY ZERO red pixels, while the TOP edge of the same outline passed its threshold. The trace's stored pixels show every one of the band's 12 rows as 255,255,255 | 255,107,107 | 238,131,131 | 255,255,255 | 255,255,255 -- one red line spread over two pixels at ~58%/42%, against a predicate needing >=64.7% coverage. WHY IT SURFACED ONLY NOW: at dpr 2 the surviving half-stroke is 2 device pixels, so one is always fully covered. The defect is LATENT, not a regression -- it became visible when the machine moved from a 200% display to 100% and the golden corpus followed. A THIRD PROBLEM THE FIX HAD TO SOLVE: once borders straddle the boundary, painting them in place is wrong regardless of the clip. The cell loop is row-major, so a RIGHT or BOTTOM border spilling forward is erased when the neighbour fills its background, while LEFT and TOP borders spill backwards and survive. The ordering is the defect, so the borders are now collected and stroked after every background in the viewport is down.

**Repro:** `app/test-results/vba-idioms-wave3-Wave-3-VB-1aeaf-trips-and-renders-edge-only-functional/trace.zip` (1 actions)
**Triage:** product-bug (confidence 1) — The border pass sits inside the per-cell text clip and strokes unsnapped raw coordinates, so half of every stroke is discarded and the remainder lands on a fractional device-pixel boundary on the horizontal axis.
**Fix:** fixed — drawBorderLine now derives the device scale from the canvas transform and snaps by PARITY (odd device widths to a pixel centre, even to a pixel edge), matching what grid.ts has always done for the gridline hairline -- a thin border provably lands on the same device pixel as the gridline it replaces. Edge borders are queued during the cell pass and stroked in a single pass afterwards, so a border may straddle its boundary without the next cell's background erasing it. Contested boundaries collapse to ONE stroke (heavier wins, ties to the later cell), which is a deliberate behaviour change: at full weight the two halves are coincident rather than disjoint, so without dedupe a thin grey neighbour would punch a stripe through a thick black border and an rgba border would double-composite. The double-border ornament derives BOTH rules from ONE snapped boundary -- snapping each independently makes Math.round tie the same way and shifts the whole ornament off an already-crisp integral boundary. Diagonals stay inside the per-cell clip and are not snapped, since snapping one axis of a diagonal skews it.
  Files: app/src/core/lib/gridRenderer/rendering/cells.ts, app/src/core/lib/gridRenderer/rendering/cellBorders.test.ts

## BUG-0102 `[fixed]`

**Found:** 2026-08-18 (review)
**Oracle:** frozen-pane-borders-absent

WITH A FREEZE OR A SPLIT ACTIVE, NO CELL BORDER RENDERS AT ALL. UNVERIFIED AGAINST THE RUNNING APP -- filed for attribution from a code reading, not as a reproduced defect. WHAT WAS OBSERVED (statically): panes are painted by renderZone (app/src/core/lib/gridRenderer/core.ts:230), which calls drawCellTextZone (core.ts:340). That function's body contains no reference to borders at all -- confirmed by scanning its whole body for /border/i, which returns nothing -- whereas the non-frozen painter drawCellText (rendering/cells.ts) draws all six sides. renderZone is the path for BOTH split panes (core.ts:809, 816, 823, 829) and frozen panes (core.ts:880, 886, 893, 900). WHAT IS NOT CLAIMED: that this has been seen on screen; that it affects the unfrozen path (it does not -- that is the path the sibling entry fixes); that no other code supplies borders for these zones by some route not found. TO CLOSE THIS: freeze a pane over a bordered range in the running app and look, then either fix drawCellTextZone to share the border queue drawCellText now uses, or reclassify. An E2E probe modelled on vba-idioms-wave3.spec.ts:468 with a freeze applied would settle it in one run.


## BUG-0103 `[fixed]`

**Found:** 2026-08-18 (review)
**Oracle:** timeline-slicer-never-persisted

A TIMELINE SLICER IS NEVER SAVED, HAS NO UNDO ARM, AND LEAKS INTO THE NEXT DOCUMENT. Insert Timeline is a shipped, one-click ribbon button on the PivotTable Analyze tab (app/extensions/Pivot/components/PivotAnalyzeSections.tsx:292-305 -> showDialog('timelineSlicer:insertDialog'); the extension is registered at app/extensions/manifest.ts:215), and TimelineSlicerState (managed at app/src-tauri/src/lib.rs:4839) reaches no save path at all. MEASURED with node: /timeline/i occurs ZERO times in app/src-tauri/src/persistence.rs and ZERO times in app/src-tauri/src/calp_commands.rs. THREE CONSEQUENCES, and only the first is named anywhere in the tree: (1) SILENT TOTAL LOSS. The user inserts a timeline, saves, reopens, and it is simply gone. assemble_workbook_for_save never reads the state. (2) NO UNDO RESTORE ARM, so a cascade delete records nothing to bring back (app/src-tauri/src/object_deps.rs:63-67 says so in a comment). (3) CROSS-DOCUMENT LEAK, which nobody has written down anywhere: reset_document_scoped_stores (app/src-tauri/src/persistence.rs:3803-3812) takes AppState, UserFilesState, SlicerState, RibbonFilterState, PaneControlState, ScriptState, PivotState and BiState -- TimelineSlicerState is NOT among them, and it is a separate managed Tauri state. So a timeline from document A survives File > New / Open into document B. That is the same class as the 2026-08-10 document-scoped-state defect, which was closed by adding exactly this function; this store was never enrolled in it. WHY IT WAS INVISIBLE: the gap is recorded ONLY in code comments (timeline_slicer/commands.rs:85-88, object_deps.rs:63-67) and in the 1.33 MB archive (docs/design/open-decisions-2026-08.md:8573-8576), which the project's own rules say must never be read as live status. docs/design/open-items.md does not mention it. A limitation documented only in a comment is invisible to the open-items list BY CONSTRUCTION.


## BUG-0104 `[fixed]`

**Found:** 2026-08-18 (review)
**Oracle:** cf-icon-sort-and-filter-silent-noop

SORTING AND FILTERING BY CONDITIONAL-FORMATTING ICON ARE SILENT NO-OPS THAT REPORT SUCCESS. Both arms are stubs that return the wrong answer without saying so, and one of them is offered to the user by name in a dialog. (1) SORT: `SortOn::Icon` (app/src-tauri/src/commands/data.rs:5729-5740) carries the comment "Icon sorting not yet implemented - fall back to value comparison" and does exactly that — it sorts by VALUE and returns Ok. The Sorting extension offers it explicitly: app/extensions/Sorting/components/SortLevelRow.tsx:183 renders `<option value="icon">Conditional Formatting Icon</option>`. So a user picks 'Conditional Formatting Icon', the rows reorder (by value), and nothing anywhere says the request was not honoured. A sort that silently sorts by the wrong key is worse than one that refuses. (2) FILTER: `FilterOn::Icon` (app/src-tauri/src/autofilter.rs:884-889) has an EMPTY arm whose comment ends "For now, icon-filtered rows are always shown" — the predicate falls through to true, so the filter hides nothing at all and the sheet looks unfiltered while reporting a filter is applied. WHY IT MATTERS BEYOND THE FEATURE: this is the class where the product returns a plausible WRONG answer rather than an error, which is the hardest kind for a user to catch — the rows did move, so the operation looks like it worked. RECORDED NOWHERE until now: neither arm appears in docs/design/open-items.md or the ledger; the only trace was the two code comments quoted above, which are invisible to any status read.

**Fix:** fixed — Both arms of the DEFECT are gone. The sort no longer falls back to value comparison, and the filter no longer falls through to true; each is implemented, and each REFUSES the shapes it genuinely cannot answer instead of guessing. The last dead half was the Sort dialog: it offered 'Conditional Formatting Icon' in Sort On and then showed 'A to Z' for the order, so it never set an icon and the backend refused every such sort with a correct message about a choice the UI never offered. The dialog now lists the icons the column actually shows, each as On Top / On Bottom, and forwards the chosen one.
  Files: app/src-tauri/src/commands/data.rs (icon sorting implemented: build_icon_match_sets, compare_by_icon; validate_sort_fields replaced the blanket refusal and refuses only three genuinely unanswerable shapes, checking EVERY level rather than fields[0]), app/src-tauri/src/autofilter.rs (FilterOn::Icon implemented via resolve_filter_icons + icon_criteria_keeps, which FAILS CLOSED -- a criteria naming nothing HIDES the row rather than showing every one, because the whole auto_filters map is restored verbatim from .cala and re-enters state without passing the door validator; validate_icon_criteria refuses the three unhonourable shapes at the door), app/src-tauri/src/conditional_formatting.rs (resolve_icons: ONE cascade -- priority order, `enabled`, `stop_if_true`, first icon wins, index clamped to the set's own count -- keyed on by both sort and filter; NEW `get_range_icons` command so the Sort dialog's picker asks that same cascade), app/src-tauri/src/lib.rs (registers get_range_icons), app/src/api/backend.ts + index.ts + lib.ts (getRangeIcons), app/extensions/Sorting/types.ts (SortLevel.icon), lib/sortHelpers.ts (getUniqueIconsInColumn, encode/decodeIconOrderValue, iconLabel), components/SortLevelRow.tsx (icon gets its OWN order branch), components/SortDialog.tsx (icon: level.icon -- the one line that closes the loop)

## BUG-0105 `[fixed]`

**Found:** 2026-08-19 (review)
**Oracle:** conditional-formatting-absent-in-panes

CONDITIONAL FORMATTING DOES NOT REACH A FROZEN OR SPLIT PANE AT ALL. `renderZone` -> `drawCellTextZone` (app/src/core/lib/gridRenderer/core.ts) paints every cell of every pane INSTEAD of `drawCellText`, and it runs NO style interceptors: a repo-wide grep of core.ts for hasStyleInterceptors/applyStyleInterceptors/useInterceptors returns NOTHING, while `drawCellText` consults them per cell. So the moment a user freezes a pane or splits the window, every conditional-formatting fill, font colour, data bar and icon stops being drawn in that pane — the cell reverts to its static style. STRICTLY BIGGER THAN BUG-0102, which was the same painter missing plain cell BORDERS and is fixed. That fix passes an empty `{}` where the main painter passes the interceptor-resolved style, with a comment saying why — so CF-driven borders are still missing in panes along with everything else CF does. NOT SEEN ON SCREEN. Verified by reading the two painters and by the absence of any interceptor call in the zone path; no app run was made. To confirm: apply a conditional format to a range, freeze a pane above it, and look.


## BUG-0106 `[fixed]`

**Found:** 2026-08-19 (review)
**Oracle:** autofilter-silently-wiped-on-deserialize-error

A MALFORMED autofilters.json SILENTLY DELETES EVERY AUTOFILTER IN THE WORKBOOK, AND THE NEXT SAVE MAKES THE LOSS PERMANENT. app/src-tauri/src/persistence.rs:3688-3697, on the .cala load path: if the section is present but `serde_json::from_slice::<AutoFilterStorage>` fails, the `else` arm runs `auto_filters.clear()`. No error is returned, nothing is logged, and the user is not told. The document opens looking fine, with every filter gone; saving then writes the empty state back over the file that still had them. THIS IS THE CLASS THE .cala RULES CALL OUT BY NAME: the test for whether a section deserves a version link is whether an older reader would MISHANDLE the document or merely lose something, and 'a stale workbook that comes back looking calculated' is the example of a drop that is a LIE. A silent clear on a PARSE FAILURE is the same lie without even a version mismatch to explain it. NOTE the two arms are not equivalent and only one is wrong: the outer `else` (no autofilters.json at all) clearing is CORRECT -- a document with no filters has none. It is the inner one, where the section EXISTS and could not be read, that destroys data. WHY IT MATTERS NOW: any change to the serialized shape of a filter makes this reachable. BUG-0104's icon-filter work adds a field to FilterCriteria, so a file written by one build and opened by another is exactly the scenario that trips it.


## BUG-0107 `[fixed]`

**Found:** 2026-08-19 (review)
**Oracle:** icon-glyph-from-wrong-rule

THE CONDITIONAL-FORMATTING ICON A USER SEES CAN COME FROM A DISABLED RULE. The drawn glyph is a JOIN OF TWO INDEPENDENT LOOKUPS that can disagree: (1) the icon INDEX comes from the backend, which cascades correctly — it skips !enabled, honours stop_if_true, and respects priority; (2) the icon SET (which glyph family) comes from `findMatchingRuleId(row, col, "iconSet")` (app/extensions/ConditionalFormatting/lib/cfStore.ts:254-274), which matches on rule TYPE and geometric range containment ONLY. It does not read `rule.enabled`, does not honour `stop_if_true`, does not consider priority, and returns the FIRST match in array order. CONSEQUENCE: a DISABLED five-icon rule listed before an enabled three-icon rule over the same range makes the renderer draw a five-arrow glyph indexed 0..2 — a picture assembled from two different rules, one of which the user switched off. AND A HARD-CODED FALLBACK: when no rule id resolves, the renderer silently draws `threeTrafficLights1` (iconSetRenderer.ts:60) rather than nothing, so a cell can show traffic lights that no rule asked for. WHY IT MATTERS BEYOND THE PICTURE: 'sort by the icon I can see' has no referent while the displayed icon is not a single value. This is the prerequisite for BUG-0104's icon sorting, not a cosmetic sibling of it — sorting on a glyph the renderer computed from a disabled rule would produce an order the user cannot explain from what is on screen.


## BUG-0108 `[open]`

**Found:** 2026-08-28 (review)
**Oracle:** bi-pivot-visible-total-ignores-slicer-mask

PERCENT-OF-VISIBLE-TOTAL MEASURES ARE WRONG IN A SLICER-FILTERED BI PIVOT. BI pivots send the engine NO filters: every surface (axis, pane, slicer) becomes a group_by column and filtering is a host-side hidden_items mask over the cached grouped result (app/src-tauri/src/pivot/commands.rs — update_bi_pivot_fields builds QueryRequest { filters: vec![] }). A measure using CLEAR_INNER/ALLSELECTED (share-of-visible-total) is rendered by the engine as a window over the grouped result BEFORE the host mask exists — so its denominator includes rows the host hides afterwards. Additionally, engine-computed subtotal/grand-total overrides (BiTotalsPlan) are skipped whenever ANY hidden_items are active, so ratio-valued leaf rows get additively summed totals. Two mechanisms, both silent.


## BUG-0109 `[fixed]`

**Found:** 2026-08-28 (review)
**Oracle:** postgres-clear-window-hardcodes-sum

THE PUSHED (POSTGRESQL) CLEAR/CLEAREXCEPT RENDER RE-AGGREGATED EVERY AGGREGATE WITH SUM. expr_to_sql_with_clear (model-engine-lib/crates/engine-connectors/src/postgres.rs) emitted SUM(inner) OVER (PARTITION BY ...) regardless of the wrapped aggregate, while the local path (clear_reagg_fn, engine-query executor/pipeline/sql.rs) re-sums only additive aggregates, carries MIN/MAX, and fails closed on the rest. A MIN(x, CLEAR(dim)) measure pushed to PostgreSQL silently returned a SUM. Pushed and local paths disagreed on the same formula.


## BUG-0110 `[fixed]`

**Found:** 2026-08-28 (review)
**Oracle:** slicer-clobbers-axis-field-dropdown-filter

A SLICER ON AN AXIS FIELD OVERWRITES THE FIELD DROPDOWN FILTER (AND VICE VERSA). apply_pivot_filter carries no origin: a slicer selection and the pivot field dropdown write the SAME hidden_items on the same field (app/src-tauri/src/pivot/commands.rs), so whichever applied last wins and the other surface shows state it no longer controls. Origin is unrecoverable at the IPC boundary (payload is {pivotId, fieldIndex, manualFilter}).


## BUG-0111 `[fixed]`

**Found:** 2026-09-03 (review)
**Oracle:** script-origin-sentinel-forgeable

A PUBLISHER COULD FORGE THE LOCAL TRUST ORIGIN BY NAMING THEIR APPLICATION `local`. ScriptHandle.origin (app/src/api/scriptHost/broker.ts) was a STRING carrying two things in one namespace: the sentinel "local" for workbook-authored scripts, and the PUBLISHER-CHOSEN application name for distributed ones -- buildHandleFromDefinition set it as `isDistributed ? (definition.packageName || "(unknown package)") : "local"`. SIX consumers read that sentinel, so a .calp application named `local` got: (1) the LOCAL JIT-prompt path in maybeRequestCapabilityGrant (host.ts) and requestLibraryCapability -- i.e. net.fetch / storage / schedule / bi.query from a single in-the-moment prompt instead of only through package consent, which is a deliberately higher bar; (2) restoration of capability grants persisted for a LOCAL script of the same id + source hash (restoreAndSyncGrants, capabilities.ts); (3) persistence of its OWN "Always" grants into that local store, outside the package-consent record that is supposed to hold them (persistAlwaysGrant); (4) SAME-TRUST-ORIGIN with the user's own scripts under the R7 predicate sameTrustOrigin (broker.ts) -- measured live: it called a local script's NON-PUBLIC exposed method and received the secret, and the same predicate is the only admission rule for caps.forms.show, so it could open the user's forms; (5) a ui.dialog identity band reading "A script in this workbook is asking you a question" (ScriptDialogPrompt.tsx); (6) the Permissions-panel chip labelled "local" with the tooltip "Authored in this workbook" (PermissionsPanel.tsx). The transparency panel's scheduled-job and held-state owner columns (codeInventory.ts) additionally reported it as provenance "local" with no package at all. buildPreviewHandle carried a milder instance of the same shape: the literal "(preview)" made two previews same-origin with EACH OTHER.

**Repro:** Publish a .calp whose calp-manifest.json name is exactly `local`; subscribe and consent to one of its object scripts. Its handle.origin is then the string "local", byte-identical to a workbook script's. Unit reproduction: app/src/api/scriptHost/__tests__/scriptOriginForgery.test.ts (gates 1-4 plus the transparency labels), app/extensions/ScriptableObjects/__tests__/scriptDialogPromptBand.test.tsx (the ui.dialog band), the "origin chip" block of permissionsPanelDebuggerMount.test.tsx (the panel chip), scriptFormDialog.test.tsx (the form band, fixed earlier the same day), and "...and an application NAMED local is distributed here too" in scriptLibraries.test.ts, which drives a live JIT gate through authorizeImportCall.
**Fix:** fixed — origin is a discriminated union now, not a namespaced string. New leaf module app/src/api/scriptHost/scriptOrigin.ts defines ScriptOrigin = {kind:"local"} | {kind:"package"; name} | {kind:"preview"}, derived from `provenance` alone by scriptOriginForMount -- the publisher name lands in `.name`, on the variant already known not to be local, and `origin === "local"` no longer type-checks anywhere. A prefix scheme ("pkg:" + name) was rejected: it closes the hole but leaves a string that still looks comparable, so the next gate gets written with the same syntax and the same bug. sameTrustOrigin delegates its origin half to sameScriptOrigin, which additionally makes a preview same-origin with NOTHING (two previews used to match). The four gates test `.kind` -- via isLocalOrigin, and via the new shared mayJitPromptForCapability predicate that both JIT gates now call. The two bands and the panel chip branch on `.kind` and DISPLAY `.name`, with wording unchanged for every honest case; codeInventory keeps showing the publisher's name through originPackageName. FormOrigin is now an alias of the same union, so the band and the trust handle can no longer derive one fact twice. A drift guard walks src/api + extensions and fails on any origin-to-string comparison.
  Files: app/src/api/scriptHost/scriptOrigin.ts, app/src/api/scriptHost/broker.ts, app/src/api/scriptHost/capabilities.ts, app/src/api/scriptHost/host.ts, app/src/api/scriptHost/scriptDialogs.ts, app/src/api/scriptHost/scriptFormSpec.ts, app/src/api/codeInventory.ts, app/src/api/index.ts, app/extensions/ScriptableObjects/components/ScriptDialogPrompt.tsx, app/extensions/ScriptableObjects/components/PermissionsPanel.tsx

## BUG-0112 `[fixed]`

**Found:** 2026-09-03 (review)
**Oracle:** distributed-module-run-fabricates-local-provenance

A DISTRIBUTED MODULE SCRIPT RAN AT THE UNLOCKED TIER WITH LOCAL PROVENANCE. A .calp application may ship MODULE SCRIPTS. core/calp/src/pull.rs materializes them into the subscriber's workbook and stamps source_package, on the stated promise that they are "Inert, transparent data ... NEVER auto-executed -- they run only on explicit user action, sandboxed"; materialize_distributed_scripts (app/src-tauri/src/calp_commands.rs:1986) writes that stamp verbatim into script_state.workbook_scripts, the very map list_scripts/get_script serve, and only __calcula_-prefixed records are hidden -- so a pulled module IS listed in Developer > Macros and IS readable with its sourcePackage intact. But the two RUN paths threw the provenance away. (1) app/extensions/MacroRecorder/lib/macroLibrary.ts runMacroModule called runObjectScriptOnce({ accessLevel: "unlocked" }) for every macro, and (2) app/src/api/objectScriptRunner.ts runObjectScriptOnce defaulted accessLevel to "unlocked" and HARD-CODED provenance: "local". buildHandleFromDefinition (broker.ts) derives handle.origin from that provenance, so the publisher's code got: the LOCAL JIT capability-prompt path instead of application consent (mayJitPromptForCapability), the automatic local ui.html grant, restoration and persistence of grants in the LOCAL per-source store, SAME-TRUST-ORIGIN with the user's own scripts under sameTrustOrigin (reach to their non-public exposed methods and their forms), and the unlocked tier -- i.e. a non-null context.api with the full cross-sheet surface. (3) app/src/api/scriptHost/host.ts hostStartModuleScriptDebugSession (reached from ScriptableObjects/lib/debugger.ts) had the identical shape and was strictly worse: it hard-coded BOTH accessLevel: "unlocked" and provenance: "local" while resolving the record -- with record.sourcePackage sitting unread in the same function -- and a debug mount is a live realm the user then drives by hand. (4) TRANSPARENCY: listMacroModules DROPPED record.sourcePackage, so the library listed a publisher's macro indistinguishably from the user's own, with nothing on screen to base the consent decision on. (5) The same fabrication in the transparency panel: codeInventory.ts reported EVERY custom function as provenance "local" / sourcePackage null, although merge_custom_function_library (calp_commands.rs) stamps sourcePackage on each function it folds into the shared reserved record -- hiding a publisher's UDF inside the user's own code on the one panel a user consults to ask whose code this is. NOT AFFECTED: the moduleRuntime route (run_script), which require_distributed_module_consent (app/src-tauri/src/scripting/commands.rs:1046) already gates Rust-side and authoritatively.

**Repro:** Publish a .calp carrying a module script whose description contains "runtime=objectScript"; subscribe. The module appears in Developer > Macros with no indication it is not yours. Press Run: it mounts with accessLevel "unlocked" and provenance "local". Same for Edit in Object Script Editor > Debug. Unit reproduction: the "a module that arrived in an application" block of app/src/api/__tests__/objectScriptRunner.test.ts (tier, provenance, the explicit-unlocked refusal, and both evasions -- an omitted id caught by content, edited source caught by identity); "debugging a module that arrived in an application" in app/src/api/scriptHost/__tests__/debugSession.test.ts; app/extensions/MacroRecorder/__tests__/macroProvenance.test.ts and the "MacroLibraryDialog provenance" block of macroLibraryDialog.test.tsx for the listing and the on-screen chip; "custom-function provenance" in app/src/api/codeInventory.test.ts; and the "nothing writes a provenance literal into a mount" drift guard in scriptOriginForgery.test.ts.
**Fix:** fixed — Provenance is DERIVED from the stored record at every run path. scriptOrigin.ts (the BUG-0111 leaf) gains three siblings to scriptOriginForMount: scriptOriginForStoredRecord (source_package -> MountOrigin; a blank stamp is local, a non-empty one can never select the local kind), accessLevelForOrigin (a package origin caps the tier at restricted) and mountProvenanceForOrigin (the inverse -- the ONE place the string "local" is written into a mount). runObjectScriptOnce now resolves the artifact BEFORE mounting, from the module store, by BOTH keys with the stricter answer winning: by IDENTITY (a new scriptId option -- catches an EDITED distributed macro, since the library's Run sends the textarea's current text) and by CONTENT (exact source match -- catches a caller that omits the id), keeping distributed_module_refusal's escape hatch that a LOCAL record holding the same source authorises it. It FAILS CLOSED: if the store cannot be read the run is refused rather than assumed local. On tier it REFUSES A CONTRADICTION AND DERIVES AN ABSENCE -- a caller that explicitly passes "unlocked" for a package artifact gets a loud throw (silently downgrading would leave the caller's belief, and the code resting on it, intact and wrong), while a caller that says nothing gets the artifact's own answer. hostStartModuleScriptDebugSession derives both fields from the record it already loads. The macro library carries sourcePackage onto MacroModuleEntry, derives its tier request from it (macroRunAccessLevel) and passes scriptId; runMacroByRef forwards the record's package so a button on a distributed report cannot ask for a tier the publisher never earned. TRANSPARENCY: the list row gets a warning-coloured chip naming the application, the detail pane a line saying the user did not write it, and describeRunRoute stops promising an unlocked mount for distributed code. codeInventory reports per-function UDF provenance from the stamp the Rust merge writes. A drift guard walks src/api + extensions and fails on any provenance: "local" literal outside scriptOrigin.ts (the "distributed" direction is allowed -- it can only narrow).
  Files: app/src/api/scriptHost/scriptOrigin.ts, app/src/api/objectScriptRunner.ts, app/src/api/scriptHost/host.ts, app/src/api/codeInventory.ts, app/src/api/index.ts, app/extensions/MacroRecorder/lib/macroLibrary.ts, app/extensions/MacroRecorder/components/MacroLibraryDialog.tsx, app/extensions/MacroRecorder/components/styles.ts

## BUG-0113 `[fixed]`

**Found:** 2026-09-05 (review)
**Oracle:** ui-html-bridge-blocked-by-shipped-csp

EVERY SHIPPED ui.html TEMPLATE THAT TALKS BACK IS INERT IN A BUILT APP. `render.setHtml` paints a script's HTML into an `allow-scripts` srcdoc iframe and injects an INLINE <script> as the postMessage bridge (extensions/_shared/scriptFrame/frameDocument.ts, formerly duplicated in shapeRenderer.ts and CustomControlHost.tsx). The app ships `script-src 'self' blob:` with no 'unsafe-inline', no nonce and no hash (app/src-tauri/tauri.conf.json), and a srcdoc child inherits its embedder's policy container — so the bridge is refused and `window.calcula` never exists. The frame still PAINTS, so a display-only template looks perfect; only the ones that talk back are dead. MEASURED against a release build (`tauri build --no-bundle`, rebuilt with src-tauri/tauri.e2e.conf.json which changes only withGlobalTauri) serving from http://tauri.localhost/: violations = ["script-src-elem blocked inline"], `window.calcula` absent, and a real click inside the frame reached the host through nothing. There is NO nonce-shaped fix: the shipped "Interactive Counter" template (extensions/Controls/Shape/shapeTemplateCatalog.ts) and the default custom-pane scaffold (extensions/ControlsPane/components/CustomControlHost.tsx) drive the bridge from inline `onclick=` ATTRIBUTES, which a nonce cannot rescue even in principle — the route is to serve app documents from a Rust custom URI scheme with their own origin and policy.

**Fix:** fixed — A srcdoc child gets a CLONE of its embedder's policy container, so the app's `script-src 'self' blob:` (no 'unsafe-inline', no nonce, no hash) landed inside the frame and refused the inline bridge -- and `sandbox=allow-scripts` without allow-same-origin makes the frame's origin OPAQUE, so the inherited 'self' matched nothing either and an external <script src> from 'self' was refused for the same reason. There was no nonce-shaped fix: the shipped Interactive Counter and the default pane scaffold drive the bridge from inline onclick= ATTRIBUTES. The bridge is now a CONSTANT loader served from Rust over a custom URI scheme with its own Content-Security-Policy RESPONSE header, so the frame document gets its own policy container; the script's HTML is pushed to it afterwards. Isolation is unchanged -- the opaque origin comes from the sandbox, not the scheme -- and the app's own script-src is untouched and still pinned by srcdocBridgeCsp.test.ts.
  Files: app/src-tauri/src/script_frame.rs (NEW: the loader document, its CSP, and the `calcula-frame` scheme constant. The loader IS the bridge -- an earlier design installed pushed content with document.open/write, whose steps erase every listener on the Window, so the loader would have lost its own `message` handler on the FIRST push and every interactive template would have painted its first state and frozen: this bug's own symptom, reproduced by its fix. Content therefore arrives as a BODY SWAP. It also mirrors the host router's identity check (`e.source !== parent`), without which a sandboxed sibling could post `calcula.setContent` at ANOTHER script's frame and have its markup installed there -- markup that passed neither that script's ui.html grant nor vHtml -- and then drive the victim's own sendMessage under the victim's id), app/src-tauri/src/lib.rs (register_uri_scheme_protocol for `calcula-frame`), app/src-tauri/tauri.conf.json (frame-src gains http://calcula-frame.localhost in BOTH csp and devCsp -- the PARENT's policy is consulted before the frame's own, so a missing entry refuses the frame before its CSP is ever read. script-src is UNCHANGED, which is the point), app/extensions/_shared/scriptFrame/frameDocument.ts (scriptFrameLoaderUrl + the ready/set-content contract + buildScriptFrameContent; buildScriptFrameDocument marked as having no production caller), app/extensions/_shared/scriptFrame/frameBridge.ts (router gains onReady; ready-gated delivery helpers, because the bridge is FETCHED now and a push sent before the listener exists is dropped in silence), app/extensions/Controls/Shape/shapeRenderer.ts + app/extensions/ControlsPane/components/CustomControlHost.tsx (both hosts: src= instead of srcdoc, content pushed on ready), app/e2e/tests/csp-srcdoc-bridge.spec.ts (the srcdoc half retired now that the loader is measured; a permanently-red test is how a suite acquires a known-failures list)

## BUG-0114 `[fixed]`

**Found:** 2026-09-15 (review)
**Oracle:** omitted-optional-argument-rejected

AN OMITTED OPTIONAL ARGUMENT IS REJECTED BY FIVE FUNCTIONS AND ACCEPTED BY TEN. Excel treats an empty argument slot as 'use the default': =SORT(range,,-1) sorts descending by column 1, and Microsoft's own SORT documentation writes it that way. Calcula answers #VALUE!. This is NOT a general parser gap — the empty slot is handled correctly by MATCH, ROUND, VLOOKUP, INDEX, FILTER, UNIQUE, SORTBY, TEXTSPLIT, IF and XLOOKUP — so it is per-function argument binding, and the inconsistency is the reason it survived: any single function a reviewer spot-checks is likely to be one of the ten that work.

MEASURED against C:/Users/Salle/AppData/Local/calcula-target/release/examples/eval-formulas.exe (the binary that scored the bake-off), each probe run as a PAIR — the slot omitted, and the documented default typed out — so a difference can only be the binding, never the semantics:
  =SORT(UNIQUE(A2:A8),,-1)  -> #VALUE!   while =SORT(UNIQUE(A2:A8),1,-1) -> spill 'Pear' x4
  =SORT(A2:A8,,1)           -> #VALUE!   while =SORT(A2:A8,1,1)          -> spill 'Apple' x7
  =SEQUENCE(B2,,B3,B4)      -> #VALUE!   while =SEQUENCE(B2,1,B3,B4)     -> spill 10 x4
  =SUBSTITUTE(A2,"p","P",)  -> #VALUE!   while =SUBSTITUTE(A2,"p","P")   -> "APPle"
  =WEEKDAY(D2,)             -> #VALUE!   while =WEEKDAY(D2,1)            -> 6

USER IMPACT, which is larger than the eval impact: a person who types the Excel form of a descending sort gets an error in the product. The AI angle is secondary — a model that writes correct Excel is marked wrong — and is how this was found, not why it matters.


## BUG-0115 `[fixed]`

**Found:** 2026-09-15 (review)
**Oracle:** criteria-date-literal-matches-nothing

A DATE LITERAL INSIDE A CRITERIA STRING MATCHES NOTHING, AND THE NEGATED FORM KEEPS THE ROW IT WAS TOLD TO DROP. Measured over a column of three typed dates (2025-01-01, 2025-01-15, 2024-06-30) with amounts 100/200/400:
  =COUNTIF(A2:A4,">=2025-01-01")        -> 0     Excel: 2
  =SUMIF(A2:A4,">=2025-01-01",B2:B4)    -> 0     Excel: 300
  =COUNTIF(A2:A4,"<>2025-01-15")        -> 3     Excel: 2   <-- THE WORST ONE
The concatenated forms are all correct, which localises the defect to literal parsing rather than comparison:
  =COUNTIF(A2:A4,">="&DATE(2025,1,1))   -> 2  OK
  =COUNTIF(A2:A4,">="&F1)               -> 2  OK
  =COUNTIF(A2:A4,">=45658")             -> 2  OK
CONTROL THAT MAKES THIS A DEFECT RATHER THAN A DESIGN CHOICE: =ISNUMBER(A2) is TRUE, so a cell typed 2025-01-15 stores a NUMBER. The range side therefore holds numbers while the criteria side refuses to produce one.

SUSPECTED CAUSE (reported by the finder, not re-derived here): ParsePolicy::CRITERIA sets dates: DateText::Reject (core/engine/src/number_text.rs:248-252), and parse_criteria (core/engine/src/evaluator.rs:6222-6305) then falls through to ExactText(whole string) at :6302 — a string no cell text ever equals. The policy's stated rationale is symmetry, 'a criteria and a cell must be read the same way', which is RIGHT for percent and currency where the cell really does store text, and INVERTS for dates, which do not.

The negated case is the one to fix first: '<>that date' counting the row that IS that date is not a zero anyone notices, it is a total that silently keeps the row the user excluded.


## BUG-0116 `[fixed]`

**Found:** 2026-09-15 (review)
**Oracle:** criteria-comparison-operators-ignore-text

FOUR OF THE FIVE CRITERIA COMPARISON OPERATORS DO NOT COMPARE TEXT AT ALL. Measured over {"Mango","Apple","Zebra"}:
  =COUNTIF(D2:D4,">=M")      -> 0     Excel: 2
  =COUNTIF(D2:D4,"<M")       -> 0     Excel: 1
  =COUNTIF(D2:D4,">Apple")   -> 0     Excel: 2
<, <=, > and >= have a numeric arm only and answer 0 rather than an error when handed text.
THE FAMILY IS INTERNALLY INCONSISTENT, which is the tell: <> DOES have a text arm (=COUNTIF(D2:D4,"<>Apple") -> 2, correct), and plain equality works (=COUNTIF(D2:D4,"Mango") -> 1). One of the five operators handles text and four do not.

Alphabetic range criteria are ordinary spreadsheet usage — 'count the customers from M onwards', 'everything before Apple' — and they silently answer zero.


## BUG-0117 `[fixed]`

**Found:** 2026-09-15 (review)
**Oracle:** criteria-argument-error-swallowed

AN ERROR IN THE CRITERIA ARGUMENT IS SWALLOWED AND BECOMES A CONFIDENT ZERO.
  =SUMIF(A2:A4,NA(),B2:B4)          -> 0     Excel: #N/A
  =SUMIF(A2:A4,1/0,B2:B4)           -> 0     Excel: #DIV/0!
  =SUMIFS(B2:B4,A2:A4,Nowhere)      -> 0     Excel: #NAME?   (Nowhere undefined)
  =COUNTIF(A2:A4,Nowhere)           -> 0     Excel: #NAME?
CONTROL: =SUM(Nowhere) correctly answers #NAME?, so the engine KNOWS the name is bad and discards that knowledge at the criteria boundary.
THE TELL: =AVERAGEIFS(B2:B4,A2:A4,NA()) answers #DIV/0! rather than #N/A — the original error was consumed and a new one manufactured by dividing over an empty match set. That proves the error is not propagating rather than merely being reformatted.

Reported cause: the catch-all `_ => CriteriaMatch::ExactText(String::new())` at core/engine/src/evaluator.rs:6304 maps every remaining EvalResult, errors included, to 'match nothing'.

A mistyped range name or an upstream #N/A therefore turns a total into a zero that looks like a real answer, in a function family whose entire job is to total things.


## BUG-0118 `[fixed]`

**Found:** 2026-09-15 (review)
**Oracle:** filter-one-argument-panics-the-evaluator

A ONE-ARGUMENT `=FILTER(A2:A9)` PANICS THE FORMULA EVALUATOR. Not an error value — a Rust panic:
  thread 'main' panicked at core/engine/src/evaluator.rs:9726:39:
  index out of bounds: the len is 1 but the index is 1

CAUSE, one line. `fn_filter` opened with
    if args.is_empty() || args.len() > 3 { return EvalResult::Error(CellError::Value); }
which rejects zero arguments and four, and lets EXACTLY ONE through — and the next statement is `self.eval_flat(&args[1])`, an unconditional index. The guard's floor is two, not one: FILTER(array, include, [if_empty]).

IT IS THE ONLY FUNCTION THAT DOES THIS. Forty-two variadic functions were probed one at a time, each in its own grader process so one panic could not hide another: SORTBY, XLOOKUP, VLOOKUP, HLOOKUP, INDEX, MATCH, SUMIF, COUNTIF, SUMIFS, AVERAGEIF, IFERROR, IFNA, TEXTJOIN, TEXTSPLIT, TEXTBEFORE, TEXTAFTER, SUBSTITUTE, REPLACE, MID, LEFT, ROUND, MROUND, LARGE, SMALL, RANK, PMT, NPV, SLOPE, CORREL, INTERCEPT, SEQUENCE, UNIQUE, SORT, CHOOSE, IF, DATEDIF, NETWORKDAYS, WORKDAY, EDATE, EOMONTH, YEARFRAC all answer #VALUE! when starved of arguments. FILTER was 1 of 42.

WHY THIS IS WORSE THAN A WRONG ANSWER. A panic has no cell representation. In the eval harness it killed a 181-task batch; in the product it is an evaluator thread dying on a formula the user typed. `=FILTER(A1:A5)` is exactly what a person leaves behind when they start the formula and commit before typing the condition.


## BUG-0119 `[fixed]`

**Found:** 2026-09-17 (live-proof)
**Oracle:** derived-measure-empty-home-table-refused-by-planner

A DERIVED MEASURE — one whose expression reaches its fact table only through [Measure] references (`Margin = [Revenue] - [Cost]`, `MarginPct = DIVIDE([Margin], [Revenue])`, `% Revenue of Total = GVAR total = [Revenue] RETURN [Revenue] / total`) — could not be queried on ANY model the host loads. `Measure::cached_table` is #[serde(skip)] and re-inferred from the expression alone on deserialization; `infer_fact_table` cannot see through a measure reference; and of every way a model reaches an engine only `Engine::load_model` ran `resolve_measure_home_tables`. Every host load path (create_connection_core, the .cala restore, the .calp subscribe and refresh) does serde + validate() + Engine::new, so the planner's `measure_tables` listed "" and refused the whole query with `QueryError::SourceNotRegistered("")` — an error naming no table. Insights' model route then lost EVERY measure's facts (it propagated the first refusal with `?`), and the pivot field list showed `table=` for derived measures.


## BUG-0120 `[fixed]`

**Found:** 2026-09-17 (live-proof)
**Oracle:** query-auto-refresh-skips-gvar-resolution

`Engine::query_auto_refresh` — the entry point behind the host's bi_query, column values, refresh, insights, cube and MCP — applied only the calculation-group expansion before planning, while `query` / `query_with_cancellation` / `query_explained` also fold ISFILTERED markers and resolve query-scoped (GVAR) bindings. A GVAR measure therefore reached the executor unresolved on the auto-refresh path and failed its guard ('reached the executor with an unresolved query-scoped (GVAR) binding'), while the same measure worked in a pivot (query_with_meta -> query). Three hand-copied overlay chains had drifted; the explain path had already been caught drifting once before (changelog: 'Engine::query_explained now resolves GVARs').


## BUG-0121 `[fixed]`

**Found:** 2026-09-18 (design-critique)
**Oracle:** ols-gate-ignores-scoped-filters

`Engine::enforce_object_level_security` inspected only the bare `filters` / `in_filters` / `or_filters` lists. Every host filter travels as a SCOPED filter (`scoped_filters` / `scoped_in_filters`, with an optional owning table and a level) — pivot slicers, `bi_query`, MCP `run_bi_query` all build them — so a caller under a role that denies `Geography[region]` could run `Revenue WHERE Geography[region] = x` (table-qualified) and bisect the denied column's values: the standard OLS inference oracle the gate's own bare-filter heuristic was written to refuse. Pre-existing on `query` (pivots) as well as on the paths that had no OLS at all.


## BUG-0122 `[fixed]`

**Found:** 2026-09-19 (manual)
**Oracle:** insert-function-category-filter-dead

Two of the Insert Function dialog's seven category buttons filtered the list to EMPTY. The filter compared a hard-coded id against a normalized category string, and the normalization disagreed with the ids it was written for: `"Date & Time".toLowerCase().replace(/[& ]/g, "_")` is `date___time` (space, ampersand, space — each replaced separately), not the `date_time` the id spelled, and `"Lookup & Reference"` became `lookup___reference` against an id of `lookup`. The equality fallback (`category.toLowerCase() === id`) missed for the same reason. Separately, the hard-coded list named 8 of the catalog's 15 categories, so Cube, Engineering, Dynamic Array, Information, Database, Writeback, UI and Matrix had no button at all.

**Repro:** app: `npx vitest run src/shell/FormulaBar/__tests__/insertFunctionDialog.test.tsx` — "actually filters on the ampersand categories that used to filter to nothing". Manually: open the fx dialog, click "Date & Time" or "Lookup & Reference"; pre-fix the list is empty.
**Triage:** app (confidence high) — Two independent spellings of the same category — an id written by hand in the dialog and a string shipped by the Rust catalog — kept in agreement by a normalizer that was never run against real category names.
**Fix:** fixed — The category list is DERIVED from the catalog `get_all_functions` returns and filters on exact string equality, so the label and the value it filters on are the same string and cannot drift. Every category the backend ships now gets a button; `null` means no filter, replacing a sentinel id. A small label map renames only `Math` -> `Math & Trig` and `UI` -> `Interface`.
  Files: app/src/shell/FormulaBar/InsertFunctionDialog.tsx, app/src/shell/FormulaBar/__tests__/insertFunctionDialog.test.tsx

## BUG-0123 `[fixed]`

**Found:** 2026-09-21 (manual)
**Oracle:** chart-axis-reverse-menu-writes-dead-field

"Reverse Axis" in a chart's axis context menu was a visible no-op. AxisContextMenu.tsx wrote a TOP-LEVEL `axis.reverse` (`toggleAxisProp("reverse", (axis as any).reverse)`), but the only reader of a reverse flag anywhere in the repository is `createScaleFromSpec` in rendering/scales.ts, which reads `axis.scale.reverse`. Clicking the item dirtied the workbook, saved the dead field into the .cala, repainted the chart unchanged, and left its own checkmark unlit because the checkmark read the same dead field. It also disagreed with ChartFormatPane's "Values in reverse order", which writes `scale.reverse` and works: the two controls for one setting could never agree, and neither showed the other's state.

**Repro:** app: `npx vitest run extensions/Charts/rendering/__tests__/furnitureAxisReverse.test.tsx` — "patches scale.reverse, and NOT a top-level axis.reverse". Manually: right-click a chart's value axis, click "Reverse Axis"; pre-fix the chart is unchanged and reopening the menu shows no checkmark, while Format Axis > "Values in reverse order" does reverse it.
**Triage:** app (confidence high) — Two spellings of one fact: AxisSpec.reverse and ScaleSpec.reverse both existed, only the nested one had a reader, and an `as any` cast at the single writer hid the mismatch from the compiler.
**Fix:** fixed — The menu reads and writes `axis.scale.reverse` — the flag the scale factory reads and the format pane writes — and the dead top-level `AxisSpec.reverse` is REMOVED from types.ts and from the JSON schema, so the gate now refuses it instead of persisting it. The `as any` is gone. Filed as part of CI-12, which also implemented the four other dead AxisSpec fields (majorUnit/minorUnit/minorTickMark/crossesAt) in drawCartesianAxes rather than removing them, since they have a working editor.
  Files: app/extensions/Charts/components/AxisContextMenu.tsx, app/extensions/Charts/types.ts, app/extensions/Charts/lib/chartSpecSchema.ts, app/extensions/Charts/lib/__tests__/chartSpecSchema.test.ts, app/extensions/Charts/rendering/__tests__/furnitureAxisReverse.test.tsx

## BUG-0124 `[fixed]`

**Found:** 2026-09-21 (manual)
**Oracle:** chart-delete-twice-on-cleared-title-deletes-chart

Delete pressed TWICE on a selected chart title deleted the whole chart. The listener branched on whether a WRITE had happened -- `if (handleChartTextDelete(chartId)) { ...; return; }` -- and fell through to `performChartDelete` when it returned false. `clearChartText` (app/extensions/Charts/handlers/chartTextEditing.ts:303) returns false when the title is ALREADY null (:306), and the reader reaches that state with the FIRST Delete, because nothing moves the selection off a cleared title: `revalidateSubSelection` (app/extensions/Charts/handlers/selectionHandler.ts:428) only ever re-checks the `series` and `dataPoint` rungs. So the second press -- the "did that work?" reflex, on a title that had visibly just vanished -- destroyed the chart, its spec, its per-point overrides and its data references.

**Repro:** Insert a chart that has a title. Click it once to select the chart, click its TITLE to select the title, press Delete (the title disappears -- correct), press Delete again. Pre-fix the whole chart is deleted. The helper half is pinned by a unit test: app: `npx vitest run extensions/Charts/handlers/__tests__/chartTextEditing.test.ts` -- "declines a second time - an already-absent title is not a write" (:622) asserts `handleChartTextDelete` returns FALSE on the second call, which is precisely the value the listener must NOT branch on.
**Triage:** app (confidence high) — A keystroke-ownership question was answered with a did-I-write-anything boolean. The two coincide everywhere except on an idempotent clear, and that is the one place where guessing wrong costs the document.
**Fix:** fixed — THE SUBJECT DECIDES WHO OWNS THE KEYSTROKE, NOT WHETHER A WRITE HAPPENED. `handleDeleteKey` now consumes the keystroke whenever the sub-selection is at `level: "element"` on a text element (`isChartTextElement`), calling `preventDefault`/`stopPropagation` and returning BEFORE the write is attempted; the return value is used only to decide whether to re-announce and repaint (app/extensions/Charts/index.ts:2161-2169). The legend branch immediately below it (:2178-2198) was written with the same shape for the same reason, so a selected legend is never a route to deleting the chart either, whether or not the spec actually changed.
  Files: app/extensions/Charts/index.ts, app/extensions/Charts/handlers/chartTextEditing.ts, app/extensions/Charts/handlers/__tests__/chartTextEditing.test.ts

## BUG-0125 `[fixed]`

**Found:** 2026-09-21 (manual)
**Oracle:** chart-delete-with-task-pane-button-focused-deletes-chart

Delete pressed while focus sat on a BUTTON in the chart's own Format task pane deleted the chart. The extension's capture-phase Delete listener asked only two questions -- `isKeyClaimed(e)` (a pointer claim, which is a GRID-OVERLAY concept and which a task-pane control does not and cannot carry) and `isTextEntryTarget(e.target)` (INPUT / TEXTAREA / contentEditable, none of which a `<button>` or a `<select>` is). Both said the keystroke was the chart's, so a Delete aimed at a tab button destroyed the chart. Three clicks from a fresh selection: select the chart, click a part of it to open the pane, click a tab.

**Repro:** app: `npx vitest run extensions/Charts/handlers/__tests__/chartKeyboardOwnership.test.ts` -- "REFUSES a Delete aimed at a focused <button> in a task pane (the data-loss case)" (:94), plus ":112" for a `<select>` and ":117" for the ribbon Design-panel button, against ":127" as the positive control (the grid container itself holds focus). Manually: select a chart, press Ctrl+1 to open the Format pane, click any tab button in it, press Delete -- pre-fix the chart is deleted.
**Triage:** app (confidence high) — A pointer claim answers "does a widget on the grid own this?" and a text-entry test answers "is the reader typing?". Neither answers "is the grid the subject?", and a focusable control that is neither an input nor an overlay falls through both.
**Fix:** fixed — A third rung, in the middle: `chartOwnsKeystroke` (app/extensions/Charts/handlers/selectionHandler.ts:472) is now ONE predicate -- `isKeyClaimed` -> `isGridFocused` -> `isTextEntryTarget` -- read by all three of the extension's capture-phase key listeners (Delete at app/extensions/Charts/index.ts:2142, the insight-cue arrow step at :2228, the element walk at :2286), because the question is the same for every one of them and a per-listener copy is a copy that drifts. `isGridFocused` is imported from Core's own `@api/keybindings` rather than re-derived: a second spelling of the `[data-focus-container="spreadsheet"]` lookup inside an extension would drift on the first change to that attribute.
  Files: app/extensions/Charts/handlers/selectionHandler.ts, app/extensions/Charts/index.ts, app/extensions/Charts/handlers/__tests__/chartKeyboardOwnership.test.ts
